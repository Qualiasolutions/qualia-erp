# Continue Here — 2026-05-07

## Last session (2026-05-07 evening)

Big portal-extension night. **All shipped to prod, all 4 deploy checks healthy.** Latest deploy: `2af3426c` at `portal.qualiasolutions.net`.

5 commits on `feat/client-request-cancel-delete` branch, pushed to GitHub. Tests 650/650, tsc clean.

## What shipped tonight

**Round 1 (early evening) — two features bundled in 2 commits + 1 test fix**

- `a301b034` — feat(portal): cancel/delete client feature requests. New `cancelFeatureRequest` + `deleteFeatureRequest` server actions. Cancel reuses `declined` status with `admin_response='Cancelled by client'` as marker (no DB constraint change). Delete cleans up storage attachments + request_comments.
- `d50e79d8` — feat(portal): client access to projects + Zoho PDF passthrough. Sidebar shows Projects to clients. New `ClientProjectListLoader` reads `client_projects` join. New `getZohoInvoicePdf()` + `/api/invoices/[zohoId]/pdf` route streams Zoho PDFs with auth check (admin → workspace; client → must match `client_projects` link).
- `c601a5f9` — test: updated has_pdf characterization for Zoho invoices

**Round 2 (after pivot — audit + slices 1+2) — 1 commit covering ~380 LOC**

- `d4f95f2d` — feat(portal): employee access to client requests + admin/team email on every client write event
  - **Security fix**: closed IDOR in `request-comments.ts` where any employee could comment on any request. Now scoped by `project_assignments`.
  - **New helper**: `lib/auth/is-staff-on-project.ts` — `isStaffOnProject(userId, projectId)` and `getEmployeeProjectIds(userId)`.
  - **Employee access**: removed redirect on `/requests`; employees now see requests on their assigned projects only. `getClientFeatureRequests` adds an employee branch using `getEmployeeProjectIds`.
  - **Email coverage**: `notifyAdminAndAssignedOfClientActivity` in `lib/email.ts`. Always emails `info@qualiasolutions.net`; optionally emails assigned employees (honors `client_activity` preference). Wired into: feature request created/cancelled/deleted, request comment, phase comment, portal message, file upload.

## Active client portal accounts (3)

| Profile          | Email                   | CRM client                             | Created    |
| ---------------- | ----------------------- | -------------------------------------- | ---------- |
| Tasos Kyriakides | an.kyriakides@gmail.com | (no link to CRM `clients` table)       | 2026-04-26 |
| GeoRise Media    | hello@georisemedia.com  | GeoRise Media → InAWay project         | 2026-05-05 |
| Underdog Sales   | gsc@underdogsales.com   | "Giuliu - Undersales Dog" → 3 projects | 2026-05-06 |

Hygiene item: Tasos's profile isn't linked to a CRM `clients` row.

## What's NOT done yet (pick up tomorrow)

### Slice 3 — Roadmap polish (skipped tonight, low priority)

- `getProjectPhases` returns ALL phases with descriptions regardless of role — clients might see internal-only phase notes. Add a role-based filter in the action or strip sensitive metadata before returning to clients.
- (Optional) flat `/roadmap` route for entry-point convenience.

### Other findings from the audit (deferred)

- `isUserManagerOrAbove` is misleadingly named (literally `isUserAdmin`). Plan to introduce `isStaffOnProject` was done; leaving the rename for later — too much surface area to refactor in one go.
- `notifyAssignedEmployees` in `lib/notifications.ts` runs under caller's RLS context; client triggers may silently fail to insert into `notifications`. Consider migrating to `createAdminClient()` for the insert.
- `docs/may-2026-team-brief.html` — untracked Qualia-themed team brief HTML (47KB). Decide whether to commit or delete.

### Branch state

- `feat/client-request-cancel-delete` — pushed to GitHub, 5 commits ahead of master, deployed to prod
- **NOT MERGED to master.** Open PR or merge tomorrow.
- Per CLAUDE.md, this is fine for direct-to-master rolling polish, but the branch name no longer reflects what's actually in it (much more than cancel/delete now).

## Verification

- Tests: 650/650 ✓
- tsc: clean ✓
- Deploy: HTTP 307 (root → /auth/login), 200 on /auth/login, 200 on /api/health, all < 2s ✓
- Production: https://portal.qualiasolutions.net

## Open questions / decisions to make tomorrow

1. Merge `feat/client-request-cancel-delete` → master, or rebase + create a fresh branch named for the actual content (e.g., `feat/portal-employee-access`)?
2. Slice 3 priority — ship it tomorrow or roll into the next polish round?
3. The team brief HTML — is that a one-off communication artifact or should it live in the repo?
