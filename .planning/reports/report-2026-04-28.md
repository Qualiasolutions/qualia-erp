# Session Report — 2026-04-28

**Project:** qualia-erp
**Employee:** qualiasolutions (Fawzi Goussous)
**Branch:** ops-diagnosis-fixes
**Phase:** Maintenance hotfix — operations diagnosis follow-up
**Date:** 2026-04-28
**Client report ID:** QS-REPORT-02
**Live commit:** 7e31f0c

## What Was Done

- Fixed two silent data-contract bugs flagged in the 2026-04-28 ops
  diagnosis: `/clients/[id]` now selects the live `projects.status`
  column (was `project_status`, which doesn't exist on that table) and
  `getAssignedVsDone` queries `project_assignments.employee_id` +
  `removed_at IS NULL` (was `profile_id` + `status='active'`). Added
  regression tests pinning both contracts.
- Built admin "Planning cleanup needed" panel on the Overview tab —
  shows live counts of active projects without target_date, active
  phases missing start/target dates, open project tasks with no phase,
  and open tasks with no due date. The retired Unphased pseudo-bucket
  is not re-introduced.
- Tightened employee workflow: project clock-in is a two-step flow
  with planned-outcome textarea and duration chips; `clockIn` server
  action now requires both for project sessions; clock-out copy says
  "(required for project sessions)" instead of "(recommended)".
- Removed hardcoded "Daily Research / Blog" preset from the modal —
  moved to `lib/work-presets.ts` behind a config layer that binds by
  stable project ID with name fallback.
- Added audited `adminOverrideClockOut()` server action + Force-close
  button in admin team view (codex follow-up landed `0636a71` with the
  UI hookup and the `activity_log` action_type CHECK migration).
- Built admin Framework Report Completeness panel (last 100 reports
  scored on client_id, framework_version, client_report_id,
  framework_project_id, team_id, per-user token auth) and the
  Client/project access drift banner (`/clients` shows three drift
  classes — owned-no-portal-access, portal-access-owner-mismatch,
  portal-access-to-archived).
- Shipped two production Supabase migrations: `advisor_safe_hardening`
  (pins search_path on `api_tokens_scope_is_valid`, adds RESTRICTIVE
  deny-all policies on the four RLS-no-policy server-only tables) and
  `activity_log_session_override_action` (adds `session_admin_overrode`
  to the activity_log action_type CHECK constraint).

## Blockers

None.

## Verification

- `npx tsc --noEmit` — clean
- `npm run lint` — clean
- `npm test -- --runInBand` — 31 suites / 660 tests / 0 failures (5
  new tests added across two new files)
- `npm run build` — clean
- Production health: HTTP 200, version `7e31f0c`, DB up at 133ms, HSTS
  enforced, auth/login responds 200
- Both Supabase migrations applied to production
  (project ref `vbpzaiqovffpsroxaulv`)

## Next Steps

1. Run authenticated browser QA at admin/employee/client roles using a
   rotated password — the diagnosis explicitly notes that any password
   pasted in chat must be rotated before this step.
2. Set `SENTRY_AUTH_TOKEN` in the Vercel project env so source-map
   uploads run cleanly on subsequent builds.
3. Bulk-review the ~25 SECURITY DEFINER functions still flagged by the
   advisor (mostly RLS helpers — needs case-by-case revoke decisions).
4. Toggle leaked-password protection in Supabase Auth → Settings.

## Commits

- 7e31f0c chore: ignore .qa-screenshots browser-qa output
- 0636a71 fix(ops): wire planning cleanup drilldowns
- 26bfdec fix(ops): operational hardening from 2026-04-28 diagnosis

## Production deployment

- URL: https://portal.qualiasolutions.net
- Deployment: dpl_BpHF1wMKmk6bzQEswBW6absZ34Qe (Vercel team
  qualiasolutionscy)
- Live version: 7e31f0c
- Aliases: portal.qualiasolutions.net (verified resolving)
