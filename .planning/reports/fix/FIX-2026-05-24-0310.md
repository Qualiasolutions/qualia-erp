# Fix Report - 2026-05-24 03:10

**Symptom:** GitHub Actions billing-style check failures keep appearing, and the 7Buddas direct form link was not safe to email until verified.
**Mode:** general
**Outcome:** fixed

## Feedback Loop

- Initial failing check: live 7Buddas form URL redirected to `/auth/login?brief=1`, dropping the project path.
- Final passing checks: local lint, typecheck, full Jest, and production build.

## Root Cause

`proxy.ts` - unauthenticated redirects cloned the current query string but replaced only the pathname with `/auth/login`, so `/projects/{id}?brief=1` became `/auth/login?brief=1`.

## Patch

- Files changed: `.github/workflows/*.yml`, `proxy.ts`, `app/auth/login/page.tsx`, `components/login-form.tsx`.
- Change summary: made GitHub workflows manual-only, preserved a safe `next` path through login, and redirected successful login back to the requested project form URL.

## Verification

- `npm run lint` - PASS
- `npx tsc --noEmit` - PASS
- `npm test -- admin-client-form-link-dialog project-workflow qualia-home-view --runInBand` - PASS
- `npm test -- --runInBand` - PASS
- `npm run build` - PASS

## Remaining Notes

- Zoho Mail tools are not exposed in this session, so email sending uses the available production email path only after the live link is verified.
