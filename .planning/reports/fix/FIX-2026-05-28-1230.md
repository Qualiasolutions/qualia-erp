# Fix Report — 2026-05-28 12:30

**Symptom:** Two "admin" buttons in the sidebar, no dashboard button for admins
**Mode:** general
**Outcome:** fixed

## Feedback Loop

- Initial inspection: `grep -n "id:.*'admin\|label:.*[Aa]dmin\|label:.*[Dd]ashboard" components/portal/qualia-sidebar.tsx`
- Final check: `npx tsc --noEmit` — exits 0

## Root Cause

`components/portal/qualia-sidebar.tsx:52` — the `admin-dashboard` PageDef was labeled "Admin" and pointed to `/admin`, exactly duplicating `admin-panel` ("Admin Panel" → `/admin`) that came in via the master merge. Both items rendered side-by-side for admin users, while admins had no entry pointing to `/dashboard`. The duplicate was a merge artifact — pre-merge the branch only had `admin-dashboard`, and master added `admin-panel`. Combining the two ROLE_ORDER lists exposed both for admins.

## Patch

- Files changed: `components/portal/qualia-sidebar.tsx`
- Change summary: Repurposed the `admin-dashboard` item to its actual job — label "Dashboard", icon `home`, href `/dashboard`, appKey `home`. `admin-panel` retains its role as the "Admin Panel" → `/admin` entry.

## Verification

- `npx tsc --noEmit` — PASS (no errors)
- ROLE_ORDER for admin now resolves to: Dashboard → Projects → Schedule → Requests → Clients → Knowledge → Admin Panel → Settings. No duplicates.

## Remaining Notes

- None.
