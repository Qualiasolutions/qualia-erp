---
phase: 01-trainee-interactive-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/project-pipeline/task-instruction-card.tsx
  - components/project-pipeline/phase-tasks.tsx
  - app/actions/pipeline.ts
  - types/database.ts
autonomous: true

must_haves:
  truths:
    - "Employee sees each phase's tasks as interactive checklist with helper text"
    - 'Each task shows title, helper text, and checkbox'
    - 'GSD command appears as copyable code block per task'
    - 'Phase progress bar updates when tasks checked'
  artifacts:
    - path: 'components/project-pipeline/task-instruction-card.tsx'
      provides: 'Specialized task card with helper text and GSD commands'
      min_lines: 80
    - path: 'components/project-pipeline/phase-tasks.tsx'
      provides: 'Renders task instruction cards instead of generic tasks'
      exports: ['PhaseTasks']
    - path: 'app/actions/pipeline.ts'
      provides: 'Task toggle handler for phase_items'
      exports: ['togglePhaseTask']
  key_links:
    - from: 'components/project-pipeline/phase-tasks.tsx'
      to: 'task-instruction-card.tsx'
      via: 'component import'
      pattern: 'import.*TaskInstructionCard'
    - from: 'components/project-pipeline/task-instruction-card.tsx'
      to: 'app/actions/pipeline.ts'
      via: 'togglePhaseTask action'
      pattern: 'togglePhaseTask'
---

<objective>
Transform phase tasks from generic task items into interactive instruction cards with trainee-friendly guidance.

Purpose: Moayad needs clear step-by-step guidance with helper text and GSD commands visible per task, not generic task cards with just title/description.

Output: Interactive task checklist component using phase_items table with helperText, copyable GSD commands, and checkbox completion tracking.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/gsd-templates.ts
@components/project-pipeline/phase-card.tsx
@components/project-pipeline/phase-tasks.tsx
@components/project-pipeline/task-item.tsx
@app/actions/phases.ts
@types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Create TaskInstructionCard component</name>
  <files>components/project-pipeline/task-instruction-card.tsx</files>
  <action>
Create a new specialized component for trainee task instructions:

1. Accept props: phaseItem (from phase_items table), onToggle handler, disabled state
2. Display structure:
   - Checkbox (18x18px) on left using shadcn Checkbox
   - Task title (text-sm font-medium)
   - Helper text below title (text-xs text-muted-foreground with Info icon)
   - If templateKey matches GSD command pattern, show copyable code block with Copy button
   - Fade to 50% opacity when completed
3. Use getPhasePromptData() from gsd-templates.ts to extract GSD command from templateKey
4. Copy button uses navigator.clipboard.writeText() with toast notification
5. Styling matches task-item.tsx aesthetic (group hover, rounded-lg, smooth transitions)
6. Use motion.div from framer-motion for smooth completion animation
7. Export as memo(TaskInstructionCard) for performance

Do NOT modify existing task-item.tsx — create separate component for phase_items.
</action>
<verify>
File exists at components/project-pipeline/task-instruction-card.tsx
Component exports TaskInstructionCard
grep "getPhasePromptData" components/project-pipeline/task-instruction-card.tsx returns match
Component includes Copy button with clipboard API
</verify>
<done>
TaskInstructionCard component exists and renders phase_items with helper text, copyable GSD commands, and completion checkbox
</done>
</task>

<task type="auto">
  <name>Add togglePhaseTask server action</name>
  <files>app/actions/pipeline.ts</files>
  <action>
Add new server action to handle phase_items checkbox toggling:

1. Function signature: `togglePhaseTask(itemId: string, phaseId: string): Promise<ActionResult>`
2. Logic:
   - Get current is_completed state from phase_items WHERE id = itemId
   - Toggle to opposite boolean value
   - If completing (false → true), set completed_at = now(), completed_by = auth.uid()
   - If uncompleting (true → false), set completed_at = null, completed_by = null
   - Update phase_items record
   - Call checkPhaseProgress(phaseId) to auto-complete phase if all tasks done
   - revalidatePath for project detail and roadmap pages
3. Return ActionResult pattern: { success: boolean; error?: string; data?: unknown }
4. Add proper error handling with try/catch
5. Import createClient from @/lib/supabase/server
6. Export from app/actions/pipeline.ts

This is separate from the existing task actions in app/actions/inbox.ts (which handle tasks table).
</action>
<verify>
grep "togglePhaseTask" app/actions/pipeline.ts returns function definition
Function calls checkPhaseProgress from app/actions/phases.ts
Function includes revalidatePath calls
</verify>
<done>
togglePhaseTask server action exists and toggles phase_items.is_completed with progress checking
</done>
</task>

<task type="auto">
  <name>Update PhaseTasks to use phase_items</name>
  <files>components/project-pipeline/phase-tasks.tsx</files>
  <action>
Refactor PhaseTasks component to render TaskInstructionCard for phase_items:

1. Change data source:
   - Instead of fetching from tasks table, fetch from phase_items WHERE phase_id = phaseId
   - ORDER BY display_order ASC
   - Select: id, title, description, helper_text, template_key, is_completed, display_order
2. Update fetch logic in phase-card.tsx getPhaseItems() call (or create if missing)
3. Replace TaskItem with TaskInstructionCard in the map
4. Remove "Add task" input — phase_items are predefined from GSD templates, not user-created
5. Keep sorting: incomplete first, then by display_order
6. Update props interface to accept PhaseItem[] instead of Task[]
7. Pass togglePhaseTask action to TaskInstructionCard onToggle handler
8. Keep AnimatePresence for smooth animations

Phase tasks are NOT user-editable checklists — they come from GSD templates. Users can add custom tasks to tasks table separately.
</action>
<verify>
grep "phase_items" components/project-pipeline/phase-tasks.tsx returns table reference
grep "TaskInstructionCard" components/project-pipeline/phase-tasks.tsx returns component usage
Component no longer shows "Add task" input
</verify>
<done>
PhaseTasks renders TaskInstructionCard components from phase_items table with helper text visible and no add-task input
</done>
</task>

</tasks>

<verification>
1. Navigate to project detail page with GSD phases (e.g., web_design project)
2. Expand a phase (e.g., SETUP)
3. Verify tasks show:
   - Checkbox on left
   - Task title
   - Helper text below title with Info icon
   - Copyable GSD command code block (if applicable)
4. Check a task checkbox
5. Verify:
   - Task fades to 50% opacity
   - Phase progress bar updates immediately
   - Database phase_items.is_completed = true
6. Uncheck the task
7. Verify task returns to 100% opacity and is_completed = false
8. Check all tasks in a phase
9. Verify phase auto-completes (if auto_progress enabled)
</verification>

<success_criteria>

1. Phase tasks display as interactive instruction cards with helper text visible
2. Each task shows title, helper text, and copyable GSD command
3. Clicking checkbox toggles is_completed in phase_items table
4. Progress bar updates automatically when tasks checked/unchecked
5. No "Add task" input appears (phase tasks are template-driven)
6. Completed tasks fade to 50% opacity
7. checkPhaseProgress runs after toggle to auto-complete phase
   </success_criteria>

<output>
After completion, create `.planning/phases/01-trainee-interactive-system/01-01-SUMMARY.md`
</output>
