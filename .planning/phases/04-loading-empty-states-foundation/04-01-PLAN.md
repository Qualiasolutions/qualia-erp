---
phase: 04-loading-empty-states-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/portal/portal-skeletons.tsx
  - app/portal/loading.tsx
  - app/portal/[id]/loading.tsx
  - app/portal/[id]/updates/loading.tsx
autonomous: true

must_haves:
  truths:
    - 'User sees content-shaped skeleton while portal projects page loads'
    - 'User sees content-shaped skeleton while portal roadmap page loads'
    - 'User sees content-shaped skeleton while portal activity feed loads'
  artifacts:
    - path: 'components/portal/portal-skeletons.tsx'
      provides: 'Portal-specific skeleton components matching real content layout'
      exports: ['PortalProjectCardSkeleton', 'PortalRoadmapSkeleton', 'PortalActivitySkeleton']
    - path: 'app/portal/loading.tsx'
      provides: 'Loading state for portal projects list page'
      min_lines: 10
    - path: 'app/portal/[id]/loading.tsx'
      provides: 'Loading state for portal roadmap page'
      min_lines: 10
    - path: 'app/portal/[id]/updates/loading.tsx'
      provides: 'Loading state for portal activity feed page'
      min_lines: 10
  key_links:
    - from: 'app/portal/loading.tsx'
      to: 'components/portal/portal-skeletons.tsx'
      via: 'import statement'
      pattern: 'import.*PortalProjectCardSkeleton'
    - from: 'app/portal/[id]/loading.tsx'
      to: 'components/portal/portal-skeletons.tsx'
      via: 'import statement'
      pattern: 'import.*PortalRoadmapSkeleton'
---

<objective>
Create content-shaped loading skeletons for all portal pages that match the real content layout.

Purpose: Users see polished shimmer skeletons (not "Loading..." text) while portal pages load, matching the exact shape of the content that will appear.

Output: Portal skeleton components + loading.tsx files for all portal pages.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@components/ui/skeleton.tsx
@components/ui/skeleton-variants.tsx
@components/portal/portal-projects-list.tsx
@components/portal/portal-roadmap.tsx
@components/portal/portal-activity-feed.tsx
@app/portal/page.tsx
@app/portal/[id]/page.tsx
@app/portal/[id]/updates/page.tsx
@app/portal/[id]/files/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Create portal skeleton components</name>
  <files>components/portal/portal-skeletons.tsx</files>
  <action>
Create new file with portal-specific skeleton variants matching exact layouts:

1. **PortalProjectCardSkeleton** - Matches the project card from portal-projects-list.tsx:
   - Card wrapper with hover transition classes
   - CardHeader with title skeleton (h-5 w-3/4) and badge skeleton (h-6 w-16)
   - Description skeleton (2 lines, h-3)
   - CardContent with progress label (h-3 w-24) + percentage (h-3 w-8)
   - Progress bar skeleton (h-2 w-full)
   - Footer row with project type (h-3 w-20) and "View Details" (h-3 w-24)

2. **PortalRoadmapSkeleton** - Matches phase cards from portal-roadmap.tsx:
   - Space-y-4 container
   - 3 phase card skeletons each with:
     - Phase header (h-6 w-48) + status badge (h-6 w-20)
     - Description (2 lines, h-3)
     - Date row with start/target dates (h-3 w-32 each)
     - Progress bar (h-2 w-full)

3. **PortalActivitySkeleton** - Matches timeline from portal-activity-feed.tsx:
   - Date header (h-4 w-24)
   - Timeline container with connecting line (absolute left-2.5 w-px bg-muted)
   - 3 activity items each with:
     - Icon circle (h-5 w-5 rounded-full)
     - Card with message (h-4 w-full) + metadata row (h-3 w-32)

4. **PortalAdminPanelSkeleton** - Matches admin panel tables:
   - Tabs skeleton (h-10 w-full)
   - Table header row (h-10)
   - 5 table row skeletons (h-12 each)

Use existing Skeleton component from @/components/ui/skeleton. Import Card components from @/components/ui/card for proper layout structure. Add 'use client' directive.
</action>
<verify>

1. File compiles without errors: `npx tsc --noEmit`
2. Skeleton components match real component layouts (compare side-by-side in code)
3. All skeletons use animate-shimmer from existing Skeleton component
   </verify>
   <done>
   Portal skeleton components exist and export PortalProjectCardSkeleton, PortalRoadmapSkeleton, PortalActivitySkeleton, PortalAdminPanelSkeleton. Each matches the layout of its corresponding real component.
   </done>
   </task>

<task type="auto">
  <name>Create loading.tsx files for portal routes</name>
  <files>
app/portal/loading.tsx
app/portal/[id]/loading.tsx
app/portal/[id]/updates/loading.tsx
  </files>
  <action>
Create 3 loading.tsx files matching the layouts of their respective pages:

**app/portal/loading.tsx** (portal projects list):

```tsx
import { PortalProjectCardSkeleton } from '@/components/portal/portal-skeletons';

export default function PortalLoading() {
  return (
    <div className="space-y-6">
      {/* Header skeleton */}
      <div className="space-y-1">
        <div className="h-8 w-48 animate-shimmer rounded bg-muted" />
        <div className="h-4 w-96 animate-shimmer rounded bg-muted" />
      </div>

      {/* Grid of project cards */}
      <div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
        {[1, 2, 3, 4, 5, 6].map((i) => (
          <PortalProjectCardSkeleton key={i} />
        ))}
      </div>
    </div>
  );
}
```

**app/portal/[id]/loading.tsx** (portal roadmap):

```tsx
import { PortalRoadmapSkeleton } from '@/components/portal/portal-skeletons';
import { Skeleton } from '@/components/ui/skeleton';

export default function PortalProjectLoading() {
  return (
    <div className="space-y-6">
      {/* Page header skeleton */}
      <div className="space-y-2">
        <Skeleton className="h-8 w-64" />
        <Skeleton className="h-4 w-full max-w-2xl" />
      </div>

      {/* Tabs skeleton */}
      <Skeleton className="h-10 w-full" />

      {/* Roadmap content */}
      <PortalRoadmapSkeleton />
    </div>
  );
}
```

**app/portal/[id]/updates/loading.tsx** (portal activity feed):

```tsx
import { PortalActivitySkeleton } from '@/components/portal/portal-skeletons';
import { Skeleton } from '@/components/ui/skeleton';

export default function PortalUpdatesLoading() {
  return (
    <div className="space-y-6">
      {/* Page header skeleton */}
      <div className="space-y-2">
        <Skeleton className="h-8 w-64" />
        <Skeleton className="h-4 w-full max-w-2xl" />
      </div>

      {/* Tabs skeleton */}
      <Skeleton className="h-10 w-full" />

      {/* Info banner skeleton */}
      <Skeleton className="h-16 w-full rounded-lg" />

      {/* Activity feed */}
      <PortalActivitySkeleton />
    </div>
  );
}
```

Note: Files page already has Suspense + fallback skeleton, no loading.tsx needed there.
</action>
<verify>

1. All 3 files compile: `npx tsc --noEmit`
2. Navigate to /portal in browser → see project cards skeleton
3. Navigate to /portal/[some-id] → see roadmap skeleton
4. Navigate to /portal/[some-id]/updates → see activity skeleton
5. No "Loading..." text visible, only shimmer skeletons
   </verify>
   <done>
   All portal routes have loading.tsx files. Navigating to any portal page shows content-shaped skeleton matching real layout. No "Loading..." text appears.
   </done>
   </task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. All portal pages show shimmer skeletons on load (not "Loading..." text)
3. Skeletons match real content shapes (cards, timelines, grids)
4. Skeleton animation is smooth (shimmer gradient)
</verification>

<success_criteria>

- Portal projects page shows grid of project card skeletons on load
- Portal roadmap page shows phase card skeletons on load
- Portal activity feed shows timeline skeletons on load
- No "Loading..." text visible anywhere in portal
- All skeletons use shimmer animation from existing system
  </success_criteria>

<output>
After completion, create `.planning/phases/04-loading-empty-states-foundation/04-01-SUMMARY.md`
</output>
