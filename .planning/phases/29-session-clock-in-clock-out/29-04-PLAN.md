---
phase: 29-session-clock-in-clock-out
plan: 04
type: execute
wave: 3
depends_on: [29-01, 29-02, 29-03]
files_modified:
  - app/admin/attendance/page.tsx
autonomous: true

must_haves:
  truths:
    - 'Admin attendance page shows work_sessions data: employee name, project, clock-in time, clock-out time, duration, summary'
    - "Active sessions (ended_at IS NULL) show a live 'Active' badge"
    - 'Admin can filter by team member'
    - 'Old morning/evening check-in imports are removed from the page'
    - 'Multiple sessions per day for the same employee each appear as separate rows'
  artifacts:
    - path: 'app/admin/attendance/page.tsx'
      provides: 'Rewritten attendance page using getSessionsAdmin'
      contains: 'getSessionsAdmin'
  key_links:
    - from: 'app/admin/attendance/page.tsx'
      to: 'app/actions/work-sessions.ts getSessionsAdmin'
      via: 'called to load session data'
      pattern: 'getSessionsAdmin'
---

<objective>
Replace the morning/evening check-in based attendance page with a session-based view that queries work_sessions.

Purpose: Implements CLEAN-03 (remove checkin references from admin attendance) and satisfies success criterion 6 (session data in attendance page).
Output: Rewritten app/admin/attendance/page.tsx using getSessionsAdmin with new columns (project, duration, summary).
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/actions/work-sessions.ts
@app/admin/attendance/page.tsx
@app/actions/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite attendance page to use session model</name>
  <files>app/admin/attendance/page.tsx</files>
  <action>
Read app/admin/attendance/page.tsx fully before editing. Then fully replace the file content.

The new page keeps the same outer layout (header with title + filter, table below) but queries `getSessionsAdmin` instead of `getAttendanceLogs`.

**New implementation:**

```
'use client'
```

**Imports to keep:** useEffect, useState, useCallback, format/parseISO from date-fns, Badge, Table components, Select components, relevant lucide icons (Clock, AlertTriangle, CheckCircle2, User, Briefcase). Add: `formatDistanceStrict` from date-fns.

**Replace:** `getAttendanceLogs` from checkins → `getSessionsAdmin` from `@/app/actions/work-sessions`. Keep `getTeamMembers` from `@/app/actions/admin` and `getCurrentWorkspaceId` from `@/app/actions`.

**Type:**

```ts
type SessionEntry = Awaited<ReturnType<typeof getSessionsAdmin>>[number];
```

**State:** Same pattern as current — `sessions`, `members`, `loading`, `filterProfile`, `workspaceId`.

**Table columns (replace old columns):**

| Column    | Width     | Content                                                                                                             |
| --------- | --------- | ------------------------------------------------------------------------------------------------------------------- |
| Date      | 140px     | format(parseISO(session.started_at), 'MMM d, yyyy')                                                                 |
| Employee  | 180px     | Avatar initial + full_name (same style as current)                                                                  |
| Project   | 160px     | Briefcase icon + session.project?.name ?? '—'                                                                       |
| Clock In  | 100px     | format(parseISO(session.started_at), 'h:mm a')                                                                      |
| Clock Out | 100px     | session.ended_at ? format(parseISO(session.ended_at), 'h:mm a') : '—'                                               |
| Duration  | 90px      | session.duration_minutes ? formatDuration(session.duration_minutes) : (session.ended_at ? '—' : <ActiveBadge />)    |
| Summary   | remaining | session.summary ? truncated text (max 80 chars with ellipsis) : <span className="text-muted-foreground/40">—</span> |

**ActiveBadge** (inline component or inline JSX):

```tsx
<Badge
  variant="outline"
  className="gap-1 border-blue-500/20 bg-blue-500/10 text-[10px] text-blue-600"
>
  <span className="inline-block h-1.5 w-1.5 animate-pulse rounded-full bg-blue-500" />
  Active
</Badge>
```

**formatDuration helper** (add at top of file, outside component):

```ts
function formatDuration(minutes: number): string {
  if (minutes < 60) return `${minutes}m`;
  const h = Math.floor(minutes / 60);
  const m = minutes % 60;
  return m > 0 ? `${h}h ${m}m` : `${h}h`;
}
```

**fetchData** function: call `getSessionsAdmin(workspaceId, { limit: 100, profileId: filterProfile !== 'all' ? filterProfile : undefined })`. Set results to `sessions` state.

**Row key:** `session.id` (unique per session row, unlike the old date+profile composite).

**Page header text:** Keep "Attendance Log" title. Change subtitle to "Work session records for all team members".

**Empty state:** Keep same empty state design (Clock icon + "No attendance records yet.").

Remove all references to `getAttendanceLogs`, `LateIndicator`, `planned_clock_out_time`, `completed_tasks`, and any morning/evening check-in concepts.
</action>
<verify>npx tsc --noEmit 2>&1 | grep attendance</verify>
<done>app/admin/attendance/page.tsx uses getSessionsAdmin, shows correct columns (project, clock in, clock out, duration, summary), TypeScript compiles clean, no references to getAttendanceLogs or daily_checkins</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — zero errors.
Run `grep -n "getAttendanceLogs\|checkins\|morning\|evening\|LateIndicator" app/admin/attendance/page.tsx` — zero results.
Run `grep -n "getSessionsAdmin\|duration_minutes\|summary" app/admin/attendance/page.tsx` — all three found.
Run `npm run build` — builds without errors.
</verification>

<success_criteria>

- Attendance page queries work_sessions via getSessionsAdmin
- Table shows: date, employee, project, clock-in, clock-out, duration, summary
- Active sessions show animated "Active" badge in duration column
- Multiple sessions per employee per day each appear as separate rows
- Old check-in imports fully removed
- TypeScript and build both pass
  </success_criteria>

<output>
After completion, create `.planning/phases/29-session-clock-in-clock-out/29-04-SUMMARY.md`
</output>
