---
phase: 18-invitation-system
plan: 02
type: execute
wave: 2
depends_on: ['18-01']
files_modified:
  - lib/email.ts
  - app/admin/projects/import/project-import-list.tsx
  - components/admin/send-invitation-modal.tsx
autonomous: true

must_haves:
  truths:
    - 'Admin can enter client email address for portal-enabled project'
    - 'Admin can click Send Invitation and email is sent via Resend'
    - 'Invitation email contains secure signup link with token'
    - 'Invitation email displays project-specific welcome message from portal settings'
    - 'Admin receives success confirmation after sending invitation'
  artifacts:
    - path: 'lib/email.ts'
      provides: 'Invitation email template function'
      exports: ['sendClientInvitation']
      min_lines: 80
    - path: 'components/admin/send-invitation-modal.tsx'
      provides: 'Modal UI for entering client email and sending invitation'
      min_lines: 150
    - path: 'app/admin/projects/import/project-import-list.tsx'
      provides: 'Send Invitation button in bulk toolbar'
      contains: 'Send Invitation'
  key_links:
    - from: 'components/admin/send-invitation-modal.tsx'
      to: 'app/actions/client-invitations.ts'
      via: 'createInvitation server action call'
      pattern: 'createInvitation'
    - from: 'lib/email.ts'
      to: 'client_invitations table'
      via: 'invitation token in signup URL'
      pattern: 'invitation_token'
    - from: 'app/admin/projects/import/project-import-list.tsx'
      to: 'components/admin/send-invitation-modal.tsx'
      via: 'modal state and trigger'
      pattern: 'SendInvitationModal'
---

<objective>
Build invitation email template and admin UI for sending client invitations with secure signup links and project-specific welcome messages.

Purpose: Enable admins to invite clients to portal-enabled projects with one-click email sending.
Output: Resend email template, send invitation modal, UI integration
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-invitation-system/18-01-SUMMARY.md
@.planning/phases/17-project-import-flow/17-03-SUMMARY.md
@.planning/phases/14-unified-notification-system/14-02-SUMMARY.md
@lib/email.ts
@app/admin/projects/import/project-import-list.tsx
@components/admin/portal-settings-modal.tsx
@~/.claude/skills/frontend-master/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create invitation email template</name>
  <files>lib/email.ts</files>
  <action>
Add sendClientInvitation function to lib/email.ts following Phase 14 email template pattern:

**Function signature:**

```typescript
export async function sendClientInvitation(params: {
  projectId: string;
  projectName: string;
  email: string;
  invitationToken: string;
  welcomeMessage?: string;
}): Promise<void>;
```

**Email template structure (match Phase 14 pattern):**

1. **Subject:** "You're invited to view {projectName} on Qualia"

2. **Header:** Qualia teal gradient background (#00A4AC to #008B92), white text, "You're Invited" heading

3. **Body content:**
   - Greeting: "Hi there,"
   - Introduction: "You've been invited to access the Qualia client portal for **{projectName}**."
   - Welcome message box (if provided from portal_settings): Light teal background, padding, italicized custom message
   - Instructions: "Click the button below to create your account and access your project dashboard, roadmap, files, and updates."

4. **CTA Button:**
   - Text: "Create Account & View Project"
   - Link: `${APP_URL}/auth/signup?token=${invitationToken}`
   - Qualia teal background, white text, rounded, centered

5. **Footer:**
   - "If you have any questions, reply to this email or contact us at info@qualiasolutions.net"
   - "This invitation link is secure and can only be used once."

**Implementation details:**

- Use getResendClient() pattern from Phase 14
- FROM_EMAIL: 'Qualia Platform <notifications@qualiasolutions.net>'
- HTML template with inline styles for email client compatibility
- Silent failure pattern: catch errors, log to console, return void
- No blocking if Resend not configured (development mode)

**Styling pattern (from Phase 14):**

- Qualia teal gradient: linear-gradient(135deg, #00A4AC 0%, #008B92 100%)
- White background for main content
- Border radius: 8px for boxes
- Padding: 24px for containers, 16px for boxes
- Font: system-ui, -apple-system, sans-serif
- Button: 16px padding vertical, 32px horizontal, border-radius 6px
  </action>
  <verify>

```bash
# Check function exists
grep -q "sendClientInvitation" lib/email.ts && echo "✓ Email function added"

# Verify Resend pattern
grep -q "getResendClient()" lib/email.ts && echo "✓ Uses Resend client pattern"

# Check for signup URL with token
grep -q "auth/signup?token=" lib/email.ts && echo "✓ Invitation link includes token"

# TypeScript check
npx tsc --noEmit
```

  </verify>
  <done>
sendClientInvitation function exists in lib/email.ts, uses Resend client pattern, includes secure signup URL with token parameter, matches Phase 14 email template styling with Qualia teal branding, TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build send invitation modal and wire to import page</name>
  <files>
components/admin/send-invitation-modal.tsx
app/admin/projects/import/project-import-list.tsx
  </files>
  <action>
**1. Create SendInvitationModal component (components/admin/send-invitation-modal.tsx):**

Import shadcn Dialog, Input, Button, Label, toast from sonner, createInvitation and getProjectsForPortalImport from actions, invitationSchema from validation, sendClientInvitation from lib/email.

**Component structure:**

- Props: { project: ProjectForImport, open: boolean, onOpenChange: (open: boolean) => void, onSuccess: () => void }
- State: email (string), isSubmitting (boolean)
- Form validation: Use invitationSchema client-side before submission

**UI layout (match PortalSettingsModal pattern from Phase 17):**

- Dialog with title "Send Client Invitation"
- Project name displayed as badge (gray background)
- Email input field with label "Client Email Address"
- Info banner (blue background): "The client will receive an email with a secure link to create their account and access {projectName}."
- Welcome message preview (if project has portal_settings): Light teal box showing configured message
- Visibility settings preview: "This project is configured to show: Roadmap, Files, Comments" (read from portal_settings)
- Primary button: "Send Invitation" (qualia teal, disabled when submitting or email invalid)
- Cancel button: secondary variant

**Submit handler:**

1. Validate email with invitationSchema.parse({ projectId, email })
2. Call createInvitation({ projectId, email }) server action
3. If success, call sendClientInvitation({ projectId, projectName, email, invitationToken, welcomeMessage })
4. Show success toast: "Invitation sent to {email}"
5. Call onSuccess() to refresh parent and close modal
6. Error: Show error toast with message

**Loading state:** Button shows "Sending..." with disabled state during submission

**2. Wire to ProjectImportList (app/admin/projects/import/project-import-list.tsx):**

Add state for send invitation modal:

- sendInvitationOpen: boolean
- selectedProjectForInvitation: ProjectForImport | null

Add "Send Invitation" button to bulk toolbar (next to "Configure Portal Settings"):

- Only enabled when exactly 1 project selected AND that project hasPortalSettings=true
- Button text: "Send Invitation"
- Icon: Mail from lucide-react
- Variant: outline (not primary - secondary action to configure)
- onClick: Open SendInvitationModal with selected project

Add SendInvitationModal at bottom of component:

- Pass selectedProjectForInvitation as project prop
- onSuccess: router.refresh() + clear selection + close modal
- Match PortalSettingsModal integration pattern

**Filter logic update:**

- Portal Ready projects (hasPortalSettings && !hasPortalAccess) are primary invitation targets
- Button should be most visible for this filter tab
  </action>
  <verify>

```bash
# Check component exists
ls components/admin/send-invitation-modal.tsx

# Verify integration in import page
grep -q "SendInvitationModal" app/admin/projects/import/project-import-list.tsx && echo "✓ Modal integrated"
grep -q "Send Invitation" app/admin/projects/import/project-import-list.tsx && echo "✓ Button added"

# TypeScript check
npx tsc --noEmit

# Build succeeds
npm run build
```

  </verify>
  <done>
SendInvitationModal component created with email input form and submission logic, wired to ProjectImportList with "Send Invitation" button in bulk toolbar (enabled when 1 Portal Ready project selected), modal calls createInvitation then sendClientInvitation on submit, success toast and refresh on completion, TypeScript and build pass without errors.
  </done>
</task>

</tasks>

<verification>
Phase 18 Plan 02 enables admin to send client invitations:

**Email Template:**

- sendClientInvitation function in lib/email.ts
- Resend integration with Qualia teal branding
- Secure signup URL: /auth/signup?token={invitationToken}
- Welcome message from portal_settings displayed
- Silent failure pattern (non-blocking)

**Admin UI:**

- SendInvitationModal component with email input
- Preview of project welcome message and visibility settings
- Form validation with invitationSchema
- Loading states and toast notifications
- Integrated into ProjectImportList bulk toolbar
- "Send Invitation" button enabled for Portal Ready projects

**Workflow:**

1. Admin selects Portal Ready project (hasPortalSettings=true)
2. Clicks "Send Invitation" in toolbar
3. Modal opens with project context
4. Admin enters client email
5. Clicks "Send Invitation"
6. Server action creates invitation record
7. Email sent via Resend with secure token
8. Success toast shows confirmation
9. Page refreshes (status will update in Plan 03)

**Ready for Plan 03:**

- Invitations are being created and sent
- Status tracking ready to be displayed
- History ready to be queried
  </verification>

<success_criteria>
**Measurable completion:**

1. lib/email.ts exports sendClientInvitation function
2. components/admin/send-invitation-modal.tsx exists and exports SendInvitationModal
3. app/admin/projects/import/project-import-list.tsx contains "Send Invitation" button
4. Email template includes signup URL with token parameter
5. Modal form validates email input before submission
6. TypeScript compilation succeeds
7. Production build succeeds (`npm run build` passes)
8. Manual test: Click "Send Invitation" opens modal, enter email, submit triggers createInvitation + sendClientInvitation
   </success_criteria>

<output>
After completion, create `.planning/phases/18-invitation-system/18-02-SUMMARY.md`
</output>
