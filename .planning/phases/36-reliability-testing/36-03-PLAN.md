---
phase: 36-reliability-testing
plan: 03
type: execute
wave: 2
depends_on: ['02']
files_modified:
  - __tests__/actions/shared.test.ts
  - __tests__/actions/auth-helpers.test.ts
  - __tests__/lib/server-utils.test.ts
  - __tests__/actions/integrations.test.ts
  - __tests__/actions/clients.test.ts
  - __tests__/actions/meetings.test.ts
  - __tests__/actions/payments.test.ts
autonomous: true

must_haves:
  truths:
    - 'Auth helper functions are tested for admin/employee role checks'
    - 'server-utils normalizeFKResponse handles all edge cases'
    - 'Secondary action modules have meaningful test coverage'
    - 'Combined test suite reaches 30%+ statement coverage'
  artifacts:
    - path: '__tests__/actions/shared.test.ts'
      provides: 'Tests for shared permission helpers and ActionResult'
      min_lines: 60
    - path: '__tests__/actions/auth-helpers.test.ts'
      provides: 'Tests for isUserAdmin, canDelete*, canModify*'
      min_lines: 80
    - path: '__tests__/lib/server-utils.test.ts'
      provides: 'Tests for normalizeFKResponse'
      min_lines: 30
    - path: '__tests__/actions/clients.test.ts'
      provides: 'Tests for client CRM operations'
      min_lines: 60
    - path: '__tests__/actions/meetings.test.ts'
      provides: 'Tests for meeting CRUD'
      min_lines: 60
    - path: '__tests__/actions/payments.test.ts'
      provides: 'Tests for payment tracking'
      min_lines: 60
  key_links:
    - from: '__tests__/actions/auth-helpers.test.ts'
      to: 'app/actions/index.ts'
      via: 'import isUserAdmin, canDelete*'
      pattern: 'import.*from.*actions'
    - from: '__tests__/lib/server-utils.test.ts'
      to: 'lib/server-utils.ts'
      via: 'direct import'
      pattern: 'import.*normalizeFKResponse'
---

<objective>
Write tests for auth helpers, server utilities, and secondary action modules (clients, meetings, payments, integrations) to push overall coverage past the 30% threshold.

Purpose: Complete REL-01 (30%+ coverage). Plan 02 covers the 4 core modules; this plan covers auth layer, utilities, and secondary modules to reach the target.
Output: ~150+ new tests across 7 test files, total coverage crosses 30%.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/actions/shared.ts
@app/actions/index.ts
@lib/server-utils.ts
@app/actions/clients.ts
@app/actions/meetings.ts
@app/actions/payments.ts
@app/actions/integrations.ts
@jest.config.ts
@__tests__/utils/render.tsx
@.planning/phases/36-reliability-testing/36-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test auth helpers, shared module, and server-utils</name>
  <files>
    __tests__/actions/shared.test.ts
    __tests__/actions/auth-helpers.test.ts
    __tests__/lib/server-utils.test.ts
  </files>
  <action>
    **server-utils tests (`__tests__/lib/server-utils.test.ts`):**

    Test `normalizeFKResponse` — this is pure function, no mocking needed:
    - Single object input -> returns that object
    - Array with one element -> returns first element
    - Array with multiple elements -> returns first element
    - Empty array -> returns null
    - null input -> returns null
    - undefined input -> returns null (if applicable)

    **Shared module tests (`__tests__/actions/shared.test.ts`):**

    Read `app/actions/shared.ts` fully. Test:
    - `isUserAdmin()` — returns true for admin role, false for employee role, false when user not found
    - `isUserManagerOrAbove()` — returns true for admin, true for manager, false for employee
    - Any permission helpers (canAccessProject, canModifyTask, etc.) — test owner can access, non-owner cannot, admin can access all
    - `ActionResult` type is correctly shaped (compile-time, but verify patterns)

    **Auth helpers tests (`__tests__/actions/auth-helpers.test.ts`):**

    Read `app/actions/index.ts` for exported auth functions. Test:
    - `isUserAdmin(userId)` — admin returns true, employee returns false, DB error returns false
    - `canDeleteProject(userId, projectId)` — admin can delete any, creator can delete own, employee cannot delete others'
    - `canDeleteTask(userId, taskId)` — admin/creator/assignee can delete, others cannot
    - Any other `can*` helpers exported from index.ts

    Use the shared mock utilities from plan 02's `__tests__/actions/test-utils.ts` (guaranteed to exist since this plan depends on plan 02).

  </action>
  <verify>
    Run `npm test -- __tests__/lib/server-utils.test.ts __tests__/actions/shared.test.ts __tests__/actions/auth-helpers.test.ts` — all pass.
  </verify>
  <done>
    Auth helpers verified for role-based access. normalizeFKResponse edge cases covered. Shared permission helpers tested for admin/employee/creator patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test secondary action modules (clients, meetings, payments) + verify 30% coverage</name>
  <files>
    __tests__/actions/clients.test.ts
    __tests__/actions/meetings.test.ts
    __tests__/actions/payments.test.ts
    __tests__/actions/integrations.test.ts
  </files>
  <action>
    **Clients tests (`__tests__/actions/clients.test.ts`):**

    Read `app/actions/clients.ts`. Test key exports:
    - `getClients()` — list with workspace filter, empty result
    - `getClientById()` — found, not found, includes contacts
    - `createClient()` — valid input, Zod validation failure
    - `updateClient()` — success, auth check
    - `updateLeadStatus()` — status transitions

    **Meetings tests (`__tests__/actions/meetings.test.ts`):**

    Read `app/actions/meetings.ts`. Test key exports:
    - `getMeetings()` / `getTodaysMeetings()` — list, filtered by date
    - `createMeeting()` — valid input, validation failure, attendee handling
    - `updateMeeting()` — success, not found
    - `deleteMeeting()` — success, auth check

    **Payments tests (`__tests__/actions/payments.test.ts`):**

    Read `app/actions/payments.ts`. Test key exports:
    - `getPayments()` — list, filter by project/client
    - `createPayment()` — valid input, validation
    - `updatePayment()` — success, auth
    - Payment calculations / summaries if any

    **Integrations tests (`__tests__/actions/integrations.test.ts`):**

    Read `app/actions/integrations.ts`. Test key exports:
    - Focus on functions that have pure logic (not just DB passthrough)
    - Test error handling paths

    After writing all tests, run full coverage check:
    ```bash
    npm test -- --coverage
    ```

    If coverage is below 30%, identify the largest untested files and add targeted tests for their most-used exports. Focus on files with the most lines that are in `collectCoverageFrom` paths.

    Strategy for hitting 30%: The codebase is ~105k lines. 30% = ~31.5k lines covered. Focus on:
    1. Action modules (18k lines total) — testing thoroughly covers many lines
    2. lib/ utilities — small but high coverage ratio
    3. The existing validation tests already cover lib/validation.ts

    If coverage still falls short, add quick tests for additional action modules (logos, health, checkins) — these are straightforward CRUD patterns.

  </action>
  <verify>
    Run `npm test` — ALL tests pass (0 failures).
    Run `npm test -- --coverage` — statement coverage >= 30%.
    If coverage < 30%, the task is NOT done — add more tests until threshold is met.
  </verify>
  <done>
    All secondary action modules tested. Combined test suite (existing + plans 02 + 03) achieves 30%+ statement coverage. `npm test` exits 0 with all tests passing.
  </done>
</task>

</tasks>

<verification>
1. `npm test` — all tests pass (0 failures across all test files)
2. `npm test -- --coverage` — statement coverage >= 30%
3. Coverage report shows meaningful coverage for: shared.ts, index.ts (auth helpers), server-utils.ts, clients.ts, meetings.ts, payments.ts
</verification>

<success_criteria>

- Auth helpers tested for role-based access patterns
- normalizeFKResponse edge cases fully covered
- Secondary action modules (clients, meetings, payments) have tests
- Overall statement coverage reaches 30%+ (the REL-01 target)
- All tests pass alongside existing test suite
  </success_criteria>

<output>
After completion, create `.planning/phases/36-reliability-testing/36-03-SUMMARY.md`
</output>
