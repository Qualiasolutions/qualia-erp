---
phase: 36-reliability-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/lib/voice-assistant-intelligence.test.ts
  - __tests__/components/dashboard-meetings.test.tsx
  - app/admin/error.tsx
  - app/agent/error.tsx
  - app/clients/error.tsx
  - app/inbox/error.tsx
  - app/knowledge/error.tsx
  - app/payments/error.tsx
  - app/projects/error.tsx
  - app/protected/error.tsx
  - app/research/error.tsx
  - app/schedule/error.tsx
  - app/seo/error.tsx
  - app/settings/error.tsx
  - app/status/error.tsx
  - app/team/error.tsx
  - app/api/cron/blog-tasks/route.ts
  - app/api/cron/morning-email/route.ts
  - app/api/cron/research-tasks/route.ts
  - app/api/cron/reminders/route.ts
  - app/api/cron/uptime-check/route.ts
  - .husky/pre-commit
  - package.json
  - jest.config.ts
autonomous: true

must_haves:
  truths:
    - 'All existing tests pass (npm test exits 0)'
    - 'Every top-level page route has an error.tsx boundary'
    - 'Cron routes never leak internal paths or stack traces in HTTP responses'
    - 'TypeScript errors are caught before commit'
  artifacts:
    - path: 'app/clients/error.tsx'
      provides: 'Error boundary for clients route'
      contains: "'use client'"
    - path: 'app/projects/error.tsx'
      provides: 'Error boundary for projects route'
      contains: "'use client'"
    - path: 'app/inbox/error.tsx'
      provides: 'Error boundary for inbox route'
      contains: "'use client'"
    - path: 'app/schedule/error.tsx'
      provides: 'Error boundary for schedule route'
      contains: "'use client'"
    - path: 'app/team/error.tsx'
      provides: 'Error boundary for team route'
      contains: "'use client'"
    - path: 'app/payments/error.tsx'
      provides: 'Error boundary for payments route'
      contains: "'use client'"
    - path: 'app/agent/error.tsx'
      provides: 'Error boundary for agent route'
      contains: "'use client'"
    - path: 'app/protected/error.tsx'
      provides: 'Error boundary for protected route'
      contains: "'use client'"
    - path: 'app/research/error.tsx'
      provides: 'Error boundary for research route'
      contains: "'use client'"
    - path: 'app/seo/error.tsx'
      provides: 'Error boundary for seo route'
      contains: "'use client'"
    - path: '.husky/pre-commit'
      provides: 'Pre-commit hook with tsc'
      contains: 'tsc --noEmit'
  key_links:
    - from: 'app/api/cron/*/route.ts'
      to: 'HTTP response'
      via: 'catch block'
      pattern: 'Internal server error|Unexpected error'
---

<objective>
Fix all broken tests, add error boundaries to every page route, sanitize cron error responses, and add tsc to pre-commit hook.

Purpose: Close REL-02 (failing tests), REL-03 (error boundaries), REL-04 (cron sanitization), REL-05 (tsc hook) — the infrastructure/hygiene items before heavy test writing begins.
Output: All tests green, error.tsx in 14 routes, safe cron errors, tsc in pre-commit.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/error.tsx
@jest.config.ts
@jest.setup.ts
@.husky/pre-commit
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix or remove failing tests + sanitize cron errors</name>
  <files>
    __tests__/lib/voice-assistant-intelligence.test.ts
    __tests__/components/dashboard-meetings.test.tsx
    app/api/cron/blog-tasks/route.ts
    app/api/cron/morning-email/route.ts
    app/api/cron/research-tasks/route.ts
    app/api/cron/reminders/route.ts
    app/api/cron/uptime-check/route.ts
  </files>
  <action>
    **Failing tests:**

    1. DELETE `__tests__/lib/voice-assistant-intelligence.test.ts` entirely — the voice assistant code was removed in Phase 33 (VAPI removal). These tests are stale with no source to test.

    2. Read `__tests__/components/dashboard-meetings.test.tsx`, identify the 3 failing assertions, and fix them. The failures are likely due to component API changes (props renamed, DOM structure changed). Read the actual component source to understand current structure, then update test expectations to match. If the component no longer exists, delete the test file.

    **Cron error sanitization:**

    In ALL 5 cron route files, replace `String(error)` / `err.message` in the catch block's HTTP response with a generic message. Pattern:

    BEFORE: `return NextResponse.json({ success: false, error: String(error) }, { status: 500 });`
    AFTER: `return NextResponse.json({ success: false, error: 'Internal server error' }, { status: 500 });`

    Keep the `console.error` logging the actual error for server-side debugging — only sanitize the HTTP response body.

    Specific files and lines:
    - `app/api/cron/blog-tasks/route.ts` line 202: `String(error)` -> `'Internal server error'`
    - `app/api/cron/morning-email/route.ts` line 124: `String(err)` in results array -> `'Send failed'` (this is per-email result, not the outer catch)
    - `app/api/cron/morning-email/route.ts` line 144: `String(error)` -> `'Internal server error'`
    - `app/api/cron/research-tasks/route.ts` line 176: `String(error)` -> `'Internal server error'`
    - `app/api/cron/reminders/route.ts` line 74: `String(error)` -> `'Internal server error'`
    - `app/api/cron/uptime-check/route.ts` line 159: `err.message` -> `'Internal server error'`

    NOTE: `weekly-digest` and `attendance-report` cron routes were checked and do NOT have error leaks — no changes needed for those.

  </action>
  <verify>
    Run `npm test` — all tests must pass (0 failures).
    Run `grep -rn 'String(error)\|String(err)\|err\.message' app/api/cron/*/route.ts` — must return no matches in HTTP response lines (console.error lines are OK).
  </verify>
  <done>
    All tests pass. No cron route leaks internal error details in HTTP responses. Voice assistant test file deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add error.tsx to all page routes + tsc pre-commit hook</name>
  <files>
    app/admin/error.tsx
    app/agent/error.tsx
    app/clients/error.tsx
    app/inbox/error.tsx
    app/knowledge/error.tsx
    app/payments/error.tsx
    app/projects/error.tsx
    app/protected/error.tsx
    app/research/error.tsx
    app/schedule/error.tsx
    app/seo/error.tsx
    app/settings/error.tsx
    app/status/error.tsx
    app/team/error.tsx
    .husky/pre-commit
    package.json
    jest.config.ts
  </files>
  <action>
    **Error boundaries:**

    Create `error.tsx` in each of these 14 directories. Each file should re-export the root error boundary to maintain consistency. Pattern:

    ```tsx
    'use client';

    export { default } from '@/app/error';
    ```

    This is a single-line re-export — keeps all error UI in one place (app/error.tsx) while ensuring Next.js catches errors at each route segment. The routes that need error.tsx:

    - app/admin/error.tsx
    - app/agent/error.tsx
    - app/clients/error.tsx
    - app/inbox/error.tsx
    - app/knowledge/error.tsx
    - app/payments/error.tsx
    - app/projects/error.tsx
    - app/protected/error.tsx
    - app/research/error.tsx
    - app/schedule/error.tsx
    - app/seo/error.tsx
    - app/settings/error.tsx
    - app/status/error.tsx
    - app/team/error.tsx

    NOTE: `app/portal/error.tsx` already exists — do NOT touch it.

    **TSC pre-commit hook:**

    Update `.husky/pre-commit` to run tsc before lint-staged:

    ```sh
    npx tsc --noEmit
    npx lint-staged
    ```

    This ensures type errors are caught before code is committed. tsc runs first — if it fails, lint-staged won't run (fail fast).

    Also update `package.json` lint-staged config to ensure it does NOT duplicate tsc (lint-staged should keep only eslint + prettier for staged files).

    **Coverage threshold:**

    In `jest.config.ts`, lower the coverage threshold from 50% to 30% to match the phase goal (REL-01 targets 30%, not 50%). Update:

    ```ts
    coverageThreshold: {
      global: {
        branches: 30,
        functions: 30,
        lines: 30,
        statements: 30,
      },
    },
    ```

  </action>
  <verify>
    Run `find app -maxdepth 2 -name error.tsx | wc -l` — should be 16 (14 new + portal existing + root app/error.tsx).
    Run `cat .husky/pre-commit` — should show tsc --noEmit before lint-staged.
    Run `npx tsc --noEmit` — must pass (exits 0).
    Run `npm run build` — must succeed.
  </verify>
  <done>
    All 14 page routes have error.tsx boundaries. Pre-commit hook runs tsc --noEmit before lint-staged. Jest coverage threshold set to 30%.
  </done>
</task>

</tasks>

<verification>
1. `npm test` exits 0 — all tests pass, no failures
2. `find app -maxdepth 2 -name error.tsx | wc -l` returns 16
3. `grep -rn 'String(error)\|String(err)\|err\.message' app/api/cron/*/route.ts` — no matches in response lines
4. `cat .husky/pre-commit` contains `tsc --noEmit`
5. `npx tsc --noEmit` exits 0
</verification>

<success_criteria>

- Zero failing tests
- 14 new error.tsx files created (re-exporting root error boundary)
- 5 cron routes sanitized (generic error messages in HTTP responses)
- Pre-commit hook includes tsc --noEmit
- Jest coverage threshold lowered to 30%
  </success_criteria>

<output>
After completion, create `.planning/phases/36-reliability-testing/36-01-SUMMARY.md`
</output>
