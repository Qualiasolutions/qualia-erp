---
phase: 42-portal-design-refresh
plan: '02'
type: execute
wave: 2
depends_on: ['42-01']
files_modified:
  - app/portal/portal-dashboard-content.tsx
  - app/portal/[id]/portal-project-content.tsx
  - components/portal/portal-page-header.tsx
  - components/portal/portal-recent-activity.tsx
  - components/portal/portal-whats-next-widget.tsx
autonomous: true

must_haves:
  truths:
    - 'Dashboard hero greeting card has cleaner visual hierarchy — greeting dominates, stats are secondary'
    - 'Quick action cards use tinted brand gradient backgrounds, not generic amber/emerald/violet'
    - 'Project detail page shows a clear progress indicator (percentage or phase count) without roadmap phase-noise overwhelming the header'
    - 'WhatsNext widget cards feel distinct from the hero — using a lighter surface tier'
    - 'PortalRecentActivity (projects overview) uses consistent card surface with no card-inside-card nesting'
  artifacts:
    - path: 'app/portal/portal-dashboard-content.tsx'
      provides: 'Redesigned dashboard with cleaner layout and brand-consistent quick actions'
      min_lines: 160
    - path: 'app/portal/[id]/portal-project-content.tsx'
      provides: 'Project detail page with simplified, clean header'
    - path: 'components/portal/portal-page-header.tsx'
      provides: 'Refined page header component with progress display'
    - path: 'components/portal/portal-whats-next-widget.tsx'
      provides: "What's Next widget with improved surface treatment"
  key_links:
    - from: 'app/portal/portal-dashboard-content.tsx'
      to: 'components/portal/portal-whats-next-widget.tsx'
      via: 'WhatsNextWidget import'
      pattern: 'WhatsNextWidget'
    - from: 'app/portal/[id]/portal-project-content.tsx'
      to: 'components/portal/portal-page-header.tsx'
      via: 'PortalPageHeader import'
      pattern: 'PortalPageHeader'
---

<objective>
Redesign the portal dashboard and project detail page to match Impeccable v4.0. The dashboard is the first thing clients see — it must communicate project status with clear hierarchy. The project detail page needs a cleaner header with visible progress without roadmap phase noise.

Purpose: Dashboard is the face of the portal. Clean hierarchy, tinted surfaces, and on-brand quick actions build trust with clients.
Output: Redesigned dashboard (hero, what's next, quick actions) and simplified project detail header with progress indicator.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-portal-design-refresh/42-01-SUMMARY.md
@~/.claude/skills/frontend-master/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign dashboard hero, quick actions, and What's Next</name>
  <files>
    app/portal/portal-dashboard-content.tsx
    components/portal/portal-whats-next-widget.tsx
  </files>
  <action>
**In `portal-dashboard-content.tsx`:**

**Hero greeting card**: The current `rounded-2xl border border-border bg-gradient-to-br from-card via-card to-muted/20` is okay but generic. Upgrade:

- Change border to `border-primary/8 dark:border-primary/12`
- Change gradient: `bg-gradient-to-br from-[#EDF0F0] via-card to-primary/[0.03] dark:from-[#121819] dark:via-card dark:to-primary/[0.04]`
- The decorative blobs: make them slightly more prominent — `bg-primary/[0.05]` (up from 0.03) and `bg-primary/[0.03]` (up from 0.02)
- The date label: change from `text-muted-foreground/40` to `text-primary/40` — tinted
- Greeting h1: keep size, change font-weight from `font-semibold` to `font-bold`
- Stats row inside hero: add `mt-6` top divider `border-t border-primary/[0.06] pt-6` to visually separate stats from greeting text

**Quick action cards**: Current gradient uses amber/emerald/violet which fights the brand. Replace ALL three with brand-tinted variants:

```typescript
const quickActions = [
  {
    label: 'Submit a request',
    description: 'Feature idea or change',
    href: '/portal/requests',
    icon: Lightbulb,
    gradient: 'from-primary/[0.06] to-primary/[0.02]',
    iconColor: 'text-primary',
    iconBg: 'bg-primary/10',
  },
  {
    label: 'View billing',
    description: 'Invoices & payments',
    href: '/portal/billing',
    icon: Receipt,
    gradient: 'from-primary/[0.08] to-primary/[0.03]',
    iconColor: 'text-primary',
    iconBg: 'bg-primary/12',
  },
  {
    label: 'Contact support',
    description: 'Get help from our team',
    href: 'mailto:support@qualiasolutions.net',
    icon: Headphones,
    external: true,
    gradient: 'from-primary/[0.04] to-transparent',
    iconColor: 'text-primary',
    iconBg: 'bg-primary/8',
  },
];
```

Update card className: `bg-gradient-to-br border-primary/[0.08] hover:border-primary/20`. Update icon container: use `action.iconBg` instead of `bg-background/80`. This makes the quick action row feel coherent with the brand instead of three competing color schemes.

**Section headers ("What's next")**: Change from `text-muted-foreground/50` to `text-primary/50` — subtle brand tint.

**In `portal-whats-next-widget.tsx`**: Read this file first. The widget cards likely use `bg-card`. If they do:

- Change outer container to `bg-card/80 border border-primary/[0.06]`
- Progress bars: ensure they use `bg-primary` (not `bg-blue-500` or similar)
- Phase name text: check it uses `text-foreground` not hardcoded colors
- No structural changes — surface/color-only pass.
  </action>
  <verify>Run `npx tsc --noEmit`. Dashboard should render with unified teal-tinted color language throughout — no competing amber/emerald/violet colors. Quick action cards should have subtle teal gradient backgrounds.</verify>
  <done>Dashboard hero uses tinted gradient, quick actions use brand-consistent teal gradients, What's Next widget uses tinted surface. All stagger animations still work.</done>
  </task>

<task type="auto">
  <name>Task 2: Simplify project detail header with clear progress indicator</name>
  <files>
    app/portal/[id]/portal-project-content.tsx
    components/portal/portal-page-header.tsx
  </files>
  <action>
Read `components/portal/portal-page-header.tsx` first to see current implementation.

**In `portal-page-header.tsx`** — add a cleaner progress display:

- The current props are `{ title, description, completedPhases, totalPhases }`. Keep them.
- If there's currently a progress bar — upgrade it: use a thin `h-1.5` progress track with rounded full, teal fill, and show `{completedPhases}/{totalPhases} phases` as a `text-[11px] text-primary/60` label next to it.
- If there's a percentage shown — keep it but style as `text-[13px] font-semibold text-primary` (not muted).
- Add a status pill based on phase completion: if `completedPhases === totalPhases` → "Completed" (green/teal), if `completedPhases > 0` → "In Progress" (primary), if `completedPhases === 0` → "Getting Started" (muted). Use a `rounded-full px-2.5 py-0.5 text-[11px] font-medium` pill with appropriate tinted background.
- Title: if currently using generic size — upgrade to `text-2xl font-bold tracking-tight` (was `text-xl font-semibold` or similar).
- Ensure the page header container does NOT have a card wrapper around it — it should be bare (no `rounded-xl border bg-card` wrapping the title+description). The title section should breathe directly on the page background.

**In `portal-project-content.tsx`**:

- The layout is `space-y-6` with PortalPageHeader → PortalTabs → PortalRoadmap. Keep this structure.
- Add `pb-4` to the PortalPageHeader wrapper to create breathing room before the tabs.
- The skeleton loading state: replace the three `h-32 animate-pulse rounded-lg bg-muted` blocks with Impeccable-style skeletons: `h-28 animate-pulse rounded-xl bg-primary/[0.04]` — tinted toward brand not flat gray.

Do NOT change: PortalRoadmap or PortalTabs — those are separate components and this plan scopes to the header/layout only.
</action>
<verify>Run `npx tsc --noEmit`. Navigate to a project detail page — should see clean title with phase progress bar/pill, no jarring card wrapper around the header, and tinted skeleton loading state.</verify>
<done>Project detail page header shows project title, description, a clear `X/Y phases` progress indicator with status pill, and no card-wrapping around the bare header section.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — 0 errors
2. Dashboard quick action cards all use teal/primary-tinted gradients (no amber/emerald/violet)
3. Dashboard hero greeting is visually dominant over stats
4. Project detail page header: title is large and bold, progress bar visible, status pill shown
5. No card-inside-card nesting on project detail header
6. All stagger animations from `lib/transitions` still fire
</verification>

<success_criteria>
Dashboard communicates project status with clear hierarchy and brand-consistent teal color language throughout. Project detail page header is clean, readable, and shows progress without clutter.
</success_criteria>

<output>
After completion, create `.planning/phases/42-portal-design-refresh/42-02-SUMMARY.md`
</output>
