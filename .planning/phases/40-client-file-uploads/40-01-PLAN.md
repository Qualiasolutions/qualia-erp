---
phase: 40-client-file-uploads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/project-files.ts
  - components/portal/portal-client-upload.tsx
  - app/portal/[id]/files/page.tsx
  - types/database.ts
autonomous: true

must_haves:
  truths:
    - 'Client can drag-and-drop or browse to select a file on the portal files page'
    - 'Client submits the file and it uploads successfully with their project association'
    - 'Uploaded file appears immediately in the portal files list after upload'
    - 'Uploaded file is stored in Supabase Storage under the project-files bucket'
  artifacts:
    - path: 'app/actions/project-files.ts'
      provides: 'uploadClientFile server action callable by portal clients'
      contains: 'uploadClientFile'
    - path: 'components/portal/portal-client-upload.tsx'
      provides: 'Drag-and-drop upload UI component for portal'
      min_lines: 80
    - path: 'app/portal/[id]/files/page.tsx'
      provides: 'Portal files page with upload section and file list'
      contains: 'PortalClientUpload'
  key_links:
    - from: 'components/portal/portal-client-upload.tsx'
      to: 'app/actions/project-files.ts'
      via: 'uploadClientFile server action'
      pattern: 'uploadClientFile'
    - from: 'app/actions/project-files.ts'
      to: "supabase.storage.from('project-files')"
      via: 'upload call with is_client_upload=true'
      pattern: 'is_client_upload'
---

<objective>
Add a DB column to distinguish client uploads, create a `uploadClientFile` server action that allows portal clients to upload, and build a drag-and-drop upload UI in the portal files page.

Purpose: Clients need self-service file upload so they can share assets, feedback, and content with the team without emailing files.
Output: Supabase migration adding `is_client_upload` column, new server action, new `PortalClientUpload` component, updated portal files page showing both uploaded files and the upload widget.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/actions/project-files.ts
@app/portal/[id]/files/page.tsx
@components/portal/portal-file-list.tsx
@lib/portal-utils.ts
@~/.claude/skills/supabase/SKILL.md
@~/.claude/skills/frontend-master/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration — add is_client_upload column and update RLS</name>
  <files>types/database.ts</files>
  <action>
    Run a Supabase migration via MCP to add `is_client_upload boolean NOT NULL DEFAULT false` to the `project_files` table.

    After adding the column, add/update the RLS INSERT policy on `project_files` so clients can insert rows where:
    - `is_client_upload = true`
    - `is_client_visible = true`
    - The project_id is one the authenticated user has access to via `client_projects` (i.e., `EXISTS (SELECT 1 FROM client_projects WHERE client_id = auth.uid() AND project_id = NEW.project_id)`)

    The existing admin INSERT policy must remain unchanged (workspace members can insert any row).

    After running the migration, update `types/database.ts` to add `is_client_upload: boolean` to the `project_files` Row, Insert, and Update types:
    - Row: `is_client_upload: boolean;`
    - Insert: `is_client_upload?: boolean;`
    - Update: `is_client_upload?: boolean;`

    Add it after `is_client_visible` in alphabetical proximity.

  </action>
  <verify>
    Run: `npx supabase db diff --linked` to confirm no schema drift.
    Check `types/database.ts` contains `is_client_upload` in the project_files Row type.
  </verify>
  <done>
    `project_files` table has `is_client_upload` column with default false. RLS allows clients to insert with is_client_upload=true for their projects. types/database.ts reflects the new column.
  </done>
</task>

<task type="auto">
  <name>Task 2: uploadClientFile server action</name>
  <files>app/actions/project-files.ts</files>
  <action>
    Add a new exported function `uploadClientFile(formData: FormData): Promise<ActionResult>` to `app/actions/project-files.ts`.

    The function must:
    1. Get authenticated user — return `{ success: false, error: 'Not authenticated' }` if none
    2. Parse `file` and `project_id` from formData. Also accept optional `description`.
    3. Validate file size <= 50MB and MIME type against the existing `ALLOWED_MIME_TYPES` array (reuse the constants already in the file)
    4. Verify client has access: call `canClientAccessProject(user.id, projectId)` (already imported as `canClientAccessProject` from `@/lib/portal-utils`). Return error if no access.
    5. Get project to retrieve `workspace_id` (same pattern as `uploadProjectFile`)
    6. Generate storage path: `${projectId}/client-uploads/${Date.now()}_${sanitizedName}` (separate subfolder from admin uploads)
    7. Upload to `project-files` bucket (same pattern as `uploadProjectFile`)
    8. Insert into `project_files` with:
       - `is_client_upload: true`
       - `is_client_visible: true` (client uploads are always visible to admin)
       - `uploaded_by: user.id`
       - all other standard fields
    9. Log activity via `createActivityLogEntry` with `action_type: 'client_file_uploaded'`, `isClientVisible: true`
    10. Call `notifyEmployeesOfClientFileUpload(projectId, uploaderName, file.name, description)` — fetch uploader name from profiles first
    11. Revalidate `/portal/${projectId}/files` and `/projects/${projectId}/files`
    12. Return `{ success: true, data: fileRecord }`

    Do NOT modify the existing `uploadProjectFile` function.

  </action>
  <verify>
    `npx tsc --noEmit` passes with no errors.
    Grep confirms `uploadClientFile` is exported: `grep -n "export async function uploadClientFile" app/actions/project-files.ts`
  </verify>
  <done>
    `uploadClientFile` is exported, uses portal auth (canClientAccessProject), sets is_client_upload=true, triggers email notification, revalidates both portal and ERP paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: PortalClientUpload component and portal files page integration</name>
  <files>
    components/portal/portal-client-upload.tsx
    app/portal/[id]/files/page.tsx
  </files>
  <action>
    Create `components/portal/portal-client-upload.tsx` as a `'use client'` component.

    Props: `{ projectId: string; onUploadComplete?: () => void }`

    The component must:
    - Use `useState` for: `isDragging`, `isUploading`, `selectedFile`, `description`, `fileSizeError`
    - Implement drag-and-drop: `onDragOver` sets `isDragging=true`, `onDragLeave` sets `isDragging=false`, `onDrop` captures `event.dataTransfer.files[0]` and validates size
    - Also support `<input type="file">` for browse-to-upload
    - File size validation: reject >50MB immediately with inline error message
    - Show selected file name + size when a file is chosen
    - Optional description `<Textarea>` (max 200 chars)
    - Submit button calls `uploadClientFile(formData)` from `@/app/actions/project-files`
    - On success: reset form, call `onUploadComplete?.()`, show success toast via `useToast`
    - On error: show error toast with the error message from result

    Visual design (portal aesthetic — matches existing portal components):
    - Drop zone: dashed border, rounded-xl, `p-8`, centered content with upload icon
    - When `isDragging=true`: teal border (`border-primary`) + light teal background (`bg-primary/5`)
    - Use `Upload` icon from lucide-react for the drop zone
    - Primary action button using shadcn `Button` with loading spinner when uploading
    - Matches the portal's clean aesthetic (no admin-style cards)

    Update `app/portal/[id]/files/page.tsx`:
    - Import `PortalClientUpload`
    - In `PortalFilesContent`, below the existing info banner and ABOVE `<PortalFileList>`, add:
      ```tsx
      <div>
        <h2 className="mb-3 text-sm font-semibold uppercase tracking-wider text-muted-foreground">Upload a File</h2>
        <PortalClientUpload projectId={projectId} />
      </div>
      ```
    - Change the existing info banner text to: "Files shared by your team appear below. You can also upload files for your team to review."
    - Keep all existing `getProjectFiles(projectId, true)` call and `PortalFileList` unchanged
    - The page already refreshes on navigation; `onUploadComplete` can call `router.refresh()` using `useRouter` from `next/navigation` — but since this is a Server Component page, instead make `PortalClientUpload` call `router.refresh()` internally after successful upload (import `useRouter` inside the client component)

  </action>
  <verify>
    `npx tsc --noEmit` passes.
    Component file exists: `ls components/portal/portal-client-upload.tsx`
    Portal files page imports PortalClientUpload: `grep -n "PortalClientUpload" app/portal/[id]/files/page.tsx`
  </verify>
  <done>
    Portal files page shows drag-and-drop upload zone above the file list. Client can select a file by dragging or browsing, see its name/size, add optional description, and submit. After upload the file list refreshes.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. `npx run build` — builds without errors
3. Portal files page at `/portal/[id]/files` renders upload zone above file list
4. `is_client_upload` column exists in `project_files` table (check via Supabase MCP)
5. `uploadClientFile` exported from `app/actions/project-files.ts`
</verification>

<success_criteria>

- Client visiting `/portal/[projectId]/files` sees an upload zone (drag-and-drop + file picker)
- Selecting a file and submitting creates a record in `project_files` with `is_client_upload=true` and `is_client_visible=true`
- File is stored in Supabase Storage under `project-files` bucket at path `{projectId}/client-uploads/...`
- After upload, the file appears in the portal file list immediately (router.refresh)
- `npx tsc --noEmit` passes
  </success_criteria>

<output>
After completion, create `.planning/phases/40-client-file-uploads/40-01-SUMMARY.md`
</output>
