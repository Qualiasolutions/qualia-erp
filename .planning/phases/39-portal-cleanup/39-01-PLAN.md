---
phase: 39-portal-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/portal/messages/page.tsx
  - app/portal/messages/loading.tsx
  - components/portal/portal-messages.tsx
  - components/portal/portal-sidebar.tsx
  - components/portal/portal-header.tsx
autonomous: true

must_haves:
  truths:
    - 'Navigating to /portal/messages returns a 404 — no page exists'
    - 'Portal sidebar shows no Messages nav item'
    - 'Portal header routeLabels has no /portal/messages entry'
    - 'TypeScript build passes with no errors'
  artifacts:
    - path: 'components/portal/portal-sidebar.tsx'
      provides: 'Portal navigation without Messages item'
      contains: 'mainNav without Messages entry'
    - path: 'components/portal/portal-header.tsx'
      provides: 'Breadcrumb route labels without Messages'
  key_links:
    - from: 'components/portal/portal-sidebar.tsx'
      to: 'mainNav array'
      via: 'remove Messages entry'
      pattern: 'Messages.*portal/messages'
    - from: 'components/portal/portal-header.tsx'
      to: 'routeLabels object'
      via: 'remove /portal/messages key'
      pattern: 'portal/messages.*Messages'
---

<objective>
Remove the Messages route and all related UI from the client portal. The Messages page (`/portal/messages`) was a thin wrapper over the activity feed and is being cut. No client-facing entry point should remain.

Purpose: Dead code removal — the route has no ongoing purpose and clutters portal navigation.
Output: Deleted route files, deleted component, cleaned-up sidebar and header.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Messages route files and component</name>
  <files>
    app/portal/messages/page.tsx
    app/portal/messages/loading.tsx
    components/portal/portal-messages.tsx
  </files>
  <action>
    Delete all three files:
    - `app/portal/messages/page.tsx` — the route page (imports PortalMessages and getClientActivityFeed)
    - `app/portal/messages/loading.tsx` — the route loading skeleton
    - `components/portal/portal-messages.tsx` — the client component (only used by the messages page)

    Do NOT delete or modify `app/actions/client-portal.ts` — `getClientActivityFeed` has independent tests in `__tests__/actions/client-portal.test.ts` and must remain.

    After deletion, remove the now-empty `app/portal/messages/` directory.

  </action>
  <verify>
    - `ls app/portal/messages/` returns "No such file or directory"
    - `ls components/portal/portal-messages.tsx` returns "No such file or directory"
    - `grep -r "portal-messages\|PortalMessages" app/ components/` returns no results
  </verify>
  <done>Route directory and component file are gone. No imports of PortalMessages exist anywhere in app/ or components/.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Messages from sidebar nav and header route labels</name>
  <files>
    components/portal/portal-sidebar.tsx
    components/portal/portal-header.tsx
  </files>
  <action>
    In `components/portal/portal-sidebar.tsx`:
    1. Remove the Messages entry from the `mainNav` array:
       Delete: `{ name: 'Messages', href: '/portal/messages', icon: MessageSquare },`
    2. Remove the `MessageSquare` import from the lucide-react import list (it is only used by the Messages nav item).
    3. The `NavItem` type alias (`type NavItem = (typeof mainNav)[0]`) derives from mainNav — no change needed there.

    In `components/portal/portal-header.tsx`:
    1. Remove the `/portal/messages` entry from `routeLabels`:
       Delete: `'/portal/messages': 'Messages',`
    2. No icon imports to clean up in this file.

    Run `npx tsc --noEmit` to confirm no type errors after the edits.

  </action>
  <verify>
    - `grep -n "MessageSquare\|portal/messages\|Messages" components/portal/portal-sidebar.tsx` returns no results
    - `grep -n "portal/messages" components/portal/portal-header.tsx` returns no results
    - `npx tsc --noEmit` exits 0
  </verify>
  <done>
    Sidebar mainNav has 2 items (Dashboard, Projects). MessageSquare import removed. Header routeLabels has no Messages entry. TypeScript clean.
  </done>
</task>

</tasks>

<verification>
1. `ls app/portal/messages/` — should fail with "No such file or directory"
2. `grep -rn "portal/messages\|PortalMessages\|portal-messages" app/ components/ --include="*.tsx" --include="*.ts"` — returns no results
3. `npx tsc --noEmit` — exits 0
4. `npm run build` — completes without errors
</verification>

<success_criteria>

- /portal/messages route no longer exists (returns Next.js 404)
- Portal sidebar shows only: Dashboard, Projects (main nav) + Requests, Billing, Settings (manage nav)
- No TypeScript errors
- Build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/39-portal-cleanup/39-01-SUMMARY.md`
</output>
