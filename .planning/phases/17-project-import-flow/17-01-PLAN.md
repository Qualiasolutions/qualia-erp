---
phase: 17-project-import-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/projects/import/page.tsx
  - app/actions/portal-import.ts
  - lib/validation.ts
autonomous: true

must_haves:
  truths:
    - 'Admin can see complete list of ERP projects with clear visual indication of portal access status'
    - 'Admin can select one or multiple projects via checkboxes for bulk operations'
    - 'Admin can filter projects by portal status (enabled vs not enabled) to focus on import candidates'
  artifacts:
    - path: 'app/admin/projects/import/page.tsx'
      provides: 'Admin UI for viewing and selecting ERP projects'
      min_lines: 150
    - path: 'app/actions/portal-import.ts'
      provides: 'Server action to fetch projects with portal status'
      exports: ['getProjectsForPortalImport']
  key_links:
    - from: 'app/admin/projects/import/page.tsx'
      to: 'app/actions/portal-import.ts'
      via: 'Server component direct call'
      pattern: 'const.*=.*await getProjectsForPortalImport'
    - from: 'app/actions/portal-import.ts'
      to: 'client_projects table'
      via: 'Count query for portal access detection'
      pattern: 'count.*client_projects.*project_id'
---

<objective>
Create admin interface for viewing ERP projects with portal access status and bulk selection capability

Purpose: Admins need a dedicated view showing all ERP projects with clear indication of which already have portal access, enabling informed bulk import decisions

Output: Admin page at /admin/projects/import with filterable project list and multi-select functionality
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/qualia/Projects/live/qualia/.planning/PROJECT.md
@/home/qualia/Projects/live/qualia/.planning/ROADMAP.md
@/home/qualia/Projects/live/qualia/.planning/STATE.md

# Prior phase SUMMARYs providing patterns

@/home/qualia/Projects/live/qualia/.planning/phases/13-erp-portal-integration/13-01-SUMMARY.md
@/home/qualia/Projects/live/qualia/.planning/phases/16-complete-portal-pages/16-02-SUMMARY.md

# Codebase patterns

@/home/qualia/Projects/live/qualia/.planning/codebase/ARCHITECTURE.md
@/home/qualia/Projects/live/qualia/.planning/codebase/CONVENTIONS.md

# Existing utilities

@/home/qualia/Projects/live/qualia/lib/integration-utils.ts
@/home/qualia/Projects/live/qualia/app/actions/client-portal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server action to fetch projects with portal status</name>
  <files>
    app/actions/portal-import.ts
    lib/validation.ts
  </files>
  <action>
    Create new file app/actions/portal-import.ts with server action getProjectsForPortalImport().

    Server action specification:
    - Auth check: require user + isUserManagerOrAbove() authorization
    - Query projects table for Active, Demos, Delayed status (exclude Archived, Canceled, Launched)
    - Join with client_projects to get portal access count per project
    - Join with clients table to get ERP client info if linked via client_id FK
    - Return array with structure: { id, name, project_type, project_status, client_id, erpClient: { name, company_name } | null, portalAccessCount: number, hasPortalAccess: boolean }
    - Use LEFT JOIN pattern via Supabase select with count aggregation
    - Normalize FK responses for erpClient using Array.isArray() pattern from existing codebase
    - Sort by: hasPortalAccess ASC, name ASC (not-enabled first for easy import)

    No validation schema needed for this read-only action.

    Follow patterns from app/actions/client-portal.ts (getPortalAdminData) for structure.

  </action>
  <verify>
    grep -n "export async function getProjectsForPortalImport" app/actions/portal-import.ts
    grep -n "isUserManagerOrAbove" app/actions/portal-import.ts
  </verify>
  <done>
    Server action exists, has admin authorization check, queries projects with client_projects join, returns typed array with portal status
  </done>
</task>

<task type="auto">
  <name>Task 2: Create admin import page with project list and filters</name>
  <files>
    app/admin/projects/import/page.tsx
  </files>
  <action>
    Create admin page at app/admin/projects/import/page.tsx as SERVER component.

    Page structure:
    - Call getProjectsForPortalImport() server action directly (no SWR, fresh data on load)
    - Pass data to client component ProjectImportList as prop
    - Handle error state if action fails

    Create client component ProjectImportList in same file:
    - 'use client' directive at top
    - Props: { projects: array from server action }
    - State: selectedProjectIds (Set), filterStatus ('all' | 'enabled' | 'not-enabled')
    - UI Layout:
      * Page header: "Import Projects to Portal" heading + description
      * Filter tabs: All (count) | Not Enabled (count) | Enabled (count)
      * Bulk actions toolbar: "Select All" checkbox + count badge when selections > 0
      * Table with columns: [Checkbox, Project Name, Type, Status, ERP Client, Portal Access Status, Actions]
      * Portal Access Status column: Badge with green checkmark if hasPortalAccess, gray "Not Enabled" otherwise
      * Actions column: "Configure Import" button (disabled for now, placeholder for Plan 02)
    - Table rows use card-interactive class for spring hover from Phase 10
    - Empty state: Show message "No projects found" with description if filtered list is empty
    - Use shadcn/ui components: Table, Badge, Button, Checkbox, Tabs
    - Follow existing admin page patterns from app/admin/reviews/page.tsx structure

    No selection functionality needed yet (Plan 02 will add import modal). Just display with checkboxes for visual consistency.

  </action>
  <verify>
    ls -la app/admin/projects/import/page.tsx
    grep -n "getProjectsForPortalImport" app/admin/projects/import/page.tsx
    grep -n "'use client'" app/admin/projects/import/page.tsx
  </verify>
  <done>
    Admin page exists at /admin/projects/import, fetches project list server-side, renders filterable table with portal status badges and checkboxes
  </done>
</task>

<task type="auto">
  <name>Task 3: Add navigation link to admin sidebar</name>
  <files>
    components/sidebar.tsx
  </files>
  <action>
    Add "Portal Import" link to admin sidebar navigation.

    Location: Find the admin-only section in sidebar (likely near "Admin Reviews" or similar)
    Link details:
    - Text: "Portal Import"
    - Icon: Upload (from lucide-react)
    - href: "/admin/projects/import"
    - Show only when user role is admin or manager (same condition as other admin links)

    Follow existing sidebar link pattern - check how /admin/reviews is added and replicate structure.

  </action>
  <verify>
    grep -n "Portal Import" components/sidebar.tsx
    grep -n "/admin/projects/import" components/sidebar.tsx
  </verify>
  <done>
    Sidebar contains "Portal Import" navigation link visible to admins, clicking navigates to import page
  </done>
</task>

</tasks>

<verification>
- [ ] Navigate to /admin/projects/import as admin user
- [ ] Verify page loads without errors
- [ ] Verify project list displays with accurate portal status badges
- [ ] Verify filter tabs show correct counts (All, Enabled, Not Enabled)
- [ ] Verify clicking filter tabs updates visible projects
- [ ] Verify checkboxes render next to each project (not functional yet, just visual)
- [ ] Verify ERP client info displays when project has client_id FK
- [ ] Verify projects without portal access show gray "Not Enabled" badge
- [ ] Verify projects with portal access show green checkmark badge
</verification>

<success_criteria>

- Admin can access /admin/projects/import page via sidebar navigation
- Page displays complete list of active ERP projects with portal status
- Portal access status is clearly indicated via badges (enabled vs not enabled)
- Filter tabs allow focusing on specific project groups
- Checkboxes render for future bulk selection (Plan 02)
- ERP client information displays when available
- UI follows existing admin design patterns and uses shadcn/ui components
  </success_criteria>

<output>
After completion, create `.planning/phases/17-project-import-flow/17-01-SUMMARY.md`
</output>
