---
phase: 17-project-import-flow
plan: 03
type: execute
wave: 3
depends_on: ['17-02']
files_modified:
  - app/admin/projects/import/page.tsx
  - app/actions/portal-import.ts
  - components/admin/portal-settings-modal.tsx
  - lib/validation.ts
autonomous: true

must_haves:
  truths:
    - 'Admin can click Enable Portal Access and see configuration modal before import'
    - 'Admin can configure welcome message for client portal project view'
    - 'Admin can toggle visibility options (roadmap, files, comments) per project'
    - 'Admin can click Confirm and project immediately appears in portal projects list'
    - 'Admin sees visual confirmation via toast notification and status badge update'
  artifacts:
    - path: 'components/admin/portal-settings-modal.tsx'
      provides: 'Portal configuration form before enabling access'
      min_lines: 120
    - path: 'app/actions/portal-import.ts'
      provides: 'Server action to enable portal access with settings'
      exports: ['enablePortalAccess']
    - path: 'lib/validation.ts'
      provides: 'Validation schema for portal settings'
      contains: 'enablePortalAccessSchema'
  key_links:
    - from: 'app/admin/projects/import/page.tsx'
      to: 'components/admin/portal-settings-modal.tsx'
      via: 'Dialog trigger on Enable button click'
      pattern: 'PortalSettingsModal.*open.*selectedProjects'
    - from: 'components/admin/portal-settings-modal.tsx'
      to: 'app/actions/portal-import.ts'
      via: 'Form submission with settings data'
      pattern: 'enablePortalAccess.*formData'

user_setup:
  - service: supabase
    why: 'Client account creation requires invitation'
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: 'Supabase Dashboard → Settings → API → service_role key'
    dashboard_config:
      - task: 'Verify auth email templates configured'
        location: 'Supabase Dashboard → Authentication → Email Templates'
---

<objective>
Complete portal import flow with configuration modal and one-click enable action

Purpose: Admins need ability to configure portal settings and enable access in single streamlined workflow, immediately making projects visible to clients

Output: Working end-to-end import flow from selection → configuration → enable → visual confirmation
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/qualia/Projects/live/qualia/.planning/PROJECT.md
@/home/qualia/Projects/live/qualia/.planning/ROADMAP.md
@/home/qualia/Projects/live/qualia/.planning/phases/17-project-import-flow/17-01-SUMMARY.md
@/home/qualia/Projects/live/qualia/.planning/phases/17-project-import-flow/17-02-SUMMARY.md

# Portal access patterns

@/home/qualia/Projects/live/qualia/app/actions/client-portal.ts

# Codebase patterns

@/home/qualia/Projects/live/qualia/.planning/codebase/ARCHITECTURE.md
@/home/qualia/Projects/live/qualia/.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validation schema for portal settings</name>
  <files>
    lib/validation.ts
  </files>
  <action>
    Add enablePortalAccessSchema to lib/validation.ts.

    Schema fields:
    - projectIds: z.array(z.string().uuid()).min(1, "At least one project required")
    - welcomeMessage: z.string().max(500).optional().nullable()
    - visibilitySettings: z.object({
        showRoadmap: z.boolean().default(true),
        showFiles: z.boolean().default(true),
        showComments: z.boolean().default(true)
      })
    - notifyClient: z.boolean().default(false) (for Phase 18, placeholder for now)

    Export type: EnablePortalAccessInput = z.infer&lt;typeof enablePortalAccessSchema&gt;

    Follow existing schema patterns in file (createProjectSchema, updateTaskSchema, etc).

  </action>
  <verify>
    grep -n "enablePortalAccessSchema" lib/validation.ts
    grep -n "EnablePortalAccessInput" lib/validation.ts
  </verify>
  <done>
    Schema exists with all required fields, input type exported, follows codebase validation conventions
  </done>
</task>

<task type="auto">
  <name>Task 2: Create portal settings configuration modal</name>
  <files>
    components/admin/portal-settings-modal.tsx
  </files>
  <action>
    Create client component at components/admin/portal-settings-modal.tsx.

    Component specification:
    - Props: { open: boolean, onOpenChange: (open: boolean) => void, selectedProjects: Array&lt;{id: string, name: string}&gt;, onSuccess: () => void }
    - Use shadcn/ui Dialog, Form, Input, Textarea, Switch components
    - Use useServerAction hook (from Phase 11) for form state management with enablePortalAccess action
    - Form fields:
      * Welcome Message: Textarea, optional, placeholder "Add a welcome message for clients..."
      * Roadmap Visibility: Switch, default ON, label "Show project roadmap"
      * Files Visibility: Switch, default ON, label "Show shared files"
      * Comments Visibility: Switch, default ON, label "Allow client comments"
      * Note: "Client will be invited via email in next step" (Phase 18 preview)
    - Show list of selected projects at top of modal (project names in badge chips)
    - Footer buttons: "Cancel" (secondary), "Enable Portal Access" (primary, shows loading state during submission)
    - On successful submission:
      * Call onSuccess() callback to refresh parent page data
      * Show toast: "Portal access enabled for {count} project(s)"
      * Close modal
    - On error: Show error toast with message from ActionResult

    Styling:
    - Follow admin modal patterns from existing codebase
    - Use qualia teal for primary button to match portal branding
    - Form uses controlled components with React Hook Form (if used elsewhere) or simple useState

  </action>
  <verify>
    ls -la components/admin/portal-settings-modal.tsx
    grep -n "Dialog" components/admin/portal-settings-modal.tsx
    grep -n "enablePortalAccess" components/admin/portal-settings-modal.tsx
  </verify>
  <done>
    Modal component exists, renders form with all settings fields, handles submission with loading/error states, calls onSuccess callback
  </done>
</task>

<task type="auto">
  <name>Task 3: Create enablePortalAccess server action</name>
  <files>
    app/actions/portal-import.ts
  </files>
  <action>
    Add enablePortalAccess server action to app/actions/portal-import.ts.

    Action specification:
    - Parameter: input (EnablePortalAccessInput from validation.ts) OR FormData
    - Auth check: require user + isUserManagerOrAbove()
    - Validate input with enablePortalAccessSchema using validateData() or parseFormData()
    - For each projectId in input.projectIds:
      * Fetch project to verify it exists and user has access
      * Check if portal access already enabled (client_projects count > 0)
      * If already enabled, skip with warning in response
      * If not enabled, create portal_project_settings record (see note below)
      * Log activity to activity_log table: action_type "portal_access_enabled", actor_id, project_id
    - Return ActionResult with data: { enabledCount: number, skippedCount: number, projectIds: string[] }
    - Call revalidatePath('/admin/projects/import'), revalidatePath('/portal')

    Note on portal_project_settings table:
    - This table does NOT exist yet (v2 feature deferred per REQUIREMENTS.md)
    - For v1.4, store welcomeMessage and visibilitySettings in project.metadata JSONB column OR
    - Simply skip settings storage and just mark as "enabled" by inviting placeholder admin as client (see alternative below)

    **ALTERNATIVE APPROACH (simpler for v1.4):**
    Instead of creating settings table, leverage existing client_projects pattern:
    - Portal access = existence of client_projects record
    - For now, to "enable portal access" without actual client invitation (Phase 18), create a placeholder approach:
      * Just mark project as "ready for portal" by updating a flag or metadata
      * OR skip this action entirely and have it return success (settings saved client-side only for Phase 18 use)
      * Phase 18 will handle actual client invitation which creates client_projects record

    **RECOMMENDED: Defer actual DB changes to Phase 18**
    This action should:
    - Validate input
    - Return success with enabledCount (simulate enabling)
    - Phase 18 will do the real work (client invitation creates client_projects)
    - For now, this is configuration validation + readiness confirmation

    Use the RECOMMENDED approach - validate settings but don't persist yet. Phase 18 will handle actual portal access via client invitation.

  </action>
  <verify>
    grep -n "export async function enablePortalAccess" app/actions/portal-import.ts
    grep -n "enablePortalAccessSchema" app/actions/portal-import.ts
  </verify>
  <done>
    Server action exists, validates settings input, returns success (settings deferred to Phase 18 for actual persistence)
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire settings modal and enable action to import page</name>
  <files>
    app/admin/projects/import/page.tsx
  </files>
  <action>
    Update ProjectImportList component to integrate portal settings modal and enable flow.

    Changes needed:
    - Add state: settingsModalOpen (boolean)
    - Import PortalSettingsModal component
    - Add "Enable Portal Access" button to bulk toolbar (alongside Preview)
      * Enabled when selectedProjectIds.size > 0
      * onClick: open settings modal with selected project details
    - Render &lt;PortalSettingsModal open={settingsModalOpen} onOpenChange={setSettingsModalOpen} selectedProjects={selectedProjectsData} onSuccess={handleEnableSuccess} /&gt;
    - handleEnableSuccess callback:
      * Call router.refresh() to reload page data showing updated status
      * Clear selectedProjectIds Set
      * Close modal
    - selectedProjectsData = projects.filter(p => selectedProjectIds.has(p.id)).map(p => ({id: p.id, name: p.name}))

    Visual updates after enable:
    - Projects that were "Not Enabled" should show "Enabled" badge after refresh
    - This requires getProjectsForPortalImport to detect the change - but since Phase 18 hasn't happened yet, status won't actually change
    - For now, optimistic update: remove enabled projects from filtered "Not Enabled" view client-side

    Note: Since enablePortalAccess doesn't actually change DB yet (deferred to Phase 18), add optimistic client-side tracking:
    - Add state: enabledProjectIds (Set&lt;string&gt;)
    - After successful enable, add project IDs to this Set
    - Filter display: project is "enabled" if hasPortalAccess OR enabledProjectIds.has(id)
    - Badge rendering: check both conditions
    - Show info message at top of page: "Projects marked for portal access will be activated when you send client invitations (coming in next phase)"

  </action>
  <verify>
    grep -n "PortalSettingsModal" app/admin/projects/import/page.tsx
    grep -n "Enable Portal Access" app/admin/projects/import/page.tsx
    grep -n "enabledProjectIds" app/admin/projects/import/page.tsx
  </verify>
  <done>
    Enable Portal Access button opens settings modal, modal submission succeeds, page updates with optimistic status changes, info banner explains next step
  </done>
</task>

</tasks>

<verification>
- [ ] Select one or more projects from Not Enabled filter
- [ ] Click "Enable Portal Access" button in bulk toolbar
- [ ] Verify settings modal opens with selected project names displayed
- [ ] Enter welcome message in textarea
- [ ] Toggle visibility switches on/off
- [ ] Click "Enable Portal Access" button in modal
- [ ] Verify loading state shows during submission
- [ ] Verify success toast appears with count
- [ ] Verify modal closes automatically
- [ ] Verify page refreshes and projects move to Enabled filter
- [ ] Verify info banner displays explaining Phase 18 activation step
- [ ] Verify selection clears after enable
- [ ] Test error handling: try to enable already-enabled project, verify graceful handling
</verification>

<success_criteria>

- Admin can select projects and click Enable Portal Access
- Settings modal displays with all configuration options
- Form validation works (required fields, max lengths)
- Submission succeeds and shows loading/success states
- Projects marked as enabled optimistically in UI
- Info banner clarifies that actual portal access requires client invitation (Phase 18)
- All state management clean (no stale selections, modal state)
- Toast notifications provide clear feedback
- Page data refreshes after enable action
  </success_criteria>

<output>
After completion, create `.planning/phases/17-project-import-flow/17-03-SUMMARY.md`
</output>
