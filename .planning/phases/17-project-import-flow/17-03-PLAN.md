---
phase: 17-project-import-flow
plan: 03
type: execute
wave: 3
depends_on: ['17-02']
files_modified:
  - app/admin/projects/import/page.tsx
  - app/actions/portal-import.ts
  - components/admin/portal-settings-modal.tsx
  - lib/validation.ts
autonomous: true

must_haves:
  truths:
    - 'Admin can click Enable Portal Access and see configuration modal before import'
    - 'Admin can configure welcome message for client portal project view'
    - 'Admin can toggle visibility options (roadmap, files, comments) per project'
    - 'Admin can click Confirm and portal settings are saved for future client invitation'
    - 'Admin sees visual confirmation via toast notification and UI badge update showing project ready for portal'
  artifacts:
    - path: 'components/admin/portal-settings-modal.tsx'
      provides: 'Portal configuration form before enabling access'
      min_lines: 120
    - path: 'app/actions/portal-import.ts'
      provides: 'Server action to save portal settings to project metadata'
      exports: ['savePortalSettings']
    - path: 'lib/validation.ts'
      provides: 'Validation schema for portal settings'
      contains: 'portalSettingsSchema'
  key_links:
    - from: 'app/admin/projects/import/page.tsx'
      to: 'components/admin/portal-settings-modal.tsx'
      via: 'Dialog trigger on Enable button click'
      pattern: 'PortalSettingsModal.*open.*selectedProjects'
    - from: 'components/admin/portal-settings-modal.tsx'
      to: 'app/actions/portal-import.ts'
      via: 'Form submission with settings data'
      pattern: 'savePortalSettings.*formData'
    - from: 'app/actions/portal-import.ts'
      to: 'projects.metadata'
      via: 'Update JSONB column with portal_settings object'
      pattern: 'metadata.*portal_settings'
---

<objective>
Complete portal import flow with configuration modal and settings persistence for Phase 18 invitation system

Purpose: Admins need ability to configure portal settings upfront so Phase 18 can use these settings when sending client invitations and creating portal access

Output: Working end-to-end configuration flow from selection → settings → persistence → visual confirmation (actual client access created in Phase 18)
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/qualia/Projects/live/qualia/.planning/PROJECT.md
@/home/qualia/Projects/live/qualia/.planning/ROADMAP.md
@/home/qualia/Projects/live/qualia/.planning/phases/17-project-import-flow/17-01-SUMMARY.md
@/home/qualia/Projects/live/qualia/.planning/phases/17-project-import-flow/17-02-SUMMARY.md

# Portal access patterns

@/home/qualia/Projects/live/qualia/app/actions/client-portal.ts

# Codebase patterns

@/home/qualia/Projects/live/qualia/.planning/codebase/ARCHITECTURE.md
@/home/qualia/Projects/live/qualia/.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validation schema for portal settings</name>
  <files>
    lib/validation.ts
  </files>
  <action>
    Add portalSettingsSchema to lib/validation.ts.

    Schema fields:
    - projectIds: z.array(z.string().uuid()).min(1, "At least one project required")
    - welcomeMessage: z.string().max(500).optional().nullable()
    - visibilitySettings: z.object({
        showRoadmap: z.boolean().default(true),
        showFiles: z.boolean().default(true),
        showComments: z.boolean().default(true)
      })

    Export type: PortalSettingsInput = z.infer&lt;typeof portalSettingsSchema&gt;

    Follow existing schema patterns in file (createProjectSchema, updateTaskSchema, etc).

  </action>
  <verify>
    grep -n "portalSettingsSchema" lib/validation.ts
    grep -n "PortalSettingsInput" lib/validation.ts
  </verify>
  <done>
    Schema exists with all required fields, input type exported, follows codebase validation conventions
  </done>
</task>

<task type="auto">
  <name>Task 2: Create portal settings configuration modal</name>
  <files>
    components/admin/portal-settings-modal.tsx
  </files>
  <action>
    Create client component at components/admin/portal-settings-modal.tsx.

    Component specification:
    - Props: { open: boolean, onOpenChange: (open: boolean) => void, selectedProjects: Array&lt;{id: string, name: string}&gt;, onSuccess: () => void }
    - Use shadcn/ui Dialog, Form, Input, Textarea, Switch components
    - Use useServerAction hook (from Phase 11) for form state management with savePortalSettings action
    - Form fields:
      * Welcome Message: Textarea, optional, placeholder "Add a welcome message for clients..."
      * Roadmap Visibility: Switch, default ON, label "Show project roadmap"
      * Files Visibility: Switch, default ON, label "Show shared files"
      * Comments Visibility: Switch, default ON, label "Allow client comments"
      * Info banner: "Settings will be applied when you send client invitations (Phase 18)"
    - Show list of selected projects at top of modal (project names in badge chips)
    - Footer buttons: "Cancel" (secondary), "Save Settings" (primary, shows loading state during submission)
    - On successful submission:
      * Call onSuccess() callback to refresh parent page data
      * Show toast: "Portal settings saved for {count} project(s)"
      * Close modal
    - On error: Show error toast with message from ActionResult

    Styling:
    - Follow admin modal patterns from existing codebase
    - Use qualia teal for primary button to match portal branding
    - Form uses controlled components with React Hook Form (if used elsewhere) or simple useState

  </action>
  <verify>
    ls -la components/admin/portal-settings-modal.tsx
    grep -n "Dialog" components/admin/portal-settings-modal.tsx
    grep -n "savePortalSettings" components/admin/portal-settings-modal.tsx
  </verify>
  <done>
    Modal component exists, renders form with all settings fields, handles submission with loading/error states, calls onSuccess callback
  </done>
</task>

<task type="auto">
  <name>Task 3: Create savePortalSettings server action</name>
  <files>
    app/actions/portal-import.ts
  </files>
  <action>
    Add savePortalSettings server action to app/actions/portal-import.ts.

    Action specification:
    - Parameter: input (PortalSettingsInput from validation.ts) OR FormData
    - Auth check: require user + isUserManagerOrAbove()
    - Validate input with portalSettingsSchema using validateData() or parseFormData()
    - For each projectId in input.projectIds:
      * Fetch project to verify it exists and user has access (workspace check)
      * Update project.metadata JSONB column by merging portal_settings object:
        metadata = { ...existingMetadata, portal_settings: { welcomeMessage, visibilitySettings, configuredAt: new Date().toISOString(), configuredBy: user.id } }
      * Use Supabase .update() with JSONB merge pattern (see existing metadata updates in codebase)
      * Log activity to activities table: action_type "portal_settings_configured", actor_id, project_id
    - Return ActionResult with data: { savedCount: number, projectIds: string[] }
    - Call revalidatePath('/admin/projects/import')

    Note: Phase 18 will read these settings from project.metadata.portal_settings when creating client_projects records and sending invitations.

    Detection in Plan 01 server action (getProjectsForPortalImport):
    - Update to also check metadata.portal_settings existence
    - Project is "ready for portal" if: hasPortalAccess (client_projects exists) OR metadata.portal_settings exists
    - Add hasPortalSettings boolean to returned data

  </action>
  <verify>
    grep -n "export async function savePortalSettings" app/actions/portal-import.ts
    grep -n "portalSettingsSchema" app/actions/portal-import.ts
    grep -n "metadata.*portal_settings" app/actions/portal-import.ts
  </verify>
  <done>
    Server action exists, validates settings input, persists to project.metadata JSONB, logs activity, returns success
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire settings modal and update import page detection</name>
  <files>
    app/admin/projects/import/page.tsx
    app/actions/portal-import.ts
  </files>
  <action>
    **Part A: Update getProjectsForPortalImport in portal-import.ts**
    - Modify query to select project.metadata column
    - Check metadata.portal_settings existence: hasPortalSettings = !!project.metadata?.portal_settings
    - Return hasPortalSettings in project object alongside hasPortalAccess
    - Badge logic: project is "Portal Ready" if hasPortalAccess OR hasPortalSettings

    **Part B: Update import page component**
    Update ProjectImportList component to integrate portal settings modal and save flow.

    Changes needed:
    - Add state: settingsModalOpen (boolean)
    - Import PortalSettingsModal component
    - Add "Configure Portal Settings" button to bulk toolbar (alongside Preview)
      * Enabled when selectedProjectIds.size > 0
      * onClick: open settings modal with selected project details
    - Render &lt;PortalSettingsModal open={settingsModalOpen} onOpenChange={setSettingsModalOpen} selectedProjects={selectedProjectsData} onSuccess={handleSaveSuccess} /&gt;
    - handleSaveSuccess callback:
      * Call router.refresh() to reload page data showing updated status
      * Clear selectedProjectIds Set
      * Close modal
    - selectedProjectsData = projects.filter(p => selectedProjectIds.has(p.id)).map(p => ({id: p.id, name: p.name}))

    Badge rendering updates:
    - "Portal Ready" (qualia teal) if hasPortalSettings but not hasPortalAccess → ready for Phase 18 invitation
    - "Portal Active" (green) if hasPortalAccess → client already has access
    - "Not Configured" (gray) if neither → needs configuration
    - Add info banner at top: "Projects marked 'Portal Ready' are configured and ready for client invitations in Phase 18"

  </action>
  <verify>
    grep -n "PortalSettingsModal" app/admin/projects/import/page.tsx
    grep -n "Configure Portal Settings" app/admin/projects/import/page.tsx
    grep -n "hasPortalSettings" app/actions/portal-import.ts
    grep -n "metadata.*portal_settings" app/actions/portal-import.ts
  </verify>
  <done>
    Configure Portal Settings button opens modal, modal submission saves to DB, page updates with "Portal Ready" badges, info banner explains Phase 18 next step
  </done>
</task>

</tasks>

<verification>
- [ ] Select one or more projects from Not Configured filter
- [ ] Click "Configure Portal Settings" button in bulk toolbar
- [ ] Verify settings modal opens with selected project names displayed
- [ ] Enter welcome message in textarea
- [ ] Toggle visibility switches on/off
- [ ] Click "Save Settings" button in modal
- [ ] Verify loading state shows during submission
- [ ] Verify success toast appears with count
- [ ] Verify modal closes automatically
- [ ] Verify page refreshes and projects show "Portal Ready" badge
- [ ] Verify info banner displays explaining Phase 18 activation step
- [ ] Verify selection clears after save
- [ ] Re-select same projects and open settings modal - verify previously saved settings don't pre-populate (Phase 18 concern)
- [ ] Verify projects with existing client_projects show "Portal Active" badge unchanged
</verification>

<success_criteria>

- Admin can select projects and click Configure Portal Settings
- Settings modal displays with all configuration options
- Form validation works (required fields, max lengths)
- Submission saves settings to project.metadata.portal_settings JSONB
- Projects marked as "Portal Ready" with teal badge in UI
- Settings persist in database and available for Phase 18 to read
- Info banner clarifies that client invitations (Phase 18) will activate portal access
- All state management clean (no stale selections, modal state)
- Toast notifications provide clear feedback
- Page data refreshes after save showing updated badges
- Activity log records portal_settings_configured action
  </success_criteria>

<output>
After completion, create `.planning/phases/17-project-import-flow/17-03-SUMMARY.md`
</output>
