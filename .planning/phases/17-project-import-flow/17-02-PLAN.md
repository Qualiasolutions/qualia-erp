---
phase: 17-project-import-flow
plan: 02
type: execute
wave: 2
depends_on: ['17-01']
files_modified:
  - app/admin/projects/import/page.tsx
  - app/actions/portal-import.ts
  - components/portal/roadmap-preview-modal.tsx
  - lib/validation.ts
autonomous: true

must_haves:
  truths:
    - 'Admin can select single or multiple projects and see accurate selection count'
    - 'Admin can click Preview button and see modal showing client-facing roadmap view'
    - 'Preview modal displays project phases exactly as client would see them in portal'
    - 'Admin can close preview and continue selecting different projects'
  artifacts:
    - path: 'app/admin/projects/import/page.tsx'
      provides: 'Selection state management and preview trigger'
      contains: 'selectedProjectIds.*Set'
    - path: 'components/portal/roadmap-preview-modal.tsx'
      provides: 'Client-facing roadmap preview component'
      min_lines: 80
    - path: 'app/actions/portal-import.ts'
      provides: 'Server action to fetch project phases for preview'
      exports: ['getProjectRoadmapPreview']
  key_links:
    - from: 'app/admin/projects/import/page.tsx'
      to: 'components/portal/roadmap-preview-modal.tsx'
      via: 'Dialog trigger and project data passing'
      pattern: 'RoadmapPreviewModal.*open.*projectId'
    - from: 'components/portal/roadmap-preview-modal.tsx'
      to: 'app/actions/portal-import.ts'
      via: 'useEffect fetching project phases'
      pattern: 'getProjectRoadmapPreview.*projectId'
---

<objective>
Enable admin to select projects and preview client-facing roadmap before enabling portal access

Purpose: Admins need confidence that the project roadmap looks good for clients before making it visible, preventing premature or incomplete imports

Output: Functional multi-select with preview modal showing exactly what client will see
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/qualia/Projects/live/qualia/.planning/PROJECT.md
@/home/qualia/Projects/live/qualia/.planning/ROADMAP.md
@/home/qualia/Projects/live/qualia/.planning/phases/17-project-import-flow/17-01-SUMMARY.md

# Portal roadmap patterns

@/home/qualia/Projects/live/qualia/app/portal/[id]/page.tsx

# Codebase patterns

@/home/qualia/Projects/live/qualia/.planning/codebase/ARCHITECTURE.md
@/home/qualia/Projects/live/qualia/.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement selection state and bulk actions toolbar</name>
  <files>
    app/admin/projects/import/page.tsx
  </files>
  <action>
    Update ProjectImportList client component from Plan 01 to add selection functionality.

    Selection state:
    - selectedProjectIds: Set&lt;string&gt; (using useState)
    - Checkbox onChange handlers toggle project ID in/out of Set
    - "Select All" checkbox in header toggles all filtered projects
    - Track selection count for display

    Bulk actions toolbar (appears when selectedProjectIds.size > 0):
    - Fixed position bar at bottom of page (z-index: z-modal from tailwind config)
    - Shows selection count: "X projects selected"
    - Two buttons: "Preview Roadmap" (primary, enabled only when exactly 1 selected), "Clear Selection" (secondary)
    - Preview button opens RoadmapPreviewModal with selected project ID
    - Use shadcn/ui Button, Card for toolbar styling
    - Follow similar pattern to schedule grid's bulk task actions if present

    Checkbox behavior:
    - Individual row checkboxes toggle single project
    - Header "Select All" checkbox has indeterminate state when some but not all selected
    - Clicking row should NOT toggle (only explicit checkbox click)

    No import action yet (that's Plan 03).

  </action>
  <verify>
    grep -n "selectedProjectIds.*useState.*Set" app/admin/projects/import/page.tsx
    grep -n "Preview Roadmap" app/admin/projects/import/page.tsx
  </verify>
  <done>
    Checkboxes are functional, selection count displays, bulk toolbar appears/hides, Preview button enabled when 1 project selected
  </done>
</task>

<task type="auto">
  <name>Task 2: Create server action to fetch project roadmap preview data</name>
  <files>
    app/actions/portal-import.ts
    lib/validation.ts
  </files>
  <action>
    Add server action getProjectRoadmapPreview(projectId: string) to app/actions/portal-import.ts.

    Action specification:
    - Auth check: require user + isUserManagerOrAbove()
    - Fetch project details: id, name, description, project_type, project_status
    - Fetch project_phases for this project ordered by sort_order ASC
    - For each phase, include: id, name, description, status, start_date, end_date, sort_order
    - Do NOT include phase_items or task details (clients don't see those)
    - Return ActionResult with data: { project: {...}, phases: [...] }
    - Follow pattern from app/actions/client-portal.ts getClientDashboardProjects for phase queries

    No validation schema needed (simple projectId string param).

  </action>
  <verify>
    grep -n "export async function getProjectRoadmapPreview" app/actions/portal-import.ts
    grep -n "project_phases" app/actions/portal-import.ts
  </verify>
  <done>
    Server action exists, fetches project + phases, returns structured data for preview modal
  </done>
</task>

<task type="auto">
  <name>Task 3: Create roadmap preview modal component</name>
  <files>
    components/portal/roadmap-preview-modal.tsx
  </files>
  <action>
    Create new client component at components/portal/roadmap-preview-modal.tsx.

    Component specification:
    - Props: { open: boolean, onOpenChange: (open: boolean) => void, projectId: string | null }
    - Use shadcn/ui Dialog for modal container
    - On mount when projectId is not null, call getProjectRoadmapPreview(projectId)
    - Loading state: Show skeleton with shimmer (reuse portal skeleton patterns from Phase 04)
    - Content layout:
      * Dialog header: "Portal Preview: {projectName}"
      * Subheading: "This is exactly what clients will see"
      * Vertical timeline of phases (copy structure from app/portal/[id]/page.tsx roadmap view)
      * Each phase shows: name, description, status badge, dates
      * Use same Badge colors for phase status as portal (green for completed, blue for in-progress, gray for pending)
    - Empty state: If no phases, show "No roadmap configured for this project" message
    - Footer: "Close" button only (no actions in preview)
    - Max height with scroll for long roadmaps (max-h-[80vh] overflow-y-auto)

    Visual consistency:
    - Match portal design system from Phase 15 (same typography, spacing, colors)
    - Use qualia teal accents for consistency with portal branding
    - Apply spring physics to modal entrance (framer-motion spring config from Phase 10)

  </action>
  <verify>
    ls -la components/portal/roadmap-preview-modal.tsx
    grep -n "getProjectRoadmapPreview" components/portal/roadmap-preview-modal.tsx
    grep -n "Dialog" components/portal/roadmap-preview-modal.tsx
  </verify>
  <done>
    Preview modal component exists, fetches roadmap data on open, displays client-facing phase timeline, closes cleanly
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire preview modal to import page</name>
  <files>
    app/admin/projects/import/page.tsx
  </files>
  <action>
    Import and integrate RoadmapPreviewModal into ProjectImportList component.

    Integration steps:
    - Add state: previewProjectId (string | null), previewOpen (boolean)
    - Import RoadmapPreviewModal from components/portal/
    - "Preview Roadmap" button onClick: set previewProjectId to first selected ID, set previewOpen to true
    - Render &lt;RoadmapPreviewModal open={previewOpen} onOpenChange={setPreviewOpen} projectId={previewProjectId} /&gt;
    - Modal closing (onOpenChange false) resets previewProjectId to null

    Also add "Preview" icon button to each table row's Actions column:
    - Eye icon from lucide-react
    - onClick: open preview for that specific row's project (even if not checked)
    - Tooltip: "Preview roadmap"

  </action>
  <verify>
    grep -n "import.*RoadmapPreviewModal" app/admin/projects/import/page.tsx
    grep -n "previewProjectId" app/admin/projects/import/page.tsx
  </verify>
  <done>
    Preview modal opens when Preview button clicked, displays roadmap for selected project, closes cleanly, row-level preview buttons also work
  </done>
</task>

</tasks>

<verification>
- [ ] Select single project via checkbox, verify bulk toolbar appears with "1 project selected"
- [ ] Verify "Preview Roadmap" button is enabled
- [ ] Click Preview, verify modal opens with project name in header
- [ ] Verify modal displays phase timeline matching portal roadmap structure
- [ ] Verify phase badges show correct colors (completed/in-progress/pending)
- [ ] Close modal, verify it closes cleanly and toolbar remains
- [ ] Select multiple projects (2+), verify Preview button still works (shows first selected)
- [ ] Click "Clear Selection", verify toolbar disappears and checkboxes uncheck
- [ ] Click row-level Eye icon, verify preview opens without checking that project
- [ ] Test with project that has no phases, verify empty state message displays
</verification>

<success_criteria>

- Admin can select projects via checkboxes with visual feedback
- Selection count displays accurately in bulk toolbar
- Preview modal opens when exactly 1 project selected
- Modal displays roadmap phases exactly as clients see in portal
- Row-level preview buttons work independently of selection state
- Empty roadmap state handled gracefully with message
- Modal uses portal design system styling for consistency
- All interactions smooth with spring physics animations
  </success_criteria>

<output>
After completion, create `.planning/phases/17-project-import-flow/17-02-SUMMARY.md`
</output>
