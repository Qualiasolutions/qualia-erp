---
phase: 35-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - vercel.json
autonomous: true

must_haves:
  truths:
    - 'Vercel Analytics dashboard receives page view and Web Vitals data from production'
    - 'Vercel Speed Insights is active and collecting performance metrics'
    - 'Uptime monitoring checks services every 15 minutes (not once daily)'
  artifacts:
    - path: 'app/layout.tsx'
      provides: 'Analytics and Speed Insights components rendered in root layout'
      contains: 'Analytics'
    - path: 'app/layout.tsx'
      provides: 'Speed Insights component'
      contains: 'SpeedInsights'
    - path: 'vercel.json'
      provides: 'Uptime cron running every 15 minutes'
      contains: '*/15 * * * *'
  key_links:
    - from: 'app/layout.tsx'
      to: '@vercel/analytics'
      via: 'Analytics component import'
      pattern: "from '@vercel/analytics'"
    - from: 'app/layout.tsx'
      to: '@vercel/speed-insights'
      via: 'SpeedInsights component import'
      pattern: "from '@vercel/speed-insights'"
    - from: 'vercel.json'
      to: 'app/api/cron/uptime-check/route.ts'
      via: 'cron schedule for uptime monitoring'
      pattern: '/api/cron/uptime-check'
---

<objective>
Add Vercel Analytics + Speed Insights to the root layout, and increase uptime check frequency from daily to every 15 minutes.

Purpose: Currently there is no page-level performance data (no Web Vitals, no page views), and uptime checks run once daily at 8 AM — meaning downtime could go unnoticed for up to 24 hours. This plan addresses OBS-02 and OBS-03.

Output: Analytics and Speed Insights active in production, uptime cron fires every 15 minutes.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/layout.tsx
@vercel.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and add Vercel Analytics + Speed Insights to root layout</name>
  <files>
    app/layout.tsx
  </files>
  <action>
    1. Install both packages: `npm install @vercel/analytics @vercel/speed-insights`

    2. Update `app/layout.tsx`:
       - Import `{ Analytics }` from `@vercel/analytics/next`
       - Import `{ SpeedInsights }` from `@vercel/speed-insights/next`
       - Add both components inside the `<body>` tag, AFTER the `<Toaster>` component (they render nothing visible — just inject tracking scripts):
         ```tsx
         <Toaster position="top-center" richColors closeButton />
         <Analytics />
         <SpeedInsights />
         ```
       - These components auto-detect production vs development — no conditional rendering needed
       - No props needed — defaults are correct (they use the Vercel project's analytics endpoint automatically)

    3. Run `npx tsc --noEmit` to verify no type errors.

  </action>
  <verify>
    - `grep "Analytics" app/layout.tsx` — Analytics component present
    - `grep "SpeedInsights" app/layout.tsx` — SpeedInsights component present
    - `grep "@vercel/analytics" package.json` — package installed
    - `grep "@vercel/speed-insights" package.json` — package installed
    - `npx tsc --noEmit` exits 0
  </verify>
  <done>
    Analytics and SpeedInsights components render in root layout. Both packages installed. No type errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Increase uptime check cron frequency to every 15 minutes</name>
  <files>
    vercel.json
  </files>
  <action>
    1. Update `vercel.json`: Change the uptime-check cron schedule from `"0 8 * * *"` (daily at 8 AM) to `"*/15 * * * *"` (every 15 minutes).

    That is the ONLY change. The cron route itself (`app/api/cron/uptime-check/route.ts`) already handles the full flow: check UptimeRobot API, alert on down services, send email + in-app notification. No code changes needed there.

    2. Run `npm run build` to verify the build still passes.

    Note on Vercel cron limits: Hobby plan allows 2 crons, Pro allows unlimited. Since this is on the Pro plan (archivedqualia team), every-15-min is fine. If it were Hobby, we'd use UptimeRobot's native alerting instead and remove this cron entirely.

  </action>
  <verify>
    - `grep "*/15" vercel.json` — cron runs every 15 minutes
    - `npm run build` succeeds
  </verify>
  <done>
    Uptime check cron fires every 15 minutes instead of once daily. Downtime alerts arrive within 15 minutes max.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx tsc --noEmit` — zero type errors
3. `grep "Analytics\|SpeedInsights" app/layout.tsx` — both components in layout
4. `grep "*/15" vercel.json` — uptime cron at 15-min interval
5. `grep "@vercel/analytics" package.json && grep "@vercel/speed-insights" package.json` — packages installed
</verification>

<success_criteria>

- @vercel/analytics and @vercel/speed-insights installed and rendered in root layout
- Vercel Analytics will show page views and Web Vitals once deployed to production
- Speed Insights will collect performance data once deployed to production
- Uptime cron runs every 15 minutes (down from 24-hour gap)
- Build passes cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/35-observability/35-02-SUMMARY.md`
</output>
