---
phase: 10-differentiator-animations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/today-dashboard/tasks-widget.tsx
  - components/today-dashboard/inbox-widget.tsx
  - components/project-pipeline/phase-card.tsx
  - components/project-pipeline/task-item.tsx
  - app/portal/[id]/page.tsx
  - components/portal/project-roadmap.tsx
autonomous: true

must_haves:
  truths:
    - 'User sees task list items appear sequentially with staggered timing (not all at once)'
    - 'User sees inbox items appear sequentially with staggered timing'
    - 'User sees roadmap phases reveal smoothly as they scroll into view'
  artifacts:
    - path: 'components/today-dashboard/tasks-widget.tsx'
      provides: 'Stagger animation on task list items'
      contains: 'staggerChildren'
    - path: 'components/project-pipeline/phase-card.tsx'
      provides: 'Scroll-triggered reveal animation'
      contains: 'useInView'
  key_links:
    - from: 'components/project-pipeline/phase-card.tsx'
      to: 'framer-motion useInView hook'
      via: 'scroll position detection'
      pattern: "useInView\\(.*\\{.*once.*\\}"
---

<objective>
Add stagger animations to list items and scroll-triggered reveals to roadmap phases for premium, Apple-level animation polish.

Purpose: Sequential item appearance (stagger) creates visual hierarchy and feels more polished than instant rendering. Scroll-triggered reveals add delight and draw attention to content as user explores.

Output: Task lists, inbox, and roadmap phases with premium animation timing and scroll-aware reveals.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-animation-system-infrastructure/05-01-SUMMARY.md

# Animation infrastructure from Phase 5

- Framer Motion already installed (v12.23.26)
- PageTransition component exists
- Reduced motion support in globals.css
- 31 files already use framer-motion imports

# Key list components already using motion

@components/today-dashboard/tasks-widget.tsx
@components/today-dashboard/inbox-widget.tsx
@components/project-pipeline/phase-card.tsx

# Requirements

ANIM-01: Stagger animations on list items
ANIM-02: Scroll-triggered reveals on roadmap phases
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stagger animations to task and inbox lists</name>
  <files>
    components/today-dashboard/tasks-widget.tsx
    components/today-dashboard/inbox-widget.tsx
  </files>
  <action>
Add stagger animations to task and inbox list items using Framer Motion's staggerChildren pattern.

**tasks-widget.tsx:**

1. Wrap the task list container (the parent element containing mapped task items) in a motion.div
2. Add variants to container:
   ```typescript
   const containerVariants = {
     hidden: { opacity: 0 },
     visible: {
       opacity: 1,
       transition: {
         staggerChildren: 0.05, // 50ms between each item
         delayChildren: 0.1, // Start after 100ms
       },
     },
   };
   ```
3. Add variants to individual task items (already motion.div from existing code):
   ```typescript
   const itemVariants = {
     hidden: { opacity: 0, y: 10 },
     visible: {
       opacity: 1,
       y: 0,
       transition: {
         duration: 0.3,
         ease: [0.16, 1, 0.3, 1], // Premium easing
       },
     },
   };
   ```
4. Apply variants: container gets `variants={containerVariants}` and `initial="hidden"` and `animate="visible"`
5. Each task item gets `variants={itemVariants}`

**inbox-widget.tsx:**

- Apply identical pattern (same variants, same stagger timing)
- Ensure both widgets feel consistent

**Important:**

- Respect existing AnimatePresence wrappers for task removal animations
- Stagger only applies on initial render, not on re-renders (key stays stable)
- Use layout="position" on items if they're drag-and-drop enabled
  </action>
  <verify> - `npm run build` succeeds with no TypeScript errors - Open dashboard at http://localhost:3000 - Observe tasks widget items appear sequentially with 50ms stagger - Observe inbox widget items appear sequentially with 50ms stagger - Refresh page multiple times to confirm consistent stagger timing - Check that task completion/deletion animations still work (AnimatePresence not broken)
  </verify>
  <done>
  Task and inbox list items appear sequentially with staggered timing (50ms between items), smooth fade+slide from opacity 0/y 10 to opacity 1/y 0, premium easing applied, and existing removal animations still functional.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Add scroll-triggered reveals to roadmap phases</name>
  <files>
    components/project-pipeline/phase-card.tsx
    components/portal/project-roadmap.tsx
  </files>
  <action>
Add scroll-triggered reveal animations to phase cards using Framer Motion's useInView hook.

**phase-card.tsx:**

1. Import `useInView` from framer-motion: `import { motion, useInView } from 'framer-motion';`
2. At top of component function, create a ref: `const ref = useRef(null);`
3. Add useInView hook: `const isInView = useInView(ref, { once: true, margin: "-100px" });`
   - `once: true` - animation triggers only once (doesn't repeat on scroll back)
   - `margin: "-100px"` - triggers when element is 100px into viewport (not immediately at edge)
4. Wrap the phase card root element with motion wrapper:
   ```tsx
   <motion.div
     ref={ref}
     initial={{ opacity: 0, y: 30 }}
     animate={isInView ? { opacity: 1, y: 0 } : { opacity: 0, y: 30 }}
     transition={{
       duration: 0.5,
       ease: [0.16, 1, 0.3, 1],
     }}
   >
     {/* existing phase card content */}
   </motion.div>
   ```

**portal/project-roadmap.tsx (if separate component):**

- Apply identical pattern to portal roadmap phases
- Same timing, same margin, same easing
- Consistent experience across admin and portal views

**Important:**

- Use `once: true` to prevent animation spam on scroll up/down
- Use negative margin to start animation slightly before element fully visible (feels more natural)
- Ensure ref is attached to the actual card element, not a nested child
  </action>
  <verify> - `npm run build` succeeds with no TypeScript errors - Navigate to project roadmap page: http://localhost:3000/projects/[any-project-id]/roadmap - Scroll down slowly and observe each phase card fade+slide into view as you scroll - Scroll back up - phases should NOT re-animate (once: true working) - Open portal roadmap: http://localhost:3000/portal/[project-id] - Verify portal phases also reveal on scroll with identical timing
  </verify>
  <done>
  Phase cards reveal smoothly as user scrolls them into view (opacity 0/y 30 → opacity 1/y 0), animation triggers once when card is 100px into viewport, same behavior in both admin and portal views, and no re-animation on scroll back up.
  </done>
  </task>

</tasks>

<verification>
Manual verification steps:

1. **Dashboard stagger animations**:
   - Visit http://localhost:3000
   - Observe tasks widget items appear sequentially (not all at once)
   - Observe inbox widget items appear sequentially
   - Each item should fade+slide from below with 50ms stagger between items

2. **Roadmap scroll reveals**:
   - Visit http://localhost:3000/projects/[any-project-id]/roadmap
   - Scroll down slowly
   - Each phase card should fade+slide into view as you scroll
   - Scroll back up - phases should NOT re-animate

3. **Portal consistency**:
   - Visit http://localhost:3000/portal/[client-project-id]
   - Verify roadmap phases reveal on scroll identically to admin view

4. **Reduced motion**:
   - Enable "Reduce motion" in OS accessibility settings
   - Refresh dashboard - all animations should become instant (prefers-reduced-motion from Phase 5 still working)

5. **No regressions**:
   - Task completion checkboxes still work
   - Task deletion animations still smooth
   - Phase expand/collapse still functional
     </verification>

<success_criteria>

- Task list items in tasks-widget and inbox-widget appear sequentially with 50ms stagger
- Stagger uses fade+slide animation (opacity 0/y 10 → 1/0)
- Phase cards on roadmap reveal smoothly as user scrolls them into viewport
- Scroll-triggered animation fires once per card (doesn't repeat on scroll back)
- Animations use premium easing (cubic-bezier(0.16, 1, 0.3, 1))
- Reduced motion setting disables all animations (respects a11y)
- No TypeScript errors, no regressions on existing animations
  </success_criteria>

<output>
After completion, create `.planning/phases/10-differentiator-animations/10-01-SUMMARY.md`
</output>
