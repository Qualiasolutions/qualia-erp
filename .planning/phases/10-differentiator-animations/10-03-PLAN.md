---
phase: 10-differentiator-animations
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - package.json
  - components/ui/drawer.tsx
  - components/new-task-modal.tsx
  - components/new-meeting-modal.tsx
  - app/portal/layout.tsx
autonomous: true

must_haves:
  truths:
    - 'User can swipe down to dismiss mobile drawers (touch gesture)'
    - 'User sees smooth spring animation when drawer opens/closes'
    - 'User experiences native-feeling drawer physics (follows finger)'
  artifacts:
    - path: 'components/ui/drawer.tsx'
      provides: 'Gesture-enabled drawer component using Vaul'
      min_lines: 50
      exports: ['Drawer', 'DrawerTrigger', 'DrawerContent', 'DrawerClose']
    - path: 'components/new-task-modal.tsx'
      provides: 'Task modal using drawer on mobile'
      contains: 'Drawer.*DrawerContent'
  key_links:
    - from: 'components/new-task-modal.tsx'
      to: 'components/ui/drawer.tsx'
      via: 'conditional rendering (mobile drawer, desktop dialog)'
      pattern: 'isMobile.*Drawer.*Dialog'
---

<objective>
Add gesture-based drawer interactions to mobile modals using Vaul library for native-feeling swipe-to-dismiss on touch devices.

Purpose: Mobile users expect native drawer behavior (swipe down to dismiss). Standard dialogs require tapping X button or backdrop. Gesture-based drawers feel premium and intuitive on mobile (iOS/Android standard pattern).

Output: New task and meeting modals become gesture-enabled drawers on mobile screens, with smooth spring physics and swipe-to-dismiss.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Mobile responsiveness already implemented (Phase 8)

- Dialogs currently work on mobile but no gesture support
- Touch targets meet 44x44px minimum (MOBILE-03)
- Mobile responsive layouts complete

# Modal components

@components/new-task-modal.tsx
@components/new-meeting-modal.tsx

# Requirements

ANIM-04: Gesture-based interactions (swipe to dismiss drawers)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vaul and create drawer component</name>
  <files>
    package.json
    components/ui/drawer.tsx
  </files>
  <action>
Install Vaul library and create a shadcn-style drawer component with gesture support.

**Step 1: Install Vaul**

```bash
npm install vaul
```

**Step 2: Create drawer.tsx**

Create `components/ui/drawer.tsx` following shadcn/ui patterns:

```tsx
'use client';

import * as React from 'react';
import { Drawer as DrawerPrimitive } from 'vaul';
import { cn } from '@/lib/utils';

const Drawer = ({
  shouldScaleBackground = true,
  ...props
}: React.ComponentProps<typeof DrawerPrimitive.Root>) => (
  <DrawerPrimitive.Root shouldScaleBackground={shouldScaleBackground} {...props} />
);
Drawer.displayName = 'Drawer';

const DrawerTrigger = DrawerPrimitive.Trigger;
const DrawerPortal = DrawerPrimitive.Portal;
const DrawerClose = DrawerPrimitive.Close;

const DrawerOverlay = React.forwardRef<
  React.ElementRef<typeof DrawerPrimitive.Overlay>,
  React.ComponentPropsWithoutRef<typeof DrawerPrimitive.Overlay>
>(({ className, ...props }, ref) => (
  <DrawerPrimitive.Overlay
    ref={ref}
    className={cn('fixed inset-0 z-50 bg-black/40', className)}
    {...props}
  />
));
DrawerOverlay.displayName = DrawerPrimitive.Overlay.displayName;

const DrawerContent = React.forwardRef<
  React.ElementRef<typeof DrawerPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof DrawerPrimitive.Content>
>(({ className, children, ...props }, ref) => (
  <DrawerPortal>
    <DrawerOverlay />
    <DrawerPrimitive.Content
      ref={ref}
      className={cn(
        'fixed inset-x-0 bottom-0 z-50 mt-24 flex h-auto flex-col rounded-t-[10px] border border-border bg-background',
        className
      )}
      {...props}
    >
      <div className="mx-auto mt-4 h-2 w-[100px] rounded-full bg-muted" />
      {children}
    </DrawerPrimitive.Content>
  </DrawerPortal>
));
DrawerContent.displayName = 'DrawerContent';

const DrawerHeader = ({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) => (
  <div className={cn('grid gap-1.5 p-4 text-center sm:text-left', className)} {...props} />
);
DrawerHeader.displayName = 'DrawerHeader';

const DrawerFooter = ({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) => (
  <div className={cn('mt-auto flex flex-col gap-2 p-4', className)} {...props} />
);
DrawerFooter.displayName = 'DrawerFooter';

const DrawerTitle = React.forwardRef<
  React.ElementRef<typeof DrawerPrimitive.Title>,
  React.ComponentPropsWithoutRef<typeof DrawerPrimitive.Title>
>(({ className, ...props }, ref) => (
  <DrawerPrimitive.Title
    ref={ref}
    className={cn('text-lg font-semibold leading-none tracking-tight', className)}
    {...props}
  />
));
DrawerTitle.displayName = DrawerPrimitive.Title.displayName;

const DrawerDescription = React.forwardRef<
  React.ElementRef<typeof DrawerPrimitive.Description>,
  React.ComponentPropsWithoutRef<typeof DrawerPrimitive.Description>
>(({ className, ...props }, ref) => (
  <DrawerPrimitive.Description
    ref={ref}
    className={cn('text-sm text-muted-foreground', className)}
    {...props}
  />
));
DrawerDescription.displayName = DrawerPrimitive.Description.displayName;

export {
  Drawer,
  DrawerPortal,
  DrawerOverlay,
  DrawerTrigger,
  DrawerClose,
  DrawerContent,
  DrawerHeader,
  DrawerFooter,
  DrawerTitle,
  DrawerDescription,
};
```

**Key features:**

- Gesture support via Vaul (built-in swipe-to-dismiss)
- Spring physics via Vaul's default damped spring
- Visual drag handle (gray bar at top)
- Overlay with backdrop blur
- Compatible with existing Dialog API patterns
  </action>
  <verify> - `npm install` completes successfully - `npm run build` succeeds with no TypeScript errors - Vaul appears in package.json dependencies - components/ui/drawer.tsx exists with all exported components - No import errors when importing drawer components
  </verify>
  <done>
  Vaul library installed in package.json, drawer.tsx created with Drawer, DrawerTrigger, DrawerContent, DrawerClose, DrawerHeader, DrawerFooter, DrawerTitle, DrawerDescription exports, gesture support enabled, visual drag handle present, and TypeScript types correct.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Convert modals to responsive drawer/dialog pattern</name>
  <files>
    components/new-task-modal.tsx
    components/new-meeting-modal.tsx
  </files>
  <action>
Convert new-task-modal and new-meeting-modal to use Drawer on mobile (<768px) and Dialog on desktop using shadcn responsive pattern.

**Pattern: Responsive drawer/dialog**

Use a hook to detect mobile:

```tsx
import { useMediaQuery } from '@/hooks/use-media-query';

function ResponsiveModal() {
  const isMobile = useMediaQuery('(max-width: 768px)');

  if (isMobile) {
    return (
      <Drawer open={open} onOpenChange={setOpen}>
        <DrawerTrigger>...</DrawerTrigger>
        <DrawerContent>
          <DrawerHeader>
            <DrawerTitle>Title</DrawerTitle>
            <DrawerDescription>Description</DrawerDescription>
          </DrawerHeader>
          {/* form content */}
          <DrawerFooter>{/* buttons */}</DrawerFooter>
        </DrawerContent>
      </Drawer>
    );
  }

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger>...</DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Title</DialogTitle>
          <DialogDescription>Description</DialogDescription>
        </DialogHeader>
        {/* form content */}
        <DialogFooter>{/* buttons */}</DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

**Create useMediaQuery hook if missing:**

If `hooks/use-media-query.ts` doesn't exist, create it:

```tsx
import * as React from 'react';

export function useMediaQuery(query: string) {
  const [value, setValue] = React.useState(false);

  React.useEffect(() => {
    function onChange(event: MediaQueryListEvent) {
      setValue(event.matches);
    }

    const result = matchMedia(query);
    result.addEventListener('change', onChange);
    setValue(result.matches);

    return () => result.removeEventListener('change', onChange);
  }, [query]);

  return value;
}
```

**Apply to both modal components:**

**new-task-modal.tsx:**

1. Import Drawer components and useMediaQuery
2. Duplicate form JSX structure (one for Drawer, one for Dialog)
3. Conditional render based on isMobile
4. Ensure form submission works in both cases
5. Preserve all existing functionality (optimistic updates, validation, server actions)

**new-meeting-modal.tsx:**

- Apply identical pattern
- Preserve meeting form fields and logic
- Ensure both desktop Dialog and mobile Drawer work

**Important:**

- Form state must be shared between both render paths
- Server action calls identical in both cases
- Validation logic unchanged
- Keep existing modal open/close logic (controlled via open/onOpenChange)
- Drawer should only render on mobile (check window.matchMedia at runtime)
  </action>
  <verify> - `npm run build` succeeds with no TypeScript errors - **Desktop test** (Chrome DevTools responsive mode, 1920px width): - Click "New Task" button → Dialog opens (centered modal) - Fill form and submit → Works as before - Click outside or X → Dialog closes - **Mobile test** (Chrome DevTools responsive mode, 375px width): - Click "New Task" button → Drawer slides up from bottom - Swipe down on drag handle → Drawer dismisses (gesture works!) - Tap backdrop → Drawer closes - Fill form and submit → Works identically to desktop - **Meeting modal** (repeat above tests for new meeting) - Switch between desktop and mobile widths → Correct component renders
  </verify>
  <done>
  new-task-modal and new-meeting-modal render as Drawer on mobile (<768px) and Dialog on desktop, gesture swipe-to-dismiss works on mobile drawers, form submission works identically in both cases, all existing functionality preserved (validation, optimistic updates, server actions), and TypeScript types correct.
  </done>
  </task>

</tasks>

<verification>
Manual verification steps:

1. **Installation verification**:
   - Check package.json contains "vaul" dependency
   - Check components/ui/drawer.tsx exists
   - Verify no import errors on build

2. **Desktop experience**:
   - Visit http://localhost:3000 at 1920px width
   - Click "New Task" button
   - Observe Dialog (centered modal, not drawer)
   - Submit form → Works as before
   - Click X or backdrop → Closes

3. **Mobile experience**:
   - Visit http://localhost:3000 at 375px width (Chrome DevTools responsive mode)
   - Click "New Task" button
   - Observe Drawer (slides up from bottom, not centered)
   - See drag handle (gray bar at top)
   - Swipe down on drawer → Dismisses smoothly (gesture works!)
   - Try again, tap backdrop → Also dismisses
   - Submit form → Works identically to desktop

4. **Gesture physics**:
   - Open drawer on mobile
   - Swipe down slightly and release → Drawer springs back
   - Swipe down past threshold → Drawer dismisses with spring
   - Feel natural, native-like physics (follows finger)

5. **Meeting modal**:
   - Repeat all above tests for "New Meeting" button
   - Verify identical behavior (drawer on mobile, dialog on desktop)

6. **Reduced motion**:
   - Enable "Reduce motion" in OS settings
   - Drawer should still work but with instant animations (no spring)

7. **Breakpoint switching**:
   - Resize browser from desktop → mobile → desktop
   - Modal should switch between Dialog and Drawer seamlessly
   - No hydration errors, no layout shift
     </verification>

<success_criteria>

- Vaul library installed and drawer.tsx component created with full API
- new-task-modal and new-meeting-modal use Drawer on mobile (<768px) and Dialog on desktop
- Mobile drawers support swipe-to-dismiss gesture (drag down to close)
- Drawer has visual drag handle (gray bar) for affordance
- Drawer physics feel natural (follows finger, spring on release)
- Form submission works identically in both Drawer and Dialog
- No regressions on existing modal functionality
- Breakpoint switching works without hydration errors
- Reduced motion setting makes animations instant
- No TypeScript errors, no console errors
  </success_criteria>

<output>
After completion, create `.planning/phases/10-differentiator-animations/10-03-SUMMARY.md`
</output>
