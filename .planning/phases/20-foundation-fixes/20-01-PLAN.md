---
phase: 20-foundation-fixes
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/client-portal.ts
  - components/portal/portal-admin-panel.tsx
  - app/portal/page.tsx
autonomous: true

must_haves:
  truths:
    - 'Admin can create a project via the portal admin panel and it appears immediately in the project list'
    - 'Project creation failures surface a specific, readable error message (no silent failures)'
    - 'router.refresh() triggers a server-side re-render so the new project shows without a full page reload'
  artifacts:
    - path: 'app/actions/client-portal.ts'
      provides: 'createProjectFromPortal server action with console.error on every failure branch'
      contains: 'createProjectFromPortal'
    - path: 'components/portal/portal-admin-panel.tsx'
      provides: 'Inline project creation form with loading state, error toast, and router.refresh()'
      contains: 'createProjectFromPortal'
    - path: 'app/portal/page.tsx'
      provides: 'Admin dashboard that passes projects list to PortalAdminPanel'
      contains: 'PortalAdminPanel'
  key_links:
    - from: 'components/portal/portal-admin-panel.tsx'
      to: 'app/actions/client-portal.ts'
      via: 'createProjectFromPortal() server action call'
      pattern: 'createProjectFromPortal'
    - from: 'app/actions/client-portal.ts'
      to: 'supabase projects table'
      via: "supabase.from('projects').insert(...)"
      pattern: "from\\('projects'\\)\\.insert"
---

<objective>
Fix the project creation flow in the portal admin panel so that creating a project from the admin UI reliably saves to the database and the new project appears immediately in the project list without a full page reload.

Purpose: Admins cannot onboard clients without first creating projects. Silent failures block the entire onboarding workflow.
Output: Working project creation form with loading states, specific error messages, and immediate UI refresh.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/actions/client-portal.ts
@components/portal/portal-admin-panel.tsx
@app/portal/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add project creation form to PortalAdminPanel with loading state and error handling</name>
  <files>components/portal/portal-admin-panel.tsx</files>
  <action>
    The PortalAdminPanel currently has NO project creation UI. Add an inline form section ABOVE the "Add Client to Project" card that lets admin create a new project directly from the panel.

    Import `createProjectFromPortal` from `@/app/actions/client-portal`.

    Add state:
    - `projectName: string` (default '')
    - `projectType: string` (default 'web_design')
    - `isCreatingProject: boolean` (separate pending flag, do NOT reuse `isPending` which is for invite flow)
    - `projectError: string | null` (default null)

    Add `handleCreateProject` async function:
    1. Validate: if `!projectName.trim()` → `toast.error('Project name is required')` and return early
    2. Set `isCreatingProject(true)`, clear `projectError`
    3. Call `const result = await createProjectFromPortal({ name: projectName.trim(), project_type: projectType })`
    4. If `!result.success`: `setProjectError(result.error || 'Failed to create project')`, `toast.error(result.error || 'Failed to create project')`, return
    5. On success: `toast.success('Project created')`, clear `projectName`, call `router.refresh()`

    Add a Card before the "Add Client to Project" card:
    - Title: "Create New Project" with a `FolderPlus` icon (import from lucide-react)
    - Grid layout: `grid gap-3 sm:grid-cols-3`
    - Input: projectName, placeholder "Project name *"
    - Select for projectType with options: web_design, ai_agent, voice_agent, seo, ads (display labels: "Web Design", "AI Agent", "Voice Agent", "SEO", "Ads")
    - Button "Create Project" with `isCreatingProject` spinner, disabled when `isCreatingProject || !projectName.trim()`
    - Below the grid, if `projectError`, show: `<p className="mt-2 text-sm text-red-600">{projectError}</p>`

    Keep ALL existing invite form code intact.

  </action>
  <verify>
    Run `npx tsc --noEmit` in /home/qualia/Projects/live/qualia — zero TS errors.
    Run `npm run build` — build succeeds.
  </verify>
  <done>
    PortalAdminPanel renders a "Create New Project" card above the invite form. The button shows a spinner while pending and displays a specific error message beneath the form on failure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify createProjectFromPortal action logs and router.refresh() wiring</name>
  <files>app/actions/client-portal.ts</files>
  <action>
    Read the existing `createProjectFromPortal` function (already in the file starting at line ~437). Verify these conditions are met — fix anything that isn't:

    1. **All failure branches log to console.error with context.** Currently the insert error branch does `console.error('[createProjectFromPortal] Insert error:', error.message, error.details, error.hint)` — confirm this is present. The "No data returned" branch must also log: add `console.error('[createProjectFromPortal] No data returned after insert')` if missing.

    2. **Error messages returned to the client are specific.** The insert error returns `DB error: ${error.message}` — good. The "no data" branch returns `'Project created but no data returned'` — acceptable.

    3. **revalidatePath is called on success.** Confirm `revalidatePath('/projects')` and `revalidatePath('/portal')` are both called after a successful insert. If not present, add them before the `return { success: true, data }`.

    4. **The function signature and return type are correct** — returns `ActionResult`.

    No changes needed if all four conditions already pass. Only make targeted fixes.

  </action>
  <verify>
    `npx tsc --noEmit` passes.
    Manually review the four conditions above against the file — all four must be true.
  </verify>
  <done>
    createProjectFromPortal logs on every failure path, returns specific error strings, and calls revalidatePath on success.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `npm run build` — zero errors
3. Visit https://qualia-erp.vercel.app/portal as admin — "Create New Project" card appears above invite form
4. Submit with empty name — toast "Project name is required" appears, no network call made
5. Submit with valid name — spinner shows, project saved, toast "Project created", project list refreshes
</verification>

<success_criteria>

- Admin can create a project from /portal without any silent failures
- Error messages from the server action appear as toast notifications in the UI
- router.refresh() fires after successful creation so the new project is immediately visible
- TypeScript compiles clean, build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-fixes/20-01-SUMMARY.md`
</output>
