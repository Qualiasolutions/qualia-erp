---
phase: 20-foundation-fixes
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/client-portal.ts
  - components/portal/portal-admin-panel.tsx
autonomous: false

must_haves:
  truths:
    - 'Admin invites a new client email and sees a credentials dialog showing email, password, and portal URL'
    - 'Client can log in at /auth/login with those credentials and land on /portal'
    - 'Client sees the project they were assigned to on their portal dashboard'
    - 'Existing client email (already has account) links to the project without creating a duplicate'
    - 'Credential copy button copies all three fields to clipboard in one click'
  artifacts:
    - path: 'app/actions/client-portal.ts'
      provides: 'inviteClientByEmail returns { userId, tempPassword } on new account creation'
      contains: 'tempPassword'
    - path: 'components/portal/portal-admin-panel.tsx'
      provides: 'Credentials box renders when tempPassword is returned, copy button works'
      contains: 'credentials'
  key_links:
    - from: 'components/portal/portal-admin-panel.tsx'
      to: 'app/actions/client-portal.ts'
      via: 'inviteClientByEmail() → result.data.tempPassword'
      pattern: 'tempPassword'
    - from: 'app/actions/client-portal.ts'
      to: 'supabase auth admin.createUser'
      via: 'adminClient.auth.admin.createUser({ email_confirm: true })'
      pattern: 'createUser'
---

<objective>
Verify the complete client invite → account creation → login → portal access flow works end-to-end. Fix any gaps discovered during testing.

Purpose: This is the core onboarding flow. If it breaks silently, Moayad cannot onboard live clients.
Output: Confirmed working invite flow with visible credentials, verified login, and correct portal landing for new clients.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@app/actions/client-portal.ts
@components/portal/portal-admin-panel.tsx
@app/portal/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix inviteClientByEmail for reliability</name>
  <files>app/actions/client-portal.ts</files>
  <action>
    Read `inviteClientByEmail` carefully (lines ~16-146 in client-portal.ts). Verify and fix these specific issues:

    **Issue 1 — Profile upsert failure is silently ignored:**
    The current code logs `console.error('[inviteClientByEmail] Profile creation error:', profileError)` but continues. This is correct — Supabase has a trigger that auto-creates profiles, so failure here is non-fatal. No change needed unless the profile doesn't exist after createUser succeeds.

    **Issue 2 — Missing workspace_id on client_projects insert:**
    The `client_projects` insert does NOT include `workspace_id`. Check the table schema via the DB (use Supabase MCP or inspect existing rows). If `workspace_id` is a NOT NULL column, this is the root cause of the silent failure. Fix: import `getCurrentWorkspaceId` (already imported at top of file) and add `workspace_id: workspaceId` to the `client_projects` insert payload.

    To check: run this via Supabase MCP → execute_sql on project ref `vbpzaiqovffpsroxaulv`:
    ```sql
    SELECT column_name, is_nullable
    FROM information_schema.columns
    WHERE table_name = 'client_projects'
    ORDER BY ordinal_position;
    ```

    If `workspace_id` is nullable, no change needed for that field.

    **Issue 3 — Return value must include tempPassword:**
    Confirm the success return is `{ success: true, data: { userId: newUserId, tempPassword, emailSent: false } }`. The UI at `portal-admin-panel.tsx` line ~121 reads `data?.tempPassword` — this must be present or the credentials box never shows.

    **Issue 4 — Error logging on createUser failure:**
    Confirm `createError` is logged with full detail: `console.error('[inviteClientByEmail] Create user error:', createError)`. If it only logs the message, add `.code`, `.status` to the log call.

    Make only the changes required by actual issues found.

  </action>
  <verify>
    `npx tsc --noEmit` — zero errors.
    If workspace_id fix was applied: `npm run build` passes.
  </verify>
  <done>
    inviteClientByEmail returns tempPassword on new account creation, logs full error detail on all failure paths, and inserts client_projects with all required non-nullable columns.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 fixed potential DB schema issues in inviteClientByEmail. The credentials flow (Task 1 in 20-01 set up createProjectFromPortal; the invite flow in portal-admin-panel.tsx was already implemented in Phase 19).

    To test the complete end-to-end invite flow:

  </what-built>
  <how-to-verify>
    1. Deploy latest changes: run `npm run build` then `vercel --prod` in /home/qualia/Projects/live/qualia (or push to master for auto-deploy)

    2. Log in as admin at https://qualia-erp.vercel.app/auth/login

    3. Navigate to https://qualia-erp.vercel.app/portal

    4. In the "Add Client to Project" form:
       - Enter a test email (use a real inbox you can check, e.g. a Gmail alias)
       - Enter a client name
       - Select any project from the dropdown
       - Click "Create & Link"

    5. Verify: a green credentials box appears below the form showing:
       - Email: [the email you entered]
       - Password: [a Qualia-xxxxx! format password]
       - Login at: https://qualia-erp.vercel.app/portal

    6. Click "Copy" — paste into a text editor and confirm all 3 lines are present

    7. Open an incognito window → https://qualia-erp.vercel.app/auth/login
       - Enter the credentials from step 5
       - Login should succeed and redirect to /portal

    8. Verify: the client's portal shows the project they were assigned to

    9. Navigate to /portal/[project-id] — confirm roadmap/files/updates pages load without error

    **If invite fails:** Note the exact error toast message and report it.
    **If login fails with the credentials:** Note the error shown on the login page.
    **If portal loads but project is missing:** Note which page (dashboard or /portal/projects) and what's shown.

  </how-to-verify>
  <resume-signal>Type "approved" if all 9 steps pass, or describe exactly which step failed and what was shown</resume-signal>
</task>

</tasks>

<verification>
Complete when checkpoint is approved with all 9 steps passing.
Fallback: if any step fails, create a gap closure task targeting the exact failure point before marking this plan done.
</verification>

<success_criteria>

- New client email → credentials dialog shown with all 3 fields
- Credentials copy works (all 3 fields in clipboard)
- Client logs in with those credentials at /auth/login → lands on /portal
- Client sees their assigned project on the portal dashboard
- All portal sub-pages (/roadmap, /files, /updates) load without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-fixes/20-02-SUMMARY.md`
</output>
