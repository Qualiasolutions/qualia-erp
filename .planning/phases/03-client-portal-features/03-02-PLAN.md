---
phase: 03-client-portal-features
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - app/actions/project-files.ts
  - types/database.ts
  - app/projects/[id]/files/page.tsx
  - app/portal/[id]/files/page.tsx
  - components/project-files/file-upload-form.tsx
  - components/project-files/file-list.tsx
  - components/portal/portal-file-list.tsx
autonomous: true

must_haves:
  truths:
    - 'Admin can upload files to a project with is_client_visible toggle'
    - 'Client sees client-visible files at /portal/[id]/files with download buttons'
    - 'Files show phase association, description, upload date'
    - 'Internal-only files are hidden from clients'
    - 'project_files table has phase_id, description, is_client_visible columns'
  artifacts:
    - path: 'app/actions/project-files.ts'
      provides: 'Updated server actions with visibility control, phase association, and client filtering'
      exports: ['uploadProjectFile', 'getProjectFiles', 'deleteProjectFile', 'getFileDownloadUrl']
      min_lines: 200
    - path: 'app/projects/[id]/files/page.tsx'
      provides: 'Admin file management page with upload and visibility toggle'
      contains: 'FileUploadForm'
      min_lines: 60
    - path: 'app/portal/[id]/files/page.tsx'
      provides: 'Client file viewing page (client-visible only)'
      contains: 'PortalFileList'
      min_lines: 50
    - path: 'components/project-files/file-upload-form.tsx'
      provides: 'File upload form with visibility toggle and phase selection'
      min_lines: 150
    - path: 'components/project-files/file-list.tsx'
      provides: 'Admin file list showing all files with edit/delete'
      min_lines: 100
    - path: 'components/portal/portal-file-list.tsx'
      provides: 'Client file list showing only client-visible files'
      min_lines: 80
  key_links:
    - from: 'app/projects/[id]/files/page.tsx'
      to: 'app/actions/project-files.ts'
      via: 'server actions'
      pattern: 'uploadProjectFile|getProjectFiles'
    - from: 'app/portal/[id]/files/page.tsx'
      to: 'app/actions/project-files.ts'
      via: 'server actions'
      pattern: 'getProjectFiles.*clientVisibleOnly'
    - from: 'components/project-files/file-upload-form.tsx'
      to: 'project_files table'
      via: 'uploadProjectFile action'
      pattern: 'is_client_visible'
---

<objective>
Add visibility control, description, and phase association to the existing project files system. Then build admin upload UI and client viewing UI.

Purpose: Share deliverables, designs, and documentation with clients while keeping internal files private.

Output: Updated project_files schema with new columns, enhanced server actions with client filtering, admin file management UI at /projects/[id]/files, client file viewing UI at /portal/[id]/files.

IMPORTANT: The project_files table and server actions (uploadProjectFile, getProjectFiles, deleteProjectFile, getFileDownloadUrl) already exist. This plan UPDATES them — not creates from scratch.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Database schema

@types/database.ts

# EXISTING file handling actions (MUST READ BEFORE MODIFYING)

@app/actions/project-files.ts

# Existing shared helpers

@app/actions/shared.ts
@lib/supabase/server.ts

# UI components

@components/ui/button.tsx
@components/ui/input.tsx
@components/ui/switch.tsx
@components/ui/select.tsx
@components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Add columns to project_files table via migration</name>
  <files>types/database.ts</files>
  <action>
Run a Supabase migration to add 3 new columns to the existing `project_files` table:

1. Run SQL via Supabase MCP or CLI:

   ```sql
   ALTER TABLE project_files
     ADD COLUMN IF NOT EXISTS description text,
     ADD COLUMN IF NOT EXISTS phase_id uuid REFERENCES project_phases(id) ON DELETE SET NULL,
     ADD COLUMN IF NOT EXISTS is_client_visible boolean NOT NULL DEFAULT false;
   ```

2. After migration, update `types/database.ts`:
   - Find the `ProjectFile` interface (line ~1546)
   - Add the 3 new fields:
     ```typescript
     description: string | null;
     phase_id: string | null;
     is_client_visible: boolean;
     ```
   - Also add optional joined data for phase:
     ```typescript
     phase?: { id: string; phase_name: string } | null;
     ```

3. If there's a generated types section in database.ts for project_files, update that too.
   Note: project_files is NOT in the auto-generated types section — only the manual `ProjectFile` interface exists.

4. Verify migration applied: query project_files to confirm new columns exist.

CRITICAL: Do NOT drop or recreate the table. Only ALTER to add columns.
</action>
<verify>
Run SQL: SELECT column_name FROM information_schema.columns WHERE table_name = 'project_files' AND column_name IN ('description', 'phase_id', 'is_client_visible');
Should return 3 rows.
grep "is_client_visible" types/database.ts returns field in ProjectFile interface.
grep "phase_id" types/database.ts returns field in ProjectFile interface.
grep "description" types/database.ts returns field in ProjectFile interface (in ProjectFile section).
</verify>
<done>
project_files table has description, phase_id, and is_client_visible columns; TypeScript types updated
</done>
</task>

<task type="auto">
  <name>Update existing project files server actions</name>
  <files>app/actions/project-files.ts</files>
  <action>
MODIFY the existing server actions in app/actions/project-files.ts. READ THE FILE FIRST — it already has uploadProjectFile, getProjectFiles, deleteProjectFile, getFileDownloadUrl.

Changes needed:

1. **Update uploadProjectFile(formData: FormData)**:
   - Extract 3 NEW fields from formData:
     - `description` (string, optional)
     - `phase_id` (string, optional)
     - `is_client_visible` (string → boolean, default false)
   - Add them to the insert object:
     ```typescript
     .insert({
       ...existing fields...,
       description: description || null,
       phase_id: phaseId || null,
       is_client_visible: isClientVisible === 'true',
     })
     ```
   - Add revalidatePath for portal: `revalidatePath(`/portal/${projectId}/files`)`
   - Keep existing authorization (canAccessProject), file validation, storage upload logic UNCHANGED

2. **Update getProjectFiles(projectId: string, clientVisibleOnly = false)**:
   - Add `clientVisibleOnly` parameter (default false)
   - Add the new columns to the select query:
     ```typescript
     .select(`
       id, project_id, workspace_id, name, original_name, storage_path,
       file_size, mime_type, uploaded_by, created_at, updated_at,
       description, phase_id, is_client_visible,
       uploader:profiles!project_files_uploaded_by_fkey (id, full_name, email, avatar_url),
       phase:project_phases!project_files_phase_id_fkey (id, phase_name)
     `)
     ```
   - If clientVisibleOnly = true: add `.eq('is_client_visible', true)`
   - Normalize FK for phase join too:
     ```typescript
     phase: Array.isArray(file.phase) ? file.phase[0] || null : file.phase,
     ```
   - Keep existing authorization logic UNCHANGED

3. **Update getFileDownloadUrl(fileId: string)**:
   - Add client authorization path:
     - After getting file record, also select `is_client_visible` and `project_id`
     - If user is NOT a workspace member, check if:
       - User has a client_projects entry for this file's project_id
       - AND file is_client_visible = true
     - If both true: allow download
     - Otherwise: deny access
   - Keep existing workspace member check as primary auth path

4. Keep deleteProjectFile UNCHANGED (already works correctly with admin auth)

PRESERVE all existing logic — authorization, error handling, storage operations. Only ADD the new fields and filtering.
</action>
<verify>
grep "is_client_visible" app/actions/project-files.ts returns field in insert and select
grep "clientVisibleOnly" app/actions/project-files.ts returns parameter
grep "phase_id" app/actions/project-files.ts returns field in insert and select
grep "description" app/actions/project-files.ts returns field in insert
grep "client_projects" app/actions/project-files.ts returns client auth check in download
Run `npx tsc --noEmit` - no errors
</verify>
<done>
Existing server actions updated with visibility control, phase association, description, and client download authorization
</done>
</task>

<task type="auto">
  <name>Create file upload form component</name>
  <files>components/project-files/file-upload-form.tsx</files>
  <action>
Create client component for uploading files:

1. Props:
   - projectId: string
   - phases: { id: string; phase_name: string }[] (for phase selection dropdown)
   - onUploadComplete?: () => void

2. Form fields:
   - File input (accept all types, max 50MB — matching existing MAX_FILE_SIZE)
   - Description textarea (optional)
   - Phase selection dropdown (optional, "No phase" as default)
   - "Share with client" switch (default: false)
   - Upload button with loading state

3. File input handling:
   - Use Input type="file" from shadcn/ui
   - Show selected filename
   - Validate size < 50MB client-side (match server limit)
   - Show error if too large

4. Form submission:
   - Create FormData with all fields including description, phase_id, is_client_visible
   - Call uploadProjectFile action (import from '@/app/actions/project-files')
   - Show loading spinner on button
   - Show success toast with filename
   - Clear form after success
   - Call onUploadComplete callback if provided

5. Visibility toggle:
   - Use Switch component from shadcn/ui
   - Label: "Share with client"
   - Helper text: "Client will see this file in their portal"
   - Default: unchecked (internal only)

6. Phase selection:
   - Select dropdown with phase names
   - First option: "No phase" (value: empty string)
   - Map phases to options

7. Styling:
   - Use Card component for form container
   - Form layout: vertical stack with gap-4
   - Submit button: full width on mobile, auto on desktop

Component uses "use client", imports from shadcn/ui and lucide-react (Upload).
</action>
<verify>
File exists at components/project-files/file-upload-form.tsx
grep "uploadProjectFile" components/project-files/file-upload-form.tsx returns action call
grep "is_client_visible" components/project-files/file-upload-form.tsx returns field
grep "Switch" components/project-files/file-upload-form.tsx returns component
Component validates file size and handles errors
</verify>
<done>
FileUploadForm component allows admins to upload files with description, phase, and visibility control
</done>
</task>

<task type="auto">
  <name>Create admin file list component</name>
  <files>components/project-files/file-list.tsx</files>
  <action>
Create client component for displaying files (admin view):

1. Props:
   - projectId: string
   - files: ProjectFile[] (from types/database.ts)
   - onFileDeleted?: () => void

2. Display:
   - If no files: empty state with icon and text
   - If has files: list/table of files with columns:
     - File icon (based on mime_type)
     - File name (original_name)
     - Description (truncated)
     - Phase name (if phase joined)
     - Visibility badge ("Client visible" or "Internal only")
     - Upload date (relative, e.g., "2 days ago" via date-fns formatDistanceToNow)
     - Uploaded by (uploader.full_name)
     - Actions: Download, Delete

3. Visibility badge:
   - If is_client_visible: Badge variant="outline" color green, Eye icon
   - If internal: Badge variant="secondary" color gray, EyeOff icon

4. File actions:
   - Download: calls getFileDownloadUrl, opens URL in new tab
   - Delete: shows confirmation, calls deleteProjectFile
   - Delete only for admins

5. Styling:
   - Use Table component from shadcn/ui
   - Responsive: stack on mobile, table on desktop
   - File rows: hover effect
   - Action buttons: ghost variant, small size

Component uses "use client", imports from shadcn/ui and lucide-react.
</action>
<verify>
File exists at components/project-files/file-list.tsx
grep "getFileDownloadUrl" components/project-files/file-list.tsx returns action call
grep "deleteProjectFile" components/project-files/file-list.tsx returns action call
grep "is_client_visible" components/project-files/file-list.tsx returns conditional rendering
Component shows visibility badges
</verify>
<done>
FileList component displays all files with visibility indicators and admin actions
</done>
</task>

<task type="auto">
  <name>Create client file list component</name>
  <files>components/portal/portal-file-list.tsx</files>
  <action>
Create client component for displaying files (client view):

1. Props:
   - files: ProjectFile[] (pre-filtered to client-visible only)

2. Display:
   - If no files: empty state "No files shared yet"
   - If has files: card grid of files with:
     - File icon (based on mime_type)
     - File name (original_name)
     - Description (full text)
     - Phase name badge (if phase joined)
     - Upload date (relative via date-fns)
     - Download button

3. File cards:
   - Use Card component from shadcn/ui
   - Layout: icon + metadata + download button
   - Grid: 1 col on mobile, 2 cols on tablet, 3 cols on desktop
   - Hover effect: subtle lift

4. Download:
   - Primary button with Download icon
   - Calls getFileDownloadUrl action
   - Opens signed URL in new tab
   - Shows loading state during URL generation

5. Styling:
   - Clean, client-friendly design
   - No delete or edit actions (read-only)
   - Prominent download buttons

Component uses "use client", imports from shadcn/ui and lucide-react (Download).
</action>
<verify>
File exists at components/portal/portal-file-list.tsx
grep "getFileDownloadUrl" components/portal/portal-file-list.tsx returns action call
Component is read-only (no delete/edit actions)
Shows phase badges and file metadata
</verify>
<done>
PortalFileList component displays client-visible files with download capability
</done>
</task>

<task type="auto">
  <name>Create admin files page</name>
  <files>app/projects/[id]/files/page.tsx</files>
  <action>
Create server component for admin file management:

1. Fetch project phases for upload form:

   ```typescript
   const { data: phases } = await supabase
     .from('project_phases')
     .select('id, phase_name, status')
     .eq('project_id', params.id)
     .order('order_index');
   ```

2. Fetch all project files (admin sees all, clientVisibleOnly=false):

   ```typescript
   const files = await getProjectFiles(params.id, false);
   ```

3. Check user role for admin/employee-only access:
   - Query session and profile.role
   - If not admin/employee: redirect to /projects/[id]

4. Page layout:
   - Page title: "Project Files"
   - FileUploadForm component (top section)
   - FileList component (below upload form)
   - Pass projectId, phases to upload form
   - Pass projectId, files to file list

5. Keep consistent with existing project detail page patterns.
   </action>
   <verify>
   File exists at app/projects/[id]/files/page.tsx
   grep "FileUploadForm" app/projects/[id]/files/page.tsx returns component
   grep "FileList" app/projects/[id]/files/page.tsx returns component
   grep "getProjectFiles" app/projects/[id]/files/page.tsx returns action call
   Page checks user role for authorization
   </verify>
   <done>
   Admin files page at /projects/[id]/files shows upload form and all files
   </done>
   </task>

<task type="auto">
  <name>Create client files page</name>
  <files>app/portal/[id]/files/page.tsx</files>
  <action>
Create server component for client file viewing:

1. Check client access using existing portal auth pattern:

   ```typescript
   // Follow same pattern as app/portal/[id]/page.tsx for auth
   const {
     data: { user },
   } = await supabase.auth.getUser();
   // Verify user has client_projects access to this project
   ```

2. Fetch client-visible files only:

   ```typescript
   const files = await getProjectFiles(params.id, true); // clientVisibleOnly=true
   ```

3. Page layout:
   - Use portal layout (no admin sidebar)
   - Page title: "Shared Files"
   - PortalFileList component
   - Pass files array

4. Empty state messaging:
   - If no files: "No files have been shared yet. Your project manager will upload files here when available."

Keep consistent with portal design patterns from Phase 2.
</action>
<verify>
File exists at app/portal/[id]/files/page.tsx
grep "getProjectFiles.\*true" app/portal/[id]/files/page.tsx returns client-visible filter
grep "PortalFileList" app/portal/[id]/files/page.tsx returns component
Portal layout used (no admin sidebar)
</verify>
<done>
Client files page at /portal/[id]/files shows only client-visible files with download capability
</done>
</task>

</tasks>

<verification>
**Manual Testing (Admin):**
1. Login as admin
2. Navigate to /projects/[id]/files
3. Upload a file with:
   - Select file (< 50MB)
   - Add description
   - Select a phase
   - Toggle "Share with client" ON
   - Click upload
4. Verify:
   - Success toast shows
   - File appears in list
   - Visibility badge shows "Client visible"
   - Phase name shows correctly
5. Upload another file with "Share with client" OFF
6. Verify internal file shows "Internal only" badge
7. Click download on a file
8. Verify file downloads successfully
9. Click delete on a file
10. Confirm deletion
11. Verify file removed from list

**Manual Testing (Client):**

1. Login as client user
2. Navigate to /portal/[assigned-project-id]/files
3. Verify:
   - Only client-visible files show
   - Internal files are hidden
   - Download buttons work
   - No delete/edit actions visible
4. Try accessing /portal/[unassigned-project-id]/files
5. Verify redirect to /portal (access denied)

**Code Verification:**

- Run `npx tsc --noEmit` - no errors
- File size validation works (50MB limit)
- Visibility filtering works correctly
- Authorization checks prevent unauthorized access
- Existing file upload/download still works (backward compatible)
  </verification>

<success_criteria>

1. project_files table has description, phase_id, is_client_visible columns ✓
2. Admin can upload files with is_client_visible toggle ✓
3. Files show description, phase, upload date, uploaded by ✓
4. Admin can delete files ✓
5. Client sees only client-visible files at /portal/[id]/files ✓
6. Client can download files via signed URLs ✓
7. Internal-only files are hidden from clients ✓
8. Phase association works correctly ✓
9. Authorization prevents clients from accessing internal files ✓
10. Existing file functionality not broken (backward compatible) ✓
11. TypeScript compilation passes ✓
    </success_criteria>

<output>
After completion, create `.planning/phases/03-client-portal-features/03-02-SUMMARY.md`
</output>
