---
phase: 03-client-portal-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/clients/[id]/page.tsx
  - components/clients/client-project-access.tsx
autonomous: true

must_haves:
  truths:
    - 'Admin can invite a client to a project via UI in client detail page'
    - 'Admin can remove client project access via UI'
    - 'Client project access section shows assigned projects with remove buttons'
    - 'Add project dropdown shows available projects to assign'
  artifacts:
    - path: 'components/clients/client-project-access.tsx'
      provides: 'UI component for managing client project assignments'
      min_lines: 150
      exports: ['ClientProjectAccess']
    - path: 'app/clients/[id]/page.tsx'
      provides: 'Client detail page with project access section'
      contains: 'ClientProjectAccess'
  key_links:
    - from: 'app/clients/[id]/page.tsx'
      to: 'components/clients/client-project-access.tsx'
      via: 'component import'
      pattern: 'ClientProjectAccess'
    - from: 'components/clients/client-project-access.tsx'
      to: 'app/actions/client-portal.ts'
      via: 'server actions'
      pattern: 'inviteClientToProject|removeClientFromProject'
---

<objective>
Wire the existing inviteClientToProject and removeClientFromProject server actions to an admin UI component, closing the gap from Phase 2 verification.

Purpose: Enable admins to assign clients to projects and manage access through the interface instead of manual database updates.

Output: Client detail page with project access section showing assigned projects and ability to add/remove access.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-portal-core/02-VERIFICATION.md

# Phase 2 context

@.planning/phases/02-client-portal-core/02-01-SUMMARY.md
@.planning/phases/02-client-portal-core/02-02-SUMMARY.md

# Existing actions to wire

@app/actions/client-portal.ts

# Key files to reference

@app/clients/[id]/page.tsx
@components/ui/select.tsx
@components/ui/button.tsx
@types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Create ClientProjectAccess component</name>
  <files>components/clients/client-project-access.tsx</files>
  <action>
Create client component for managing project assignments:

1. Props:
   - clientId: string
   - initialProjects: Project[] (projects client already has access to)
   - availableProjects: Project[] (projects client doesn't have access to yet)
   - isAdmin: boolean

2. State management:
   - assignedProjects: local state initialized from initialProjects
   - selectedProjectId: string for dropdown selection
   - isPending: useTransition for optimistic updates

3. Display section "Project Access":
   - If no assigned projects: empty state "No projects assigned yet"
   - If has projects: list of assigned projects with:
     - Project name
     - Project status badge (from PROJECT_STATUSES constant)
     - "Remove Access" button (Trash icon from lucide-react)
   - Use Card component from shadcn/ui for each project
   - Grid layout: grid-cols-1 md:grid-cols-2 gap-4

4. Add project section (admin only):
   - Select dropdown with available projects
   - "Add Access" button (Plus icon)
   - On click: call inviteClientToProject(selectedProjectId, clientId)
   - Optimistically update assignedProjects array
   - Show success toast
   - Clear selection

5. Remove access:
   - On click: call removeClientFromProject(projectId, clientId)
   - Optimistically remove from assignedProjects array
   - Show success toast

6. Error handling:
   - Display error toast if action fails
   - Revert optimistic update on error

7. Styling:
   - Section title: text-lg font-semibold mb-4
   - Empty state: text-muted-foreground text-center py-8
   - Project cards: border rounded-lg p-4
   - Remove button: variant="ghost" size="sm" text-destructive

Component uses "use client" directive, imports from shadcn/ui and lucide-react.
</action>
<verify>
File exists at components/clients/client-project-access.tsx
grep "inviteClientToProject" components/clients/client-project-access.tsx returns action call
grep "removeClientFromProject" components/clients/client-project-access.tsx returns action call
grep "useTransition" components/clients/client-project-access.tsx returns hook usage
Component handles optimistic updates and error states
</verify>
<done>
ClientProjectAccess component renders assigned projects with add/remove capability for admins
</done>
</task>

<task type="auto">
  <name>Wire ClientProjectAccess into client detail page</name>
  <files>app/clients/[id]/page.tsx</files>
  <action>
Add ClientProjectAccess component to client detail page:

1. Import ClientProjectAccess component
2. Import inviteClientToProject, removeClientFromProject from actions/client-portal
3. Fetch client's assigned projects (query client_projects joined with projects):
   ```typescript
   const { data: assignedProjects } = await supabase
     .from('client_projects')
     .select('projects(*)')
     .eq('client_id', params.id);
   ```
4. Fetch all active projects for dropdown:
   ```typescript
   const { data: allProjects } = await supabase
     .from('projects')
     .select('id, name, project_status')
     .eq('project_status', 'Active')
     .order('name');
   ```
5. Filter availableProjects (exclude already assigned):
   ```typescript
   const assignedIds = new Set(assignedProjects.map((ap) => ap.project_id));
   const availableProjects = allProjects.filter((p) => !assignedIds.has(p.id));
   ```
6. Check if current user is admin (query session + profile.role)
7. Add ClientProjectAccess to page layout:
   - Position: After client details section, before activities/notes
   - Pass props: clientId, initialProjects (normalized), availableProjects, isAdmin
8. Normalize FK response for assigned projects using normalizeFKResponse pattern

Keep existing client detail page structure intact - this is an additive change.
</action>
<verify>
grep "ClientProjectAccess" app/clients/[id]/page.tsx returns component usage
Page fetches assignedProjects and availableProjects from Supabase
Component receives all required props including isAdmin check
FK normalization applied to client_projects join
</verify>
<done>
Client detail page displays project access section with add/remove capability for admins
</done>
</task>

</tasks>

<verification>
**Manual Testing:**
1. Login as admin
2. Navigate to /clients
3. Click on a client
4. Verify "Project Access" section appears
5. If client has no projects:
   - Empty state should show
6. Select a project from dropdown
7. Click "Add Access"
8. Verify:
   - Project appears in assigned list
   - Success toast shows
   - Dropdown updates (removes assigned project)
9. Click "Remove Access" on a project
10. Verify:
    - Project disappears from list
    - Success toast shows
    - Project reappears in dropdown
11. Refresh page
12. Verify changes persisted in database

**Code Verification:**

- Run `npx tsc --noEmit` - no errors
- Component follows shadcn/ui patterns
- Optimistic updates implemented
- Error states handled with toast
  </verification>

<success_criteria>

1. Admin sees "Project Access" section in client detail page ✓
2. Section shows all projects client has access to ✓
3. Admin can add project access via dropdown + button ✓
4. Admin can remove project access via remove button ✓
5. Changes persist to client_projects table ✓
6. Optimistic updates work smoothly ✓
7. Error states show toast notifications ✓
8. Empty state shown when client has no projects ✓
9. TypeScript compilation passes ✓
   </success_criteria>

<output>
After completion, create `.planning/phases/03-client-portal-features/03-01-SUMMARY.md`
</output>
