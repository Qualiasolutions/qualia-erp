---
phase: 14-unified-notification-system
plan: 02
type: execute
wave: 2
depends_on: ['14-01']
files_modified:
  - lib/email.ts
  - app/actions/phase-comments.ts
  - app/actions/project-files.ts
autonomous: true

must_haves:
  truths:
    - 'Employee receives email when assigned client comments on project'
    - 'Employee receives email when assigned client uploads file'
    - 'Employee receives email when assigned client takes tracked action'
    - "Emails respect user's notification preferences"
  artifacts:
    - path: 'lib/email.ts'
      provides: 'Email templates for client action notifications'
      exports:
        [
          'notifyEmployeesOfClientComment',
          'notifyEmployeesOfClientFileUpload',
          'notifyEmployeesOfClientActivity',
        ]
      min_lines: 900
    - path: 'app/actions/phase-comments.ts'
      provides: 'Email notifications wired into comment creation'
      contains: 'notifyEmployeesOfClientComment'
    - path: 'app/actions/project-files.ts'
      provides: 'Email notifications wired into file upload'
      contains: 'notifyEmployeesOfClientFileUpload'
  key_links:
    - from: 'lib/email.ts notification functions'
      to: 'shouldSendEmail helper'
      via: 'Preference check before sending'
      pattern: 'shouldSendEmail'
    - from: 'app/actions/phase-comments.ts'
      to: 'lib/email.ts notifyEmployeesOfClientComment'
      via: 'Import and call after createPhaseComment mutation success'
      pattern: 'notifyEmployeesOfClientComment'
    - from: 'app/actions/project-files.ts'
      to: 'lib/email.ts notifyEmployeesOfClientFileUpload'
      via: 'Import and call after uploadProjectFile mutation success'
      pattern: 'notifyEmployeesOfClientFileUpload'
    - from: 'Email notification functions'
      to: 'project_assignments table'
      via: 'getProjectAssignedEmployees'
      pattern: "getProjectAssignedEmployees\\(projectId\\)"
---

<objective>
Wire email notifications for client actions (comments, file uploads, general activities) that notify assigned employees while respecting notification preferences.

Purpose: Close communication gap between portal clients and ERP employees
Output: Email templates, preference-aware notification routing, mutation wiring
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/qualia/Projects/live/qualia/.planning/PROJECT.md
@/home/qualia/Projects/live/qualia/.planning/ROADMAP.md
@/home/qualia/Projects/live/qualia/.planning/STATE.md
@/home/qualia/Projects/live/qualia/lib/email.ts
@/home/qualia/Projects/live/qualia/lib/notifications.ts
@/home/qualia/Projects/live/qualia/app/actions/phase-comments.ts
@/home/qualia/Projects/live/qualia/app/actions/project-files.ts
@/home/qualia/Projects/live/qualia/.planning/phases/14-unified-notification-system/14-01-SUMMARY.md
@~/.claude/skills/supabase/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create email templates for client action notifications</name>
  <files>
    lib/email.ts
    app/actions/notification-preferences.ts
  </files>
  <action>
Add three new notification functions to lib/email.ts following existing patterns:

**1. notifyEmployeesOfClientComment(projectId: string, clientName: string, commentText: string): Promise&lt;void&gt;**

Implementation:

- Get assigned employees via getProjectAssignedEmployees(projectId) from lib/notifications.ts
- Fetch project name from projects table
- For each employee:
  - Check shouldSendEmail(employeeId, 'client_comment')
  - If true, send email with:
    - Subject: `${clientName} commented on ${projectName}`
    - Body: HTML template with purple gradient header (Qualia branding)
    - Comment preview (truncate to 300 chars if needed)
    - CTA button: "View Comment" → ${APP_URL}/projects/${projectId}
    - Footer: "You're receiving this because you're assigned to this project. Manage preferences in Settings."
- Silent failure pattern: log error but return success (don't block mutations)

HTML template structure (match existing notifyPhaseSubmitted style):

```html
<div
  style="background: linear-gradient(135deg, #00A4AC 0%, #008C94 100%); padding: 24px; border-radius: 12px 12px 0 0;"
>
  <h1 style="color: white;">New Comment from Client</h1>
</div>
<div style="background: #ffffff; padding: 32px; border: 1px solid #e5e7eb;">
  <p>Hi {employeeName},</p>
  <p><strong>{clientName}</strong> left a comment on <strong>{projectName}</strong>:</p>
  <blockquote
    style="background: #f9fafb; border-left: 4px solid #00A4AC; padding: 16px; margin: 24px 0;"
  >
    {commentText}
  </blockquote>
  <a
    href="{viewUrl}"
    style="display: inline-block; background: #00A4AC; color: white; padding: 12px 24px; border-radius: 8px;"
    >View Comment</a
  >
  <hr style="margin: 32px 0; border-top: 1px solid #e5e7eb;" />
  <p style="color: #9ca3af; font-size: 12px;">Manage notification preferences in Settings.</p>
</div>
```

**2. notifyEmployeesOfClientFileUpload(projectId: string, clientName: string, fileName: string, fileDescription?: string): Promise&lt;void&gt;**

Implementation:

- Same structure as notifyEmployeesOfClientComment
- Check shouldSendEmail(employeeId, 'client_file_upload')
- Subject: `${clientName} uploaded a file to ${projectName}`
- Body: Show file name, optional description
- CTA: "View File" → ${APP_URL}/projects/${projectId}/files
- Icon/styling: Use file icon color (#3b82f6 blue)

**3. notifyEmployeesOfClientActivity(projectId: string, clientName: string, activityType: string, activityDetails: string): Promise&lt;void&gt;**

Implementation:

- Generic notification for other client actions
- Check shouldSendEmail(employeeId, 'client_activity')
- Subject: `${clientName} ${activityType} on ${projectName}`
- Body: Generic activity description
- CTA: "View Project" → ${APP_URL}/projects/${projectId}
- Use neutral gray gradient

Pattern: Follow existing email functions (notifyPhaseSubmitted, notifyPhaseApproved) for HTML structure, silent failure, and Resend client usage
</action>
<verify>
Functions exist: grep "export async function notifyEmployeesOf" lib/email.ts | wc -l (should be 3)
Import shouldSendEmail: grep "import.\*shouldSendEmail" lib/email.ts
TypeScript compiles: npx tsc --noEmit lib/email.ts
</verify>
<done>
Three email notification functions exist with preference checks, HTML templates use Qualia branding, silent failure pattern implemented
</done>
</task>

<task type="auto">
  <name>Task 2: Wire notifications into phase comments and file upload actions</name>
  <files>
    app/actions/phase-comments.ts
    app/actions/project-files.ts
  </files>
  <action>
Wire email notifications into existing client portal mutations:

**1. In app/actions/phase-comments.ts:**

Locate the `createPhaseComment` function (line 20).
After successful comment insertion but before return:

```typescript
// Notify assigned employees
const { data: author } = await supabase
  .from('profiles')
  .select('full_name')
  .eq('id', userId)
  .single();

await notifyEmployeesOfClientComment(projectId, author?.full_name || 'A client', commentText);
```

Import at top:

```typescript
import { notifyEmployeesOfClientComment } from '@/lib/email';
```

**2. In app/actions/project-files.ts:**

Locate the `uploadProjectFile` function (line 129).
After successful file insertion but before return:

```typescript
// Notify assigned employees
const { data: uploader } = await supabase
  .from('profiles')
  .select('full_name')
  .eq('id', userId)
  .single();

await notifyEmployeesOfClientFileUpload(
  projectId,
  uploader?.full_name || 'A client',
  fileName,
  fileDescription
);
```

Import at top:

```typescript
import { notifyEmployeesOfClientFileUpload } from '@/lib/email';
```

**3. Generic activity notifications:**

Add notifyEmployeesOfClientActivity calls to other client-facing mutations if they exist:

- Portal request submissions
- Support ticket creation
- Any other tracked client actions

Pattern: Always call notification functions AFTER successful mutation but BEFORE final return statement. Use silent failure (don't await or check result) to avoid blocking user actions.

  </action>
  <verify>
Imports added: grep "notifyEmployeesOf" app/actions/phase-comments.ts app/actions/project-files.ts
Comment action wired: grep -A5 "createPhaseComment" app/actions/phase-comments.ts | grep "notifyEmployeesOfClientComment"
File action wired: grep -A5 "uploadProjectFile" app/actions/project-files.ts | grep "notifyEmployeesOfClientFileUpload"
TypeScript compiles: npx tsc --noEmit app/actions/phase-comments.ts app/actions/project-files.ts
  </verify>
  <done>
Email notifications wired into comment and file upload mutations (in phase-comments.ts and project-files.ts), notifications fire after successful DB operations, imports added to action files
  </done>
</task>

</tasks>

<verification>
1. Email templates follow existing Qualia branding and HTML structure
2. shouldSendEmail helper is called before sending each email
3. Notifications use project_assignments table for employee routing
4. Silent failure pattern prevents notification errors from blocking mutations
5. All three notification functions compile without TypeScript errors
</verification>

<success_criteria>

- Three email notification functions exist in lib/email.ts
- Notification preferences checked via shouldSendEmail before sending
- Comment creation in phase-comments.ts triggers notifyEmployeesOfClientComment
- File upload in project-files.ts triggers notifyEmployeesOfClientFileUpload
- HTML templates use Qualia teal branding (#00A4AC)
- Notifications route to assigned employees via project_assignments
- Silent failure prevents blocking user actions
  </success_criteria>

<output>
After completion, create `.planning/phases/14-unified-notification-system/14-02-SUMMARY.md`
</output>
