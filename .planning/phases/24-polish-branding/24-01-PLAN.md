---
phase: 24-polish-branding
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - components/portal/portal-sidebar.tsx
  - app/portal/layout.tsx
  - components/login-form.tsx
autonomous: true

must_haves:
  truths:
    - "Portal sidebar logo area shows 'Qualia Solutions' as the brand name (not 'Qualia')"
    - "Portal layout exports metadata with title 'Client Portal | Qualia Solutions'"
    - "Login page client portal button description reads 'Your project hub, powered by Qualia Solutions'"
    - "Browser tab on any portal page reads 'Client Portal | Qualia Solutions'"
  artifacts:
    - path: 'components/portal/portal-sidebar.tsx'
      provides: 'Updated brand name in SidebarContent logo area'
      contains: 'Qualia Solutions'
    - path: 'app/portal/layout.tsx'
      provides: 'Portal-specific metadata export'
      contains: 'export const metadata'
    - path: 'components/login-form.tsx'
      provides: 'Updated client portal card description'
      contains: 'Qualia Solutions'
  key_links:
    - from: 'app/portal/layout.tsx'
      to: 'Next.js metadata'
      via: 'export const metadata'
      pattern: 'export const metadata'
    - from: 'components/portal/portal-sidebar.tsx'
      to: 'brand text'
      via: 'SidebarContent logo span'
      pattern: 'Qualia Solutions'
---

<objective>
Rebrand the client portal from generic "Qualia" to "Qualia Solutions Client Portal" — update sidebar brand name, add portal-specific metadata for proper browser tab titles, and update the login page client portal card.

Purpose: Clients see a professional, distinctly branded experience that communicates "dedicated client portal" rather than "internal tool we opened up".
Output: Branded sidebar header, portal-specific page titles, updated login card.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/portal/portal-sidebar.tsx
@app/portal/layout.tsx
@components/login-form.tsx
@~/.claude/skills/frontend-master/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand portal sidebar header and add portal metadata</name>
  <files>
    components/portal/portal-sidebar.tsx
    app/portal/layout.tsx
  </files>
  <action>
    In `components/portal/portal-sidebar.tsx`, update the SidebarContent logo area (around line 172-178):
    - Change the brand span text from "Qualia" to "Qualia Solutions"
    - Keep the "Q" icon badge with `bg-qualia-600` unchanged

    In `app/portal/layout.tsx`, add a portal-specific metadata export above the default export:
    ```typescript
    import type { Metadata } from 'next';

    export const metadata: Metadata = {
      title: {
        default: 'Client Portal | Qualia Solutions',
        template: '%s | Client Portal',
      },
      description: 'Your project portal, powered by Qualia Solutions.',
    };
    ```
    This scopes all portal page titles to "Client Portal" context, overriding the root layout's "Qualia Internal Suite" metadata for the /portal subtree.

    Do NOT modify root `app/layout.tsx` — that stays as "Qualia Internal Suite" for the ERP app.

  </action>
  <verify>
    Run `npx tsc --noEmit` from the project root to confirm no type errors.
    In the browser, navigate to /portal — browser tab should read "Client Portal | Qualia Solutions".
    Sidebar logo text should show "Qualia Solutions".
  </verify>
  <done>
    Portal sidebar shows "Qualia Solutions" as brand name.
    Any portal page tab reads "Client Portal | Qualia Solutions" (or "{Page} | Client Portal").
    npx tsc --noEmit passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update login-form client portal card with branded copy</name>
  <files>
    components/login-form.tsx
  </files>
  <action>
    In `components/login-form.tsx`, find the client portal button card (around line 77-83):
    - Change the title span text from "Client Portal" to "Qualia Solutions Client Portal"
    - Change the description from "View your projects & updates" to "Your project hub, powered by Qualia Solutions"

    This ensures the login page differentiation is clear — clients see a branded entry point, not a generic "Client Portal" label.

    Do NOT change the mode logic, button structure, or any other part of the form.

  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors.
    Visit /auth/login — the client portal card should show "Qualia Solutions Client Portal" as the title and updated description.
  </verify>
  <done>
    Login page client portal card shows "Qualia Solutions Client Portal" title and updated description.
    npx tsc --noEmit passes.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. `/portal` page tab reads "Client Portal | Qualia Solutions"
3. `/auth/login` — client portal card shows branded copy
4. Portal sidebar shows "Qualia Solutions" in logo area
5. ERP app (non-portal routes) still shows "Qualia Internal Suite" in tab — root metadata unchanged
</verification>

<success_criteria>

- Portal sidebar brand name: "Qualia Solutions" (was "Qualia")
- Portal page titles: "Client Portal | Qualia Solutions" format
- Login card: "Qualia Solutions Client Portal" with updated description
- Root ERP metadata: unchanged ("Qualia Internal Suite")
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/24-polish-branding/24-01-SUMMARY.md`
</output>
