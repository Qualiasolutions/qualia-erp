---
phase: 15-portal-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/portal/portal-sidebar.tsx
  - components/portal/portal-header.tsx
  - components/portal/portal-page-header.tsx
  - components/portal/portal-tabs.tsx
  - components/portal/portal-skeletons.tsx
  - components/portal/portal-projects-list.tsx
  - components/portal/portal-roadmap.tsx
  - components/portal/portal-file-list.tsx
  - components/portal/portal-activity-feed.tsx
  - components/portal/portal-request-list.tsx
  - components/portal/portal-billing-summary.tsx
  - components/portal/portal-invoice-list.tsx
  - app/portal/layout.tsx
  - app/portal/page.tsx
autonomous: true

must_haves:
  truths:
    - Portal typography matches ERP's Geist font hierarchy exactly
    - Portal spacing system uses same px values as ERP components
    - Portal shadows use elevation-1 through elevation-5 system from tailwind.config
    - Portal dark mode colors match ERP's opacity-based approach
    - Visual comparison between portal and ERP shows identical spacing/typography feel
  artifacts:
    - path: 'components/portal/portal-sidebar.tsx'
      provides: 'Updated typography, spacing, and elevation'
      contains: 'font-sans text-sm tracking-tight'
      min_lines: 240
    - path: 'components/portal/portal-header.tsx'
      provides: 'ERP-style header with refined spacing'
      contains: 'shadow-elevation-2'
      min_lines: 20
    - path: 'components/portal/portal-page-header.tsx'
      provides: 'Consistent heading hierarchy'
      contains: 'text-2xl font-semibold tracking-tight'
      min_lines: 15
    - path: 'components/portal/portal-projects-list.tsx'
      provides: 'Card elevation and spacing'
      contains: 'shadow-elevation-2 hover:shadow-elevation-3'
      min_lines: 60
  key_links:
    - from: 'components/portal/*.tsx'
      to: 'tailwind.config.ts elevation system'
      via: 'shadow-elevation-* classes'
      pattern: 'shadow-elevation-[1-5]'
    - from: 'components/portal/*.tsx'
      to: 'lib/color-constants.ts'
      via: 'SEMANTIC_COLORS imports'
      pattern: "SEMANTIC_COLORS\\.(success|warning|error|info|brand)"
---

<objective>
Align portal typography, spacing, and elevation system with ERP's Apple-like design language.

Purpose: Establish visual foundation matching ERP's premium feel — correct font hierarchy, consistent spacing units, and elevation shadows create immediate quality perception.

Output: Portal components use identical typographic scale, spacing system, and shadow elevation as ERP dashboard.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tailwind.config.ts
@lib/color-constants.ts
@components/ui/button.tsx
@app/today-page.tsx
@components/portal/portal-sidebar.tsx
@components/portal/portal-header.tsx
@components/portal/portal-page-header.tsx
</context>

<tasks>

<task type="auto">
  <name>Update portal typography and spacing to match ERP design system</name>
  <files>
    components/portal/portal-sidebar.tsx
    components/portal/portal-header.tsx
    components/portal/portal-page-header.tsx
    components/portal/portal-tabs.tsx
    components/portal/portal-skeletons.tsx
  </files>
  <action>
    Apply ERP's typography hierarchy and spacing system to portal layout components:

    **Typography changes (from tailwind.config and ERP patterns):**
    - Page titles: `text-2xl font-semibold tracking-tight` (ERP standard)
    - Section headings: `text-lg font-semibold tracking-tight`
    - Body text: `text-sm` (default), `text-[13px]` for compact areas
    - Muted text: `text-xs text-muted-foreground/70` for metadata
    - Ensure Geist font family is used (already configured via font-sans)

    **Spacing changes (match ERP patterns from today-page.tsx):**
    - Container padding: `px-4 py-6 md:px-8 md:py-8` (layout.tsx)
    - Section gaps: `space-y-6` for major sections, `space-y-4` for related groups
    - Card padding: `p-4` for compact cards, `p-6` for content-rich cards
    - Icon-text gaps: `gap-2.5` for nav items, `gap-2` for inline icons
    - Button height: `h-10` for primary, `h-9` for secondary, `h-8` for compact

    **Components to update:**
    1. portal-sidebar.tsx — nav links, user menu, logo area spacing
    2. portal-header.tsx — header height, padding, title typography
    3. portal-page-header.tsx — heading hierarchy, description spacing
    4. portal-tabs.tsx — tab height, padding, active state typography
    5. portal-skeletons.tsx — match updated real component spacing

    **Reference ERP components for exact values:**
    - app/today-page.tsx for dashboard spacing patterns
    - components/ui/button.tsx for interaction spacing
    - tailwind.config.ts for font family and tracking values

    **Do NOT change:**
    - Color scheme (keep qualia brand colors) — that's handled separately
    - Animation timings (already correct from Phase 5/10)
    - Component structure or logic

  </action>
  <verify>
    npx tsc --noEmit
    Compare portal sidebar nav item height (should be h-10 like ERP)
    Compare page header text sizes (should be text-2xl font-semibold tracking-tight)
    Check skeleton spacing matches updated real components
  </verify>
  <done>
    Portal layout components use ERP's typography scale (tracking-tight, correct font sizes)
    Portal spacing matches ERP patterns (container padding, section gaps, card padding)
    TypeScript compilation succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Apply elevation shadow system and color refinements to portal cards and surfaces</name>
  <files>
    components/portal/portal-projects-list.tsx
    components/portal/portal-roadmap.tsx
    components/portal/portal-file-list.tsx
    components/portal/portal-activity-feed.tsx
    components/portal/portal-request-list.tsx
    components/portal/portal-billing-summary.tsx
    components/portal/portal-invoice-list.tsx
    app/portal/page.tsx
  </files>
  <action>
    Apply ERP's elevation shadow system and color refinements to portal content components:

    **Elevation system (from tailwind.config.ts):**
    - Flat cards (project cards, request cards): `shadow-elevation-1` base, `hover:shadow-elevation-2` hover
    - Raised surfaces (sidebar, header): `shadow-elevation-2` (already done in layout)
    - Floating elements (dropdowns, popovers): `shadow-elevation-3`
    - Modals/dialogs: `shadow-elevation-4`
    - Critical overlays: `shadow-elevation-5`

    Replace existing shadows:
    - Remove generic `shadow`, `shadow-sm`, `shadow-lg`
    - Replace with appropriate elevation tier based on component hierarchy
    - Add hover elevation transitions: `transition-shadow duration-200 ease-premium`

    **Color refinements:**
    - Border opacity: Use `border-border/40` for subtle dividers (ERP pattern)
    - Background gradients: `bg-gradient-to-b from-card to-card/95` for subtle depth
    - Muted backgrounds: `bg-muted/40` for hover states (not `bg-muted`)
    - Status badges: Import from `lib/color-constants.ts` (PROJECT_STATUS_COLORS, PHASE_STATUS_COLORS) instead of inline colors

    **Components to update:**
    1. portal-projects-list.tsx — project cards elevation, hover states
    2. portal-roadmap.tsx — phase cards elevation, timeline connector colors
    3. portal-file-list.tsx — file rows hover elevation
    4. portal-activity-feed.tsx — timeline item borders, timestamp colors
    5. portal-request-list.tsx — request card elevation and status colors
    6. portal-billing-summary.tsx — summary card elevation
    7. portal-invoice-list.tsx — invoice row hover states
    8. app/portal/page.tsx — dashboard card elevations

    **Hover state pattern (from ERP components):**
    ```tsx
    className="shadow-elevation-1 hover:shadow-elevation-2 transition-shadow duration-200 ease-premium"
    ```

    **Status badge pattern (use color-constants):**
    ```tsx
    import { PROJECT_STATUS_COLORS } from '@/lib/color-constants';
    // Then use: PROJECT_STATUS_COLORS[status]?.bg, .text, .border
    ```

    **Do NOT change:**
    - Component functionality or data fetching
    - Animation keyframes (already correct from Phase 10)
    - Layout structure (only visual refinements)

  </action>
  <verify>
    npx tsc --noEmit
    Grep for legacy shadow classes: grep -r "shadow-sm\|shadow-lg" components/portal/ (should return 0 results)
    Grep for elevation usage: grep -r "shadow-elevation" components/portal/ (should show consistent usage)
    Check that PROJECT_STATUS_COLORS is imported in status badge components
  </verify>
  <done>
    All portal cards use elevation-1 base with elevation-2 hover transitions
    Legacy shadow classes removed and replaced with elevation system
    Status badges use color-constants.ts instead of inline Tailwind classes
    Border and background opacities match ERP's refined approach
    TypeScript compilation succeeds with no errors
  </done>
</task>

</tasks>

<verification>
**Visual comparison (side-by-side ERP dashboard vs portal):**
- Typography scale matches (headings, body, metadata)
- Spacing feels identical (padding, gaps, margins)
- Shadows follow same elevation hierarchy
- Dark mode colors have same opacity levels
- Status badges use consistent color system

**Technical verification:**

- `npx tsc --noEmit` passes
- No legacy shadow classes remain in portal components
- Elevation system used consistently across all cards/surfaces
- color-constants.ts imported for status colors
  </verification>

<success_criteria>
**Measurable completion:**

1. Portal page titles are `text-2xl font-semibold tracking-tight` (same as ERP)
2. Portal cards use `shadow-elevation-1` and `hover:shadow-elevation-2` (not generic shadows)
3. Portal container padding matches ERP: `px-4 py-6 md:px-8 md:py-8`
4. Portal status badges import from `color-constants.ts` (not inline Tailwind)
5. Side-by-side visual comparison shows no discernible typography/spacing difference from ERP
   </success_criteria>

<output>
After completion, create `.planning/phases/15-portal-design-system/15-01-SUMMARY.md`
</output>
