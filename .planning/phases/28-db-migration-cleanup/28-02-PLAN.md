---
phase: 28-db-migration-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - components/task-time-tracker.tsx
  - components/today-dashboard/team-task-card.tsx
autonomous: true

must_haves:
  truths:
    - 'task-time-tracker.tsx file is deleted'
    - 'team-task-card.tsx has no import of TaskTimeTracker'
    - 'team-task-card.tsx has no import of TeamMemberTaskTimeLog'
    - 'TeamTaskCard renders without any timer button, stop button, clock badge, or elapsed display'
    - 'TeamMemberTask type used in team-task-card.tsx no longer has time_log field (removed in 28-01)'
    - 'npx tsc --noEmit passes with zero errors'
    - 'npm run build succeeds'
  artifacts:
    - path: 'components/today-dashboard/team-task-card.tsx'
      provides: 'Task card without timer UI'
      min_lines: 200
  key_links:
    - from: 'components/today-dashboard/team-task-card.tsx'
      to: 'app/actions/team-dashboard.ts'
      via: 'TeamMemberTask import'
      pattern: 'import.*TeamMemberTask.*team-dashboard'
---

<objective>
Remove the TaskTimeTracker component file and strip all timer-related code from team-task-card.tsx — leaving clean task cards with no timer UI, no time_log prop references, and no imports from the deleted time-logs.ts.

Purpose: Completes CLEAN-01. After this plan, the codebase has zero references to the old per-task timer system.
Output: Deleted task-time-tracker.tsx, cleaned team-task-card.tsx, passing tsc and build.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@components/today-dashboard/team-task-card.tsx
@.planning/phases/28-db-migration-cleanup/28-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete TaskTimeTracker component file</name>
  <files>components/task-time-tracker.tsx</files>
  <action>
    Delete `components/task-time-tracker.tsx` entirely. This file imports from `@/app/actions/time-logs` (already deleted in 28-01) and from `@/app/actions/team-dashboard` (TeamMemberTaskTimeLog type, already removed). Keeping it would cause compile errors.

    Run: `rm /home/qualia/Projects/qualia-erp/components/task-time-tracker.tsx`

  </action>
  <verify>
    ls components/task-time-tracker.tsx — should return "No such file"
  </verify>
  <done>task-time-tracker.tsx no longer exists in the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Strip timer UI from team-task-card.tsx and run build verification</name>
  <files>components/today-dashboard/team-task-card.tsx</files>
  <action>
    Edit `components/today-dashboard/team-task-card.tsx`:

    1. **Remove import:** Delete `import { TaskTimeTracker } from '@/components/task-time-tracker';`

    2. **Remove time_log from usage:** The `TeamMemberTask` type (imported from team-dashboard.ts) no longer has `time_log` after 28-01. Find and remove:
       - The entire `{/* Time tracker: interactive for task owner, read-only badge for others */}` comment block
       - The `{isOwner ? ( <TaskTimeTracker ... /> ) : ( <TaskTimeTracker ... readOnly /> )}` JSX (approximately lines 278-287)

    3. **Remove the `isOwner` variable** if it is now only used for the timer block. Check if `isOwner` appears anywhere else in the file — if the only usage was the TaskTimeTracker conditional, remove the declaration:
       ```
       const isOwner = currentUserId != null && currentUserId === task.assignee_id;
       ```
       If `isOwner` is still used elsewhere (e.g., action menu gating), keep it.

    4. The right-side row should now contain only: due date badge + actions dropdown menu. Verify the flex container `<div className="flex shrink-0 items-center gap-2 ...">` still looks correct with just those two elements.

    After editing, run:
    ```bash
    npx tsc --noEmit
    npm run build
    ```

    Fix any remaining TypeScript errors. Common issues to watch for:
    - Any remaining reference to `task.time_log` anywhere in team-task-card.tsx
    - Any other file importing from `@/components/task-time-tracker` (grep first)
    - Any other file importing `TeamMemberTaskTimeLog` from team-dashboard.ts

    Run before editing:
    ```bash
    grep -r "task-time-tracker\|TaskTimeTracker\|TeamMemberTaskTimeLog\|time_log" /home/qualia/Projects/qualia-erp/components/ /home/qualia/Projects/qualia-erp/app/ --include="*.tsx" --include="*.ts" -l
    ```
    Address any hits found outside of team-task-card.tsx.

  </action>
  <verify>
    1. grep "TaskTimeTracker\|task-time-tracker\|time_log\|TeamMemberTaskTimeLog" components/today-dashboard/team-task-card.tsx — 0 matches
    2. grep -r "task-time-tracker\|TaskTimeTracker" components/ app/ --include="*.tsx" --include="*.ts" — 0 matches
    3. npx tsc --noEmit — exits 0
    4. npm run build — exits 0
  </verify>
  <done>
    team-task-card.tsx compiles without errors and contains no timer UI. npx tsc --noEmit and npm run build both pass clean.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "TaskTimeTracker\|task-time-tracker\|time_log\|task_time_logs" components/ app/ lib/ --include="*.tsx" --include="*.ts"` returns 0 matches
2. `npx tsc --noEmit` exits 0 with no output
3. `npm run build` succeeds
</verification>

<success_criteria>

- task-time-tracker.tsx deleted
- team-task-card.tsx imports only what it needs, no timer references
- TypeScript compiles clean
- Build succeeds
- Phase 28 success criteria 2 and 4 satisfied: no timer UI anywhere in task cards, app builds clean
  </success_criteria>

<output>
After completion, create `.planning/phases/28-db-migration-cleanup/28-02-SUMMARY.md`
</output>
