---
phase: 26-team-sync-daily-structure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/project-wizard/project-wizard.tsx
  - components/project-wizard/step-provisioning.tsx
  - lib/integrations/types.ts
autonomous: true

must_haves:
  truths:
    - 'VAPI checkbox option no longer appears in project creation wizard'
    - 'GitHub and Vercel options remain functional'
    - 'TypeScript compiles clean — no IntegrationSelections.vapi references cause errors'
  artifacts:
    - path: 'components/project-wizard/project-wizard.tsx'
      provides: 'Project creation wizard without VAPI'
      contains: 'github'
  key_links:
    - from: 'components/project-wizard/project-wizard.tsx'
      to: 'IntegrationSelections'
      via: 'selectedIntegrations'
      pattern: "selectedIntegrations\\.github"
---

<objective>
Remove VAPI from the project creation wizard (PROV-03).

Purpose: VAPI provisioning is not relevant to most project types and clutters the wizard. GitHub and Vercel auto-provisioning stay. VAPI references are removed from UI, state, and integration selection types.

Output: Project wizard shows only GitHub and Vercel options. No VAPI checkbox, no VAPI provisioning step.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@components/project-wizard/project-wizard.tsx
@components/project-wizard/step-provisioning.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove VAPI from wizard state, UI, and provisioning step</name>
  <files>
    components/project-wizard/project-wizard.tsx
    components/project-wizard/step-provisioning.tsx
    lib/integrations/types.ts
  </files>
  <action>
    **In `components/project-wizard/project-wizard.tsx`:**

    1. Remove `vapi: boolean` from `configuredIntegrations` state initialization and type — change to `{ github: boolean; vercel: boolean }`.
    2. Remove `vapi: false` from `selectedIntegrations` state initialization — change `IntegrationSelections` usage to only include `github` and `vercel`.
    3. Update `hasAnyIntegrations`: remove `|| configuredIntegrations.vapi`.
    4. Update `hasSelectedIntegrations`: remove `|| selectedIntegrations.vapi`.
    5. Remove the reset line `setSelectedIntegrations({ github: false, vercel: false, vapi: false })` — change to `{ github: false, vercel: false }`.
    6. Remove the VAPI checkbox block (around line 567–585): the block starting with `{configuredIntegrations.vapi && (` through its closing `)}`.
    7. In the provisioning summary string array (around line 637): remove `selectedIntegrations.vapi && 'VAPI assistant'` from the array.

    **In `components/project-wizard/step-provisioning.tsx`:**

    1. Change the step ID union type from `'github' | 'vercel' | 'vapi'` to `'github' | 'vercel'`.
    2. Remove the `vapi` entry from the `STEP_CONFIG` object (name: 'VAPI Assistant', ...).
    3. Remove `if (selectedIntegrations.vapi) selectedSteps.push('vapi')` from the steps array builder.
    4. Remove the result handling block for `step.id === 'vapi'`.
    5. Update `handleRetry` parameter type from `'github' | 'vercel' | 'vapi'` to `'github' | 'vercel'`.

    **In `lib/integrations/types.ts`:**
    Read the file first, then remove the `vapi` field from the `IntegrationSelections` type. Keep `VAPIConfig` type if it is used elsewhere (check with grep). If `VAPIConfig` is only used in integrations settings page (not wizard), keep it there.

    Run `npx tsc --noEmit` and fix all remaining type errors.

  </action>
  <verify>
    1. `grep -n "vapi\|VAPI" components/project-wizard/project-wizard.tsx components/project-wizard/step-provisioning.tsx` returns no results
    2. `npx tsc --noEmit` passes with no errors
    3. Open the project wizard (or code-review the JSX) — only GitHub and Vercel checkboxes render in the integrations section
  </verify>
  <done>VAPI removed from wizard. GitHub and Vercel remain. TypeScript is clean.</done>
</task>

</tasks>

<verification>
- No VAPI references in project-wizard.tsx or step-provisioning.tsx
- TypeScript compiles clean
- IntegrationSelections type only has github and vercel
</verification>

<success_criteria>
The project creation wizard no longer shows a VAPI option. GitHub and Vercel provisioning work exactly as before. No TypeScript errors introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/26-team-sync-daily-structure/26-03-SUMMARY.md`
</output>
