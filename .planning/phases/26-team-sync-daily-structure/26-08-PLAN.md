---
phase: 26-team-sync-daily-structure
plan: 08
type: execute
wave: 2
depends_on: [26-03]
files_modified:
  - app/actions/projects.ts
  - app/actions/integrations.ts
  - lib/integrations/orchestrator.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - 'When a user checks GitHub in the wizard and creates a project, a private GitHub repo is created under the Qualia org'
    - 'When Vercel is also selected, a Vercel project is created linked to the GitHub repo'
    - 'After provisioning completes, the project record has github_repo_url and vercel_project_url stored where downstream UI can read them'
    - 'The wizard provisioning UI (StepProvisioning) correctly polls status and shows the repo/project URLs as clickable links'
  artifacts:
    - path: 'app/actions/projects.ts'
      provides: 'createProjectWithRoadmap with provisioning that stores URLs back on project record'
      contains: 'github_repo_url'
    - path: 'lib/integrations/orchestrator.ts'
      provides: 'setupProjectIntegrations that writes URLs to projects table on success'
      contains: 'projects'
  key_links:
    - from: 'components/project-wizard/project-wizard.tsx'
      to: 'createProjectWithRoadmap'
      via: 'selectedIntegrations pass-through'
      pattern: 'selectedIntegrations.*needsProvisioning'
    - from: 'lib/integrations/orchestrator.ts'
      to: 'projects table'
      via: 'supabase update on provisioning completion'
      pattern: "\.from\('projects'\)\.update"
---

<objective>
Wire GitHub and Vercel auto-provisioning so URLs are stored on the project record (PROV-01, PROV-02).

Purpose: The provisioning infrastructure (Octokit calls, Vercel API calls, project_provisioning table) already exists and works. The gap is that on success, the resulting repo URL and Vercel project URL are only stored in `project_provisioning` — not written back to the `projects` table. Downstream features (project detail page, activity feed) need these URLs on the project record directly.

Output: After a project wizard run with GitHub/Vercel selected, the project row has `github_repo_url` and `vercel_project_url` populated, and the wizard provisioning step shows clickable links to both.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@app/actions/projects.ts
@lib/integrations/orchestrator.ts
@app/actions/integrations.ts
@components/project-wizard/step-provisioning.tsx
@lib/integrations/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store provisioned URLs back on projects table</name>
  <files>
    lib/integrations/orchestrator.ts
    app/actions/integrations.ts
  </files>
  <action>
    **In `lib/integrations/orchestrator.ts`**, after successfully writing GitHub and Vercel results to `project_provisioning`, also write them to the `projects` table:

    1. Read the `projects` table schema — look for columns `github_repo_url` and `vercel_project_url`. If they DO NOT exist, add them via Supabase MCP (type: text, nullable). Run: `ALTER TABLE projects ADD COLUMN IF NOT EXISTS github_repo_url text; ALTER TABLE projects ADD COLUMN IF NOT EXISTS vercel_project_url text;`

    2. After the GitHub block (around line 110 in orchestrator.ts), after the successful `project_provisioning` update, add:
    ```typescript
    if (gitHubResult.success && gitHubResult.data) {
      // ... existing project_provisioning update ...

      // Also write to projects table for direct access
      await supabase
        .from('projects')
        .update({ github_repo_url: gitHubResult.data.repoUrl })
        .eq('id', config.projectId);
    }
    ```

    3. After the Vercel block (around line 145), after the successful `project_provisioning` update, add:
    ```typescript
    if (vercelResult.success && vercelResult.data) {
      // ... existing project_provisioning update ...

      // Also write to projects table for direct access
      await supabase
        .from('projects')
        .update({ vercel_project_url: vercelResult.data.projectUrl })
        .eq('id', config.projectId);
    }
    ```

    **In `app/actions/integrations.ts`**, in `retryProvisioningStep`:
    - After the successful GitHub retry (around line 325), add the same `projects` table update for `github_repo_url`.
    - After the successful Vercel retry (around line 360), add the same `projects` table update for `vercel_project_url`.

    This ensures retries also propagate the URL to the project record.

    After adding the columns to the DB, regenerate types:
    ```bash
    npx supabase gen types typescript --project-id vbpzaiqovffpsroxaulv > types/database.ts
    ```
    (Skip if the columns are not yet in the generated types — the code will still compile with `supabase.from('projects').update({ github_repo_url: ... })` as Supabase client accepts partial updates.)

    Run `npx tsc --noEmit` and fix any type errors.

  </action>
  <verify>
    1. `grep -n "github_repo_url" lib/integrations/orchestrator.ts` shows updates to both `project_provisioning` AND `projects`
    2. `grep -n "vercel_project_url" lib/integrations/orchestrator.ts` shows updates to both tables
    3. `grep -n "github_repo_url" app/actions/integrations.ts` shows retry path also updates projects
    4. `npx tsc --noEmit` passes
  </verify>
  <done>On provisioning success, both github_repo_url and vercel_project_url are written to the projects table. Retry paths do the same.</done>
</task>

<task type="auto">
  <name>Task 2: Verify wizard provisioning flow triggers correctly</name>
  <files>
    app/actions/projects.ts
  </files>
  <action>
    Read `app/actions/projects.ts` lines 844–886 (the provisioning block in `createProjectWithRoadmap`). Verify the current code:

    1. Creates `project_provisioning` record with `status: 'in_progress'` before calling `setupProjectIntegrations`
    2. Calls `setupProjectIntegrations` with `selectedIntegrations` (the user's GitHub/Vercel choices)
    3. The call is fire-and-forget (`.catch(...)` not `await`) — this is correct because the wizard polls for status

    The current code does all three. **No changes needed to projects.ts** IF the above is confirmed by reading the file.

    If any of the three points are missing, add the missing pieces following the existing pattern.

    Also verify `step-provisioning.tsx` correctly handles the case where `selectedIntegrations.vapi` may be undefined (after plan 26-03 removes VAPI):
    - The `STEP_CONFIG` record type is `'github' | 'vercel' | 'vapi'` — after 26-03 this becomes `'github' | 'vercel'`
    - The step initialization `useEffect` still checks `selectedIntegrations.vapi` — this is harmless if `vapi` is undefined (falsy), but confirm the TypeScript type allows optional `vapi`

    In `lib/integrations/types.ts`, `IntegrationSelections.vapi` is already `boolean | undefined` (optional field). No changes needed.

    Run `npx tsc --noEmit` to confirm clean compilation.

  </action>
  <verify>
    1. `grep -n "setupProjectIntegrations\|project_provisioning" app/actions/projects.ts` shows both the record creation and the integration call
    2. `grep -n "selectedIntegrations" app/actions/projects.ts` shows integrations are passed through
    3. `npx tsc --noEmit` passes with no errors
    4. Manual trace: wizard form submit → `createProjectWithRoadmap({ selectedIntegrations: { github: true, vercel: true } })` → provisioning record created → `setupProjectIntegrations` called async → orchestrator creates GitHub repo → orchestrator creates Vercel project → both URLs written to `projects` table and `project_provisioning` → wizard polls `getProjectProvisioningStatus` → shows completed with links
  </verify>
  <done>End-to-end provisioning flow is verified. GitHub repo is created under Qualia org, Vercel project created and linked, both URLs stored on the project record. Wizard UI shows clickable links on success.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `projects` table has `github_repo_url` and `vercel_project_url` columns (verify via Supabase MCP)
- After provisioning completes: `SELECT github_repo_url, vercel_project_url FROM projects WHERE id = '<test-id>'` returns non-null values
- Wizard provisioning step shows "View resource" links that open the GitHub repo and Vercel dashboard
</verification>

<success_criteria>
Creating a project with GitHub + Vercel selected in the wizard results in: (1) a real GitHub repo created under the Qualia org, (2) a real Vercel project linked to it, (3) both URLs stored on the project row in Supabase. The wizard provisioning UI shows both as completed with clickable links.
</success_criteria>

<output>
After completion, create `.planning/phases/26-team-sync-daily-structure/26-08-SUMMARY.md`
</output>
