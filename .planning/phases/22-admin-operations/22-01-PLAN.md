---
phase: 22-admin-operations
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/client-portal.ts
  - components/portal/portal-admin-panel.tsx
autonomous: true

must_haves:
  truths:
    - 'Admin can select multiple CRM clients at once and create portal access for all of them in one operation'
    - 'Each client in the batch gets their own credentials card result after the batch runs'
    - 'Partial batch failures are surfaced per-client (not a single error that hides successes)'
  artifacts:
    - path: 'app/actions/client-portal.ts'
      provides: 'bulkSetupPortalForClients server action accepting crmClientIds[] and projectIds[]'
      exports: ['bulkSetupPortalForClients']
    - path: 'components/portal/portal-admin-panel.tsx'
      provides: 'Bulk setup mode toggle in setup card + multi-CRM-client select + bulk results list'
  key_links:
    - from: 'components/portal/portal-admin-panel.tsx'
      to: 'app/actions/client-portal.ts'
      via: 'bulkSetupPortalForClients call with selected crmClientIds and projectIds'
      pattern: 'bulkSetupPortalForClients'
---

<objective>
Enable Moayad to onboard multiple clients in a single operation instead of repeating the two-step flow for each client.

Purpose: Moayad regularly onboards 3-5 clients per project. Repeating the two-step form for each wastes time and risks mistakes.
Output: A "Bulk Setup" mode in the admin panel + a `bulkSetupPortalForClients` server action that runs `setupPortalForClient` in sequence and returns per-client results.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-admin-operations/PLAN.md
@.planning/phases/20-foundation-fixes/20-04-SUMMARY.md
@components/portal/portal-admin-panel.tsx
@app/actions/client-portal.ts
@~/.claude/skills/frontend-master/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bulkSetupPortalForClients server action</name>
  <files>app/actions/client-portal.ts</files>
  <action>
    Add a new exported async function `bulkSetupPortalForClients(crmClientIds: string[], projectIds: string[]): Promise<ActionResult>` to the end of the file.

    Implementation:
    - Auth check: same pattern as `setupPortalForClient` (isUserManagerOrAbove)
    - Validate: both arrays must be non-empty, return error if either is empty
    - Run `setupPortalForClient(clientId, projectIds)` sequentially for each crmClientId (not Promise.all — sequential to avoid rate limit issues with auth admin API)
    - Collect results: `Array<{ crmClientId: string; success: boolean; email?: string; name?: string; tempPassword?: string; alreadyExisted?: boolean; projectsLinked?: number; error?: string }>`
    - Return: `{ success: true, data: { results, totalSuccess: N, totalFailed: N } }` — always return success:true if at least one succeeded; return `success: false` only if ALL failed
    - Do NOT call `revalidatePath` here — each internal `setupPortalForClient` call handles its own revalidation

    Add export to the function signature so it's importable by the admin panel component.

  </action>
  <verify>npx tsc --noEmit (zero errors). Confirm function appears in exports.</verify>
  <done>bulkSetupPortalForClients is exported from app/actions/client-portal.ts and TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Bulk Setup mode to portal admin panel setup card</name>
  <files>components/portal/portal-admin-panel.tsx</files>
  <action>
    Add a "Bulk Setup" toggle button next to the "Setup Client Access" card header. Toggling switches between single-client mode (existing two-step flow) and bulk mode. Touch ONLY the setup card region — do not modify the client table section (handled in Plan 02).

    Bulk mode UI:
    - Step 1 (same): Multi-select CRM clients using checkboxes (same checkbox-button pattern as existing project multi-select). Show name + email. Min 1 selected to proceed.
    - Step 2 (same): Multi-select projects (existing project checkbox grid). Same Back/Next buttons.
    - Submit button: "Create Portal Access for {N} Clients" — calls `bulkSetupPortalForClients(selectedCrmClientIds, selectedProjectIds)`
    - Results: Show a results list (not a credentials card) — one row per client:
      - Green check + "ClientName — email/password/linked projects" if success with new account
      - Green check + "ClientName — linked to N project(s)" if already existed
      - Red X + "ClientName — error message" if failed
    - Below results, show a single "Copy All Credentials" button if any new passwords exist. Copies a plain-text block: one line per client with `Name | Email | Password | Portal: {portalUrl}` (skip clients without tempPassword).
    - After bulk setup completes, call `router.refresh()` once (not per client).

    Single-client mode: preserve existing two-step flow exactly — no changes.

    Import `bulkSetupPortalForClients` from `@/app/actions/client-portal`.

    State additions:
    - `isBulkMode: boolean` (default false)
    - `selectedCrmClientIds: string[]` (replaces selectedCrmClientId in bulk mode only)
    - `bulkResults: Array<{...}> | null`
    - `isBulkPending: boolean`

  </action>
  <verify>
    npm run build (no TypeScript or JSX errors). Manually verify in browser:
    1. Toggle "Bulk" button appears in Setup Client Access card header
    2. Bulk mode shows checkboxes for CRM clients
    3. Step 2 shows project multi-select
    4. Submit button label includes client count
  </verify>
  <done>
    Bulk mode is toggleable. Admin can select multiple CRM clients + projects and submit in one operation. Results list shows per-client outcome. "Copy All Credentials" copies a multi-line text block for new accounts.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` completes successfully
- In browser: toggle between single and bulk modes works; bulk setup with 2 test CRM clients runs and shows per-client results
</verification>

<success_criteria>
Admin can onboard multiple clients in one form submission. Bulk results are clear per-client. Copy All Credentials produces a shareable text block for all new accounts in one click.
</success_criteria>

<output>
After completion, create `.planning/phases/22-admin-operations/22-01-SUMMARY.md`
</output>
