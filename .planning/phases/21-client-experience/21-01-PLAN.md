---
phase: 21-client-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/portal/portal-whats-next-widget.tsx
  - app/portal/portal-dashboard-content.tsx
autonomous: true

must_haves:
  truths:
    - 'Client dashboard prominently shows the current phase name and its status'
    - 'Client dashboard shows the next upcoming phase name'
    - 'Phase timeline renders completed phases, active phase, and future phases in visual order'
    - 'Widget handles projects with no phases gracefully (no crash, appropriate empty state)'
    - 'Widget handles multiple projects — one section per project'
  artifacts:
    - path: 'components/portal/portal-whats-next-widget.tsx'
      provides: "What's Next widget component"
      exports: ['WhatsNextWidget']
    - path: 'app/portal/portal-dashboard-content.tsx'
      provides: 'Updated dashboard integrating WhatsNextWidget'
  key_links:
    - from: 'app/portal/portal-dashboard-content.tsx'
      to: 'components/portal/portal-whats-next-widget.tsx'
      via: 'import and render WhatsNextWidget passing projects prop'
      pattern: 'WhatsNextWidget'
    - from: 'components/portal/portal-whats-next-widget.tsx'
      to: 'ProjectWithPhases.currentPhase / nextPhase'
      via: 'props consumed directly from usePortalDashboard data'
      pattern: 'currentPhase|nextPhase'
---

<objective>
Add a "What's Next" hero widget to the client dashboard that clearly surfaces the current
and upcoming phases across all client projects. The data already exists in the API response
(currentPhase, nextPhase, progress, totalPhases, completedPhases) — this plan makes it
visually prominent instead of buried in a small progress bar.

Purpose: Clients should answer "where are we?" within 5 seconds of opening the dashboard.
Output: WhatsNextWidget component + integrated dashboard.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/portal/portal-dashboard-content.tsx
@components/portal/portal-recent-activity.tsx
@components/portal/portal-dashboard-stats.tsx
@app/actions/client-portal.ts
@lib/swr.ts
@~/.claude/skills/frontend-master/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WhatsNextWidget component</name>
  <files>components/portal/portal-whats-next-widget.tsx</files>
  <action>
    Create a new 'use client' component `WhatsNextWidget` that accepts the same
    `ProjectWithPhases[]` array already returned by `usePortalDashboard`.

    Interface (copy from portal-dashboard-content.tsx — do NOT import from there):
    ```
    interface ProjectWithPhases {
      id: string;
      name: string;
      status: string;
      project_type: string | null;
      description: string | null;
      progress: number;
      totalPhases: number;
      completedPhases: number;
      currentPhase: { name: string; status: string } | null;
      nextPhase: { name: string } | null;
    }
    ```

    Component props: `{ projects: ProjectWithPhases[]; isLoading: boolean }`

    Layout per project (render one card per project if multiple):
    - Section header: project name in small muted text
    - Large progress percentage (e.g., "67%") as a typographic hero number using
      tabular-nums, text-3xl font-semibold
    - Thin full-width progress bar below it: bg-border/50 track, bg-qualia-600 fill,
      h-1 rounded-full, width animated via `transition-all duration-500`
    - Below the bar: two columns
      - Left: "Now" label (text-[11px] muted) + current phase name (text-[13px] font-medium)
        — if no current phase show "Complete" in green
      - Right: "Next" label (text-[11px] muted) + next phase name (text-[13px] text-muted-foreground)
        — if no next phase show "—"
    - If project has zero phases: show "No phases configured" in muted text

    Loading skeleton: show Skeleton for the number, bar, and labels using shadcn Skeleton.

    Empty state (no projects): return null (the projects list component handles this).

    Wrap each project block in a `div` with class:
    `rounded-lg border border-border/40 bg-card px-5 py-4`

    If multiple projects, wrap all in `div className="space-y-3"`.

    Imports allowed: lucide-react (CheckCircle2 for complete state), shadcn Skeleton,
    cn from @/lib/utils. No framer-motion — use CSS transitions only.

  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -i "portal-whats-next" || echo "no type errors"</verify>
  <done>Component file exists, exports WhatsNextWidget, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Integrate WhatsNextWidget into dashboard</name>
  <files>app/portal/portal-dashboard-content.tsx</files>
  <action>
    Edit `portal-dashboard-content.tsx`:

    1. Import `WhatsNextWidget` from `@/components/portal/portal-whats-next-widget`

    2. In the JSX, insert `<WhatsNextWidget>` between the stats block and the
       `<PortalRecentActivity>` block. Pass `projects={projects}` and `isLoading={isLoading}`.

    Section order after change:
    1. Welcome heading
    2. PortalDashboardStats
    3. WhatsNextWidget  ← insert here
    4. PortalRecentActivity
    5. Quick links

    Add a small section label above WhatsNextWidget:
    ```
    <h2 className="text-sm font-medium text-foreground">What's next</h2>
    ```
    wrapped in a `div className="space-y-3"` together with the widget.

    No other changes to this file.

  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>Build passes, WhatsNextWidget renders between stats and projects list in dashboard</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors
2. `npm run build` completes successfully
3. Visit http://localhost:3000/portal as a client user — the "What's next" section appears
   showing current phase and next phase for each project
4. A project with 0 phases shows "No phases configured" instead of crashing
5. Loading state shows skeletons, not layout shift
</verification>

<success_criteria>

- WhatsNextWidget renders on client dashboard between stats and projects list
- Current and next phase names are visible per project
- Progress percentage and bar are displayed
- No TypeScript errors, build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/21-client-experience/21-01-SUMMARY.md`
</output>
