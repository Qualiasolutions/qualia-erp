---
phase: 21-client-experience
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/portal/portal-projects-list.tsx
  - components/portal/portal-page-header.tsx
  - app/portal/[id]/portal-project-content.tsx
autonomous: true

must_haves:
  truths:
    - 'Projects list page shows a progress bar and percentage for each project'
    - 'Project detail header shows overall phase progress (X of Y phases complete)'
    - 'Progress bar on projects list reflects completed vs total phases'
    - 'Project with zero phases shows no progress bar (not a broken 0% bar)'
    - 'Visual indicators are consistent with existing qualia-600 brand color'
  artifacts:
    - path: 'components/portal/portal-projects-list.tsx'
      provides: 'Project cards with inline progress bars'
    - path: 'components/portal/portal-page-header.tsx'
      provides: 'Page header with optional progress indicator'
    - path: 'app/portal/[id]/portal-project-content.tsx'
      provides: 'Project detail passing progress data to header'
  key_links:
    - from: 'components/portal/portal-projects-list.tsx'
      to: 'progressMap prop'
      via: 'progressMap[project.project_id] renders the bar width'
      pattern: 'progressMap'
    - from: 'app/portal/[id]/portal-project-content.tsx'
      to: 'components/portal/portal-page-header.tsx'
      via: 'passes completedPhases and totalPhases as props'
      pattern: 'completedPhases|totalPhases'
---

<objective>
Add visual progress bars throughout the portal to replace text-only status. Two surfaces:
1. Projects list — inline progress bar on each project row
2. Project detail header — "X of Y phases complete" with a progress bar

The data already exists (project_phases queries) — this plan surfaces it visually.

Purpose: Clients see at a glance how far along their project is without reading text.
Output: Updated portal-projects-list, portal-page-header, portal-project-content.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/portal/portal-projects-list.tsx
@components/portal/portal-page-header.tsx
@app/portal/[id]/portal-project-content.tsx
@lib/swr.ts
@~/.claude/skills/frontend-master/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Progress bars on projects list</name>
  <files>components/portal/portal-projects-list.tsx</files>
  <action>
    Read the full current file first. The component receives `progressMap?: Record<string, number>`.
    This map already exists as a prop but is not visually rendered beyond what the calling page provides.

    The projects list page (`app/portal/projects/`) passes `progressMap`. Confirm whether the
    calling page populates it with phase data — if it doesn't, that's a data gap to fix in this task
    by checking `app/portal/projects/page.tsx`.

    **In portal-projects-list.tsx**, add progress rendering to each project row:

    After the project name + badge + description block, add:
    ```
    {typeof progress === 'number' && project.totalPhases > 0 && (
      <div className="mt-2 flex items-center gap-2">
        <div className="h-1 flex-1 overflow-hidden rounded-full bg-border/40">
          <div
            className="h-full rounded-full bg-qualia-600 transition-all duration-500"
            style={{ width: `${progress}%` }}
          />
        </div>
        <span className="shrink-0 text-[11px] tabular-nums text-muted-foreground/50">
          {progress}%
        </span>
      </div>
    )}
    ```

    Where `progress` = `progressMap[clientProject.project_id] ?? progressMap[project.id]`.

    If the projects list page (`app/portal/projects/page.tsx`) does not currently build
    `progressMap` from phase data, add the query: fetch `project_phases` for all project IDs,
    compute percentage, and pass as `progressMap`. Follow the same pattern used in
    `getClientDashboardProjects` in `app/actions/client-portal.ts`.

    Do NOT add totalPhases to the interface if it's not in the data shape — only use what
    `progressMap` already provides (a 0-100 number per project_id).

    Keep the visual minimal: `h-1` bar, same qualia-600 color, no animation beyond the CSS
    transition already on the bar width.

  </action>
  <verify>npx tsc --noEmit 2>&1 | grep "portal-projects-list\|projects/page" || echo "clean"</verify>
  <done>Projects list shows a progress bar for each project that has phases; zero-phase projects show no bar</done>
</task>

<task type="auto">
  <name>Task 2: Progress indicator in project detail header</name>
  <files>
    components/portal/portal-page-header.tsx
    app/portal/[id]/portal-project-content.tsx
  </files>
  <action>
    **portal-page-header.tsx** — extend the component to accept optional progress props:

    Add to interface:
    ```
    completedPhases?: number;
    totalPhases?: number;
    ```

    Below the description paragraph, add (only when both values are present and totalPhases > 0):
    ```
    <div className="mt-3 flex items-center gap-3">
      <div className="h-1 w-48 overflow-hidden rounded-full bg-border/40">
        <div
          className="h-full rounded-full bg-qualia-600 transition-all duration-700"
          style={{ width: `${Math.round((completedPhases / totalPhases) * 100)}%` }}
        />
      </div>
      <span className="text-[12px] text-muted-foreground/60">
        {completedPhases} of {totalPhases} phases complete
      </span>
    </div>
    ```

    The header is a server component (no 'use client') — keep it that way. These are plain
    props, no hooks needed.

    ---

    **portal-project-content.tsx** — the component calls `usePortalProjectWithPhases` which
    already returns `phases`. Compute the counts client-side:

    After `const { project, phases, ... } = usePortalProjectWithPhases(projectId);`

    Add:
    ```
    const totalPhases = phases?.length ?? 0;
    const completedPhases = (phases ?? []).filter(
      (p) => p.status === 'completed' || p.status === 'done'
    ).length;
    ```

    Pass to PortalPageHeader:
    ```
    <PortalPageHeader
      title={project.name}
      description={project.description}
      completedPhases={completedPhases}
      totalPhases={totalPhases}
    />
    ```

    Only add these two derived values — no other changes to this file.

  </action>
  <verify>
    npx tsc --noEmit 2>&1 | grep "portal-page-header\|portal-project-content" || echo "clean"
    npm run build 2>&1 | tail -5
  </verify>
  <done>
    - portal-page-header accepts and renders completedPhases/totalPhases when provided
    - portal-project-content passes phase counts to header
    - Build passes, no TypeScript errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` passes
3. Visit /portal/projects as a client — each project row with phases shows a progress bar
4. Visit /portal/[project-id] — header shows "X of Y phases complete" with a progress bar
5. A project with 0 phases shows no broken bar on either surface
</verification>

<success_criteria>

- Progress bars appear on projects list and project detail header
- Visual uses consistent qualia-600 color and h-1 thin-bar pattern
- No TypeScript errors, build passes
- Zero-phase projects handled gracefully (no bar rendered)
  </success_criteria>

<output>
After completion, create `.planning/phases/21-client-experience/21-03-SUMMARY.md`
</output>
