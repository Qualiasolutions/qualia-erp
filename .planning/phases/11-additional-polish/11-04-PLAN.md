---
phase: 11-additional-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/schedule-utils.ts
  - lib/schedule-shared.ts
autonomous: true

must_haves:
  truths:
    - Schedule logic consolidated into single utilities file
    - No duplicate logic between schedule files
    - All schedule components continue working without changes
  artifacts:
    - path: lib/schedule-utils.ts
      provides: All schedule utilities including types, timezone, converters
      min_lines: 350
    - path: lib/schedule-shared.ts
      provides: Removed (content merged into schedule-utils.ts)
      exists: false
  key_links:
    - from: components/today-dashboard/daily-schedule-grid.tsx
      to: lib/schedule-utils.ts
      via: import schedule types and helpers
      pattern: 'from.*schedule-utils'
---

<objective>
Consolidate schedule utility files from 3 to 2 by merging schedule-shared.ts into schedule-utils.ts.

Purpose: Eliminate duplication between schedule utility files and provide single source for schedule types, timezone handling, and conversion helpers. Reduce maintenance burden.

Output: schedule-shared.ts content merged into schedule-utils.ts, all imports updated, file deleted.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Merge schedule-shared.ts into schedule-utils.ts</name>
  <files>
    lib/schedule-utils.ts
    lib/schedule-shared.ts
  </files>
  <action>
Consolidate schedule files:

1. Read schedule-shared.ts content (126 lines):
   - Timezone constants (TIMEZONE_CYPRUS, TIMEZONE_JORDAN)
   - ScheduleItemType, ScheduleTask, ScheduleMeeting types
   - Type guards (isScheduleMeeting, isScheduleTask)
   - useTimezone hook
   - tasksToScheduleItems converter

2. Append to schedule-utils.ts (after imports, before functions):
   - Add 'use client' directive at top (needed for useTimezone hook)
   - Add React import: `import { useState, useEffect } from 'react';`
   - Timezone constants
   - All schedule item types and interfaces
   - Type guards
   - useTimezone hook
   - tasksToScheduleItems converter

3. Organization in schedule-utils.ts:

   ```typescript
   'use client';

   import { isToday, parseISO } from 'date-fns';
   import { useState, useEffect } from 'react';
   import type { Task } from '@/app/actions/inbox';
   import { TimeBlock, TIME_BLOCKS, parseTimeToMinutes, TaskPriority } from './schedule-constants';

   // === Timezone Constants ===
   export const TIMEZONE_CYPRUS = 'Europe/Nicosia';
   // ... rest from schedule-shared

   // === Schedule Types ===
   export type ScheduleItemType = ...;
   // ... types from schedule-shared

   // === Type Guards ===
   export function isScheduleMeeting(...) { ... }

   // === Hooks ===
   export function useTimezone() { ... }

   // === Converters ===
   export function tasksToScheduleItems(...) { ... }

   // === Scoring and Filtering (existing code) ===
   const PRIORITY_SCORES = ...;
   // ... rest of existing schedule-utils functions
   ```

4. Note: schedule-utils.ts becomes client-side only (needed for useTimezone hook)
   - This is acceptable - task scoring and filtering don't need server-side execution
   - schedule-constants.ts remains pure (no 'use client') for constants

5. DO NOT delete schedule-shared.ts yet (done in next task after imports updated)
   </action>
   <verify>
   Read lib/schedule-utils.ts - confirm 'use client' directive, merged types, useTimezone hook, and existing functions all present
   </verify>
   <done>
   schedule-utils.ts contains all schedule-shared.ts content plus original functions, properly organized with 'use client' directive
   </done>
   </task>

<task type="auto">
  <name>Update imports and delete schedule-shared.ts</name>
  <files>lib/schedule-shared.ts</files>
  <action>
Update component imports and delete old file:

1. Find all files importing from schedule-shared:

   ```bash
   grep -r "from.*schedule-shared" --include="*.tsx" --include="*.ts"
   ```

2. Update each import:
   - Change `from '@/lib/schedule-shared'` to `from '@/lib/schedule-utils'`
   - Verify imported items exist in updated schedule-utils.ts
   - Common imports: `useTimezone`, `ScheduleItem`, `ScheduleMeeting`, `ScheduleTask`, `isScheduleMeeting`, `isScheduleTask`, `tasksToScheduleItems`, `TIMEZONE_CYPRUS`, `TIMEZONE_JORDAN`

3. Expected files to update (based on codebase analysis):
   - components/today-dashboard/daily-schedule-grid.tsx
   - components/schedule/\*.tsx (if any)
   - Any other files found by grep

4. After all imports updated, delete schedule-shared.ts:

   ```bash
   rm lib/schedule-shared.ts
   ```

5. Verify no remaining references:

   ```bash
   grep -r "schedule-shared" --include="*.tsx" --include="*.ts"
   ```

   Should return no results.

6. Run TypeScript check:
   ```bash
   npx tsc --noEmit
   ```
   Should pass with no errors.
   </action>
   <verify>

- Grep shows no remaining schedule-shared imports
- schedule-shared.ts file deleted
- TypeScript check passes
- Components importing from schedule-utils.ts
  </verify>
  <done>
  All imports updated to schedule-utils.ts, schedule-shared.ts deleted, TypeScript compilation passes
  </done>
  </task>

</tasks>

<verification>
Code check:
```bash
# File deleted
ls lib/schedule-shared.ts  # Should fail

# No remaining references

grep -r "schedule-shared" --include="_.tsx" --include="_.ts" # Should be empty

# TypeScript compiles

npx tsc --noEmit # Should pass

# Verify schedule-utils exports

grep -n "export" lib/schedule-utils.ts | grep -E "useTimezone|ScheduleItem|tasksToScheduleItems"

```

Manual testing:
1. Visit dashboard - schedule grid should render without errors
2. Toggle timezone - should switch between Cyprus/Jordan times
3. Check browser console - no import errors
</verification>

<success_criteria>
- [ ] schedule-shared.ts content merged into schedule-utils.ts
- [ ] All components import from schedule-utils.ts (not schedule-shared)
- [ ] schedule-shared.ts file deleted
- [ ] TypeScript compilation passes
- [ ] Schedule components work without changes
- [ ] File count reduced from 3 to 2 (schedule-utils + schedule-constants)
</success_criteria>

<output>
After completion, create `.planning/phases/11-additional-polish/11-04-SUMMARY.md`
</output>
```
