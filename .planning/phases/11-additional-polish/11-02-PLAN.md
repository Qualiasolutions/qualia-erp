---
phase: 11-additional-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/portal/portal-activity-feed.tsx
  - components/portal/portal-roadmap.tsx
  - components/portal/portal-file-list.tsx
  - components/project-overview.tsx
autonomous: true

must_haves:
  truths:
    - Portal pages show consistent date formatting across all components
    - Dates use centralized formatting from lib/utils.ts
    - No inline format() calls with hardcoded format strings in portal
  artifacts:
    - path: components/portal/portal-activity-feed.tsx
      provides: Standardized date formatting via utils
      contains: 'formatDate|formatTime'
      min_lines: 150
    - path: components/portal/portal-roadmap.tsx
      provides: Standardized date formatting via utils
      contains: 'formatDate'
      min_lines: 300
    - path: components/portal/portal-file-list.tsx
      provides: Standardized date formatting via utils
      contains: 'formatDate|formatRelativeTime'
      min_lines: 100
    - path: components/project-overview.tsx
      provides: Standardized date formatting via utils
      contains: 'formatDate'
      min_lines: 200
  key_links:
    - from: components/portal/*
      to: lib/utils.ts
      via: import formatDate, formatRelativeTime
      pattern: 'import.*formatDate.*from.*utils'
---

<objective>
Standardize date formatting across portal pages using centralized utilities.

Purpose: Eliminate inconsistent date formatting (direct format() calls with varying format strings) and ensure all portal components use the same date presentation style.

Output: Portal components use `formatDate`, `formatDateTime`, `formatRelativeTime` from lib/utils.ts instead of inline format() calls.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Standardize date formatting in portal components</name>
  <files>
    components/portal/portal-activity-feed.tsx
    components/portal/portal-roadmap.tsx
    components/portal/portal-file-list.tsx
    components/project-overview.tsx
  </files>
  <action>
Replace inline date-fns format() calls with centralized utilities:

**portal-activity-feed.tsx:**

- Remove local `formatTime()` function (lines 56-59)
- Replace `format(date, 'MMMM d, yyyy')` (line 53) with `formatDate(date, 'MMMM d, yyyy')` from utils
- Replace `format(date, 'h:mm a')` (line 58) with `formatDate(date, 'h:mm a')` from utils
- Add import: `import { formatDate } from '@/lib/utils';`

**portal-roadmap.tsx:**

- Replace `format(new Date(phase.start_date), 'MMM d, yyyy')` (line 211) with `formatDate(phase.start_date)`
- Replace `format(new Date(phase.target_date), 'MMM d, yyyy')` (line 229) with `formatDate(phase.target_date)`
- Import formatDate from utils (already has date-fns import, replace with utils import)

**portal-file-list.tsx:**

- Search for any format() calls with hardcoded strings
- Replace with formatDate() or formatRelativeTime() from utils as appropriate
- If no format calls exist, verify and skip

**project-overview.tsx:**

- Already uses formatDate from utils (lines 4, 95, 104) - verify consistency
- No changes needed if already using utils

Implementation notes:

- lib/utils.ts formatDate() uses DATE_FORMAT constant ('MMM d, yyyy') by default
- For custom formats, pass format string as second param: formatDate(date, 'MMMM d, yyyy')
- formatRelativeTime() returns "X ago" format
- Do NOT change dashboard components (out of scope - only portal)
  </action>
  <verify>
  Grep for 'format\(' in components/portal/ and components/project-overview.tsx - should only find imports from utils, not direct date-fns usage
  </verify>
  <done>
  All portal components use formatDate/formatDateTime/formatRelativeTime from lib/utils.ts, no inline format() calls with hardcoded date format strings
  </done>
  </task>

</tasks>

<verification>
Code check:
```bash
# Should find NO direct format() usage (only imports)
grep -n "format(.*date" components/portal/*.tsx components/project-overview.tsx

# Should find standardized imports

grep -n "formatDate.*from.*utils" components/portal/\*.tsx components/project-overview.tsx

```

Manual testing:
1. Visit portal activity page - dates should display as before
2. Visit portal roadmap - phase dates should display as before
3. Visit project overview - dates should display consistently
</verification>

<success_criteria>
- [ ] All portal components import formatDate/formatRelativeTime from lib/utils.ts
- [ ] No inline format() calls with hardcoded format strings in portal
- [ ] Date formatting is consistent across portal pages
- [ ] No visual regressions (dates display as before)
</success_criteria>

<output>
After completion, create `.planning/phases/11-additional-polish/11-02-SUMMARY.md`
</output>
```
