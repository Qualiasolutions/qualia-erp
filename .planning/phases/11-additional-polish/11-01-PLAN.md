---
phase: 11-additional-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/activity-feed.ts
  - components/portal/portal-activity-feed.tsx
autonomous: true

must_haves:
  truths:
    - User can load more activity feed items without full page reload
    - Activity feed shows loading state while fetching more items
    - Load more button disappears when all items are loaded
  artifacts:
    - path: app/actions/activity-feed.ts
      provides: Cursor-based pagination support
      exports: ['getProjectActivityFeed']
      min_lines: 130
    - path: components/portal/portal-activity-feed.tsx
      provides: Load more button with cursor tracking
      min_lines: 180
  key_links:
    - from: components/portal/portal-activity-feed.tsx
      to: app/actions/activity-feed.ts
      via: getProjectActivityFeed with cursor param
      pattern: 'getProjectActivityFeed.*cursor'
---

<objective>
Implement cursor-based pagination for portal activity feed with incremental loading.

Purpose: Improve performance and UX for projects with long activity histories by loading items in chunks instead of all at once.

Output: Portal activity feed with "Load more" button that fetches items in batches of 20 using cursor-based pagination.
</objective>

<execution_context>
@/home/qualia/.claude/get-shit-done/workflows/execute-plan.md
@/home/qualia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Add cursor-based pagination to activity feed server action</name>
  <files>app/actions/activity-feed.ts</files>
  <action>
Update getProjectActivityFeed to support cursor-based pagination:

1. Add `cursor` parameter (optional string - ISO timestamp of last item)
2. Modify query:
   - If cursor provided, add `.lt('created_at', cursor)` to query (fetch items older than cursor)
   - Keep existing ordering: `.order('created_at', { ascending: false })`
   - Default limit remains 20 (not 50)
3. Return structure: `{ success: true, data: { items: normalized, hasMore: boolean, nextCursor: string | null } }`
   - `hasMore`: true if returned items.length === limit (more items likely exist)
   - `nextCursor`: created_at of last item in returned array (null if no items)
4. Maintain backward compatibility for existing callers (dashboard still uses simple array return)

Implementation notes:

- Use `.lt()` not `.lte()` to avoid duplicate items
- Cursor is ISO timestamp string from `created_at` field
- Normalize FK response as before with `normalizeFKResponse`
  </action>
  <verify>
  Read app/actions/activity-feed.ts and confirm cursor parameter, hasMore logic, and return structure
  </verify>
  <done>
  getProjectActivityFeed accepts cursor param and returns paginated response with hasMore and nextCursor
  </done>
  </task>

<task type="auto">
  <name>Add Load More UI to portal activity feed</name>
  <files>components/portal/portal-activity-feed.tsx</files>
  <action>
Update PortalActivityFeed to support pagination:

1. Add state:
   - `activities` (local state initialized with props.activities)
   - `cursor` (string | null)
   - `hasMore` (boolean, initially true)
   - `isLoading` (boolean for load more)

2. Create loadMore function:
   - Set isLoading true
   - Call getProjectActivityFeed with current cursor
   - Append new items to activities array
   - Update cursor and hasMore from response
   - Set isLoading false
   - Handle errors with silent failure (just stop loading)

3. Add Load More button after timeline:
   - Only show if hasMore is true
   - Disabled during loading
   - Shows spinner icon when loading
   - Text: "Load more" (not loading) or "Loading..." (loading)
   - Styling: Centered, secondary button, mb-8

4. Props change: Accept `projectId: string` (needed for loadMore action call)

Implementation notes:

- Use transition hook for smoother updates
- De-duplicate by id when appending (in case of race conditions)
- Maintain date grouping after appending new items
  </action>
  <verify>
  Read components/portal/portal-activity-feed.tsx and confirm state management, loadMore function, and button rendering
  </verify>
  <done>
  Portal activity feed shows Load More button, fetches additional items on click, appends to list, and hides button when no more items
  </done>
  </task>

</tasks>

<verification>
Manual testing:
1. Visit portal page for project with 20+ activity items
2. Scroll to bottom, click "Load more"
3. Verify:
   - New items append to existing list
   - No duplicate items
   - Button disappears when all items loaded
   - Loading state shows during fetch
</verification>

<success_criteria>

- [ ] User can click "Load more" to fetch additional activity items
- [ ] Items append to existing list without duplicates
- [ ] Button shows loading state during fetch
- [ ] Button disappears when all items are loaded
- [ ] Date grouping maintained after loading more items
      </success_criteria>

<output>
After completion, create `.planning/phases/11-additional-polish/11-01-SUMMARY.md`
</output>
